# Fix round 1 review (verbatim, as relayed by the coordinator)

Persisted before folding, per STANDARD_WORKFLOW.md's "persist each reviewer's output verbatim before
folding it."

---

Fix round 1 — one pass, then merge and a crates.io release. Both lenses credited the layout arithmetic (correct in all 16 combinations *by construction*, via the single `next_idx` cursor — no case analysis needed), the liveness predicate (it uses `tranche_guard::void_targets` and genuinely dodges the BG-D9 deferral trap), and the export-leak guards (real exports, byte-scans, and AcroForm text decoding that would catch a UTF-16BE leak). Good work.

Six blocking items and four that must land before publish.

**1. [Important] "Two defects" is an exhaustive count and the repo documents a THIRD.** `experimental.rs:53-55` and `NOTICE:79-81`. The third is the subject of `design/stale-snapshot-latch/DESIGN.md` §0.1: `promote_flow_review` built `filed_basis`/`coverage` from a stale `app.snapshot`, and that number becomes **Form 8949 column (e)**. It shipped in v0.10.0, and §0.1 records that the justification for deferring it was "**False**" — i.e. it was established as filing-affecting only by later review, the notice's exact predicate. It is also the *worst* of the three: a wrong basis figure on a filed form, versus a clipped narrative and a bypassable status promise.
Fix: drop the count. `summary` → "Defects that affect what gets FILED have shipped and were found only by later review; all known ones are fixed." Make `defects` exemplary rather than exhaustive, and add: "a filed basis figure could be derived from a stale in-editor ledger image after a failed re-projection". That wording is robust whether or not a fourth ever turns up.

**2. [Important] The banners hand-copy the facts instead of deriving them.** `btctax-tui/src/draw.rs:162` and `btctax-tui-edit/src/draw_edit.rs:425` carry a byte-identical literal asserting "(now fixed)"; only `NOTICE.title` is shared. `summary`/`defects`/`action` are consumed by nothing but `plain_text()`, so the struct's decomposition is exercised by no front-end. Change a fact and both banners keep reassuring the filer, with nothing red.
**This is the same defect class already fixed in `draw_edit.rs:7673`** — read that comment; it says only `STALE_FACT_1` was shared and the sentence had been reworded twice with no test able to catch a third drift. Apply the same remedy: put a one-row rendering in core beside `plain_text()` (e.g. `ExperimentalNotice::one_line()`), have both TUIs call it, and add a drift test — mutation: reword `NOTICE.summary`, the banner test must red.

**3. [Important] The feature's own screen has no banner.** `draw_defensive_filing` (`draw_edit.rs:99-200`) renders the dashboard, the declare flow and the promote flow, and `open_defensive_filing` is its only entry — nothing in the declare/promote/export tails routes back to Browse. So a filer can declare, promote and export the entire Approach-B journey and never see the notice. Insert the same `Constraint::Length(1)` row there, gated on `uses_approach_b`, composing with the existing stale marker the way Browse composes with PSEUDO. Extend the composition KAT to that screen.

**4. [Important] `export-snapshot` writes the disclosure files and emits nothing.** `cmd/admin.rs:188-221`: it runs the same `promote_export_gate`, writes `form_8275.txt` (`:193`) — or one per promoted year on the all-years arm (`:212-218`) — plus `basis_methodology.txt`. That is the CSV/preparer handoff path. Compute `uses_approach_b` there, surface it on `ExportReport`, and `eprintln!` from the `main.rs` arm beside the existing blocker disclosure. Add the fourth directory-wide leak guard for that out_dir.

**5. [Important] The call to action is unperformable by its own audience.** `experimental.rs:60-61` says check every figure "against your own records" — but the scope line one field above says this feature is for a tranche of **undocumented** BTC. There are no records; that is the premise. And defect 2 is stated as a verbatim internal status string with no consequence.
Replace `action` with checks tied to what this actually produces: (a) open `form_8275.pdf` and confirm the Part II narrative renders **whole**, continuing onto page 2's Part IV, rather than stopping mid-sentence after about one line; (b) confirm the basis in Form 8949 column (e) for each promoted lot equals the floor you consented to at promote time; (c) confirm the tranche quantity and acquisition window on the 8275 match what you declared. Restate defect 2 by consequence: "a change the editor said had been rolled back could still be written to your vault, silently changing the figures on your forms".

**6. [Important] The presentation-neutrality contract has no test.** `experimental.rs:19,33` claims no ANSI, no assumed width, no terminal formatting in the data — and it holds only because the literals use `\`-continuations. Change one `\` to `\n` and `summary`/`action` become hard-wrapped indented blobs a web UI must re-parse, with every test still green. Add a test over `NOTICE` asserting, for `title`, `summary`, `action` and each `defects` element: no `\n`, `\r`, `\u{1b}`, `\t`, and no run of two or more spaces. Mutation: swap a `\` for `\n` → reds.

**Before publish — these are free now and awkward or breaking afterwards:**
- **`#[non_exhaustive]` on `ExperimentalNotice`** and **derive `serde::Serialize`**. All fields are `pub`, so adding one later (a link, a severity, a version) is breaking; and the stated consumer is a web UI, which will want to serialize it. `btctax-core` publishes to crates.io in the next step.
- **`LIMITATIONS.md`** (`crates/btctax-cli/`) — this is what `btctax limitations` prints and what the CLI's NOT-AUTHORISED banner tells the filer to read. It mirrors `NOTICE`'s legal sections but gained no EXPERIMENTAL section. Add one. Same for `README.md`'s "Defensive filing" section.
- **`experimental.rs:65`** — `plain_text`'s doc still cites "the TUI export-directory file", the artifact `7a9d184` deliberately removed and the module's own constraint forbids. Drop the clause.
- **`cmd/tranche.rs:137` / `cmd/promote.rs:238`** — the post-write ledger re-read uses `?`, so a failure in a disclosure-only step turns a *succeeded* declare into a non-zero exit, and the natural response (re-run) creates a duplicate tranche. The predicate is trivially true there anyway. Reuse the loaded events or drop the `?`.

Nits worth folding while you are in these files: share one `NOTICE_NEEDLES` const (the three copies drifted — two are missing `"check every figure"`); rename `IrsPdfReport::experimental_notice` to say it is a flag; drop the double newline in the `eprintln!`.

Gate: `make check` AND `cargo fmt --all -- --check`. Mutations via `cp` backup, never `git checkout --`. Every mutation record TRUE.

Return only: status, commit SHAs, one-line test summary, concerns.
