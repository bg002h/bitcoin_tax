warning: function `params_years_whose_forms_cannot_be_read` is never used
    --> crates/xtask/src/blockers.rs:1463:8
     |
1463 | pub fn params_years_whose_forms_cannot_be_read(
     |        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
     |
     = note: `#[warn(dead_code)]` (part of `#[warn(unused)]`) on by default

# TY2026 blockers — DERIVED at HEAD, 58 rows (15 UNMEASURED)

Generated by `cargo run -p xtask -- blockers 2026`. Every row names its evidence; **UNMEASURED** means the fact behind it was not measured, which is not the same as fine.

## clears itself in January — waiting only on an IRS document — 41 row(s)

| state | when | what | evidence |
|---|---|---|---|
| BLOCKED | January 2027 package | `f1040s1` is not bundled for TY2026 — no `forms/2026/f1040s1.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = edits (1 respelled, 0 added, 0 removed, 0 lines moved, 0 retired, 0 introduced, 3 meanings changed; 3 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f1040s1` = "TY2026 revision not released; draft archived (its label set cannot be read, FR-58); January 2027 package"; `xtask port-status` row `f1040s1`; archive holds 2020, 2021, 2022, 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f1040s1a` is not bundled for TY2026 — no `forms/2026/f1040s1a.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = **REBUILT** (44 respelled, 131 added, 0 removed, 44 lines moved, 7 retired, 28 introduced, 33 meanings changed; 2 boxes UNREAD, 3 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f1040s1a` = "TY2026 revision not released; draft archived (REBUILT: 175 added fields); January 2027 package"; `xtask port-status` row `f1040s1a`; archive holds 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f1040s2` is not bundled for TY2026 — no `forms/2026/f1040s2.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = edits (15 respelled, 9 added, 4 removed, 25 lines moved, 15 retired, 22 introduced, 21 meanings changed; 2 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f1040s2` = "TY2026 revision not released; draft archived; January 2027 package"; `xtask port-status` row `f1040s2`; archive holds 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f1040s3` is not bundled for TY2026 — no `forms/2026/f1040s3.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = edits (2 respelled, 1 added, 0 removed, 3 lines moved, 0 retired, 1 introduced, 4 meanings changed; 2 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f1040s3` = "TY2026 revision not released; draft archived; January 2027 package"; `xtask port-status` row `f1040s3`; archive holds 2020, 2021, 2022, 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f1040sa` is not bundled for TY2026 — no `forms/2026/f1040sa.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = **REBUILT** (1 respelled, 31 added, 18 removed, 0 lines moved, 0 retired, 13 introduced, 10 meanings changed; 0 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f1040sa` = "TY2026 revision not released; draft archived (REBUILT); January 2027 package"; `xtask port-status` row `f1040sa`; archive holds 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f1040sb` is not bundled for TY2026 — no `forms/2026/f1040sb.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = transfers (0 respelled, 0 added, 0 removed, 0 lines moved, 0 retired, 0 introduced, 2 meanings changed; 2 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f1040sb` = "TY2026 revision not released; draft archived; January 2027 package"; `xtask port-status` row `f1040sb`; archive holds 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f1040sc` is not bundled for TY2026 — no `forms/2026/f1040sc.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = edits (45 respelled, 5 added, 1 removed, 33 lines moved, 0 retired, 1 introduced, 16 meanings changed; 22 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f1040sc` = "TY2026 revision not released; draft archived (REBUILT); January 2027 package"; `xtask port-status` row `f1040sc`; archive holds 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f6251` is not bundled for TY2026 — no `forms/2026/f6251.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = transfers (0 respelled, 0 added, 0 removed, 0 lines moved, 0 retired, 0 introduced, 8 meanings changed; 2 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f6251` = "TY2026 revision not released; draft archived (line 1 → 1a/1b restructure expected); January 2027 package"; `xtask port-status` row `f6251`; archive holds 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f8959` is not bundled for TY2026 — no `forms/2026/f8959.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = transfers (0 respelled, 0 added, 0 removed, 0 lines moved, 0 retired, 0 introduced, 12 meanings changed; 2 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f8959` = "TY2026 revision not released; draft archived; January 2027 package"; `xtask port-status` row `f8959`; archive holds 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f8960` is not bundled for TY2026 — no `forms/2026/f8960.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = transfers (0 respelled, 0 added, 0 removed, 0 lines moved, 0 retired, 0 introduced, 2 meanings changed; 5 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f8960` = "TY2026 revision not released; draft archived; January 2027 package"; `xtask port-status` row `f8960`; archive holds 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f8995` is not bundled for TY2026 — no `forms/2026/f8995.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = edits (2 respelled, 12 added, 9 removed, 8 lines moved, 0 retired, 6 introduced, 4 meanings changed; 0 boxes UNREAD, 1 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f8995` = "TY2026 revision not released; draft archived (REBUILT); January 2027 package"; `xtask port-status` row `f8995`; archive holds 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `f8995a` is not bundled for TY2026 — no `forms/2026/f8995a.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = edits (0 respelled, 3 added, 0 removed, 0 lines moved, 0 retired, 3 introduced, 6 meanings changed; 2 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f8995a` = "TY2026 revision not released; draft archived; January 2027 package"; `xtask port-status` row `f8995a`; archive holds 2024, 2025, 2026-DRAFT |
| BLOCKED | when the IRS posts it | `i1040gi` is read as a PER-REVISION family: the module derives its revision set from `design/forms/extract/`, so archiving `i1040gi--2026` REDS the suite and makes the transcription due — it does not silently clear, and it does not silently pass | crates/btctax-core/src/tax/state_local_refund.rs:75 |
| BLOCKED | January 2027 package | `schedule_d` is not bundled for TY2026 — no `forms/2026/schedule_d.map.toml` and no template, so the form cannot be filled. the FORM = unchanged, field map = transfers (0 respelled, 0 added, 0 removed, 0 lines moved, 0 retired, 0 introduced, 0 meanings changed; 4 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] schedule_d` = "TY2026 revision not released; draft archived; January 2027 package"; `xtask port-status` row `f1040sd`; archive holds 2020, 2021, 2024, 2025, 2026-DRAFT |
| BLOCKED | January 2027 package | `schedule_se` is not bundled for TY2026 — no `forms/2026/schedule_se.map.toml` and no template, so the form cannot be filled. the FORM = **CHANGED**, field map = edits (2 respelled, 0 added, 0 removed, 0 lines moved, 0 retired, 0 introduced, 5 meanings changed; 3 boxes UNREAD, 0 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] schedule_se` = "TY2026 revision not released; draft archived; January 2027 package"; `xtask port-status` row `f1040sse`; archive holds 2024, 2025, 2026-DRAFT |
| BLOCKED | when the IRS posts it | no TY2026 revision of `f1040` (form) is archived; the newest is `f1040--2025`, and 9 source file(s) read this family | `design/forms/extract/` holds f1040--{2020,2021,2022,2023,2024,2025}; cited by crates/btctax-cli/src/lib.rs, crates/btctax-core/src/tax/dependents_statement.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `f1040s8` (form) is archived; the newest is `f1040s8--2024`, and 1 source file(s) read this family | `design/forms/extract/` holds f1040s8--{2020,2021,2022,2024}; cited by crates/btctax-forms/tests/attestation.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `f1098` (form) is archived; the newest is `f1098--2025`, and 1 source file(s) read this family | `design/forms/extract/` holds f1098--{2022,2025}; cited by crates/btctax-input-form/src/spec/sections.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `f1099int` (form) is archived; the newest is `f1099int--2024`, and 1 source file(s) read this family | `design/forms/extract/` holds f1099int--{2024}; cited by crates/xtask/src/box_census.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `f1099sa` (form) is archived; the newest is `f1099sa--2025`, and 3 source file(s) read this family | `design/forms/extract/` holds f1099sa--{2019,2025}; cited by crates/btctax-input-form/src/seam.rs, crates/xtask/src/box_census.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `f8283` (form) is archived — and it is PERIODIC, so there may never be one: periodic (Rev. 12-2025) — served from forms/2025/ by bundled::periodic_template until a later revision is archived | `design/forms/extract/` holds f8283--{2024,2025}; cited by crates/btctax-forms/tests/supported_years_cross_product.rs, crates/xtask/src/forms_extract.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `f8615` (form) is archived; the newest is `f8615--2025`, and 1 source file(s) read this family | `design/forms/extract/` holds f8615--{2025}; cited by crates/btctax-core/src/tax/return_inputs.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `f8889` (form) is archived; the newest is `f8889--2025`, and 5 source file(s) read this family | `design/forms/extract/` holds f8889--{2024,2025}; cited by crates/btctax-core/src/tax/form8889.rs, crates/btctax-core/src/tax/tables.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `i1040gi` (instructions) is archived; the newest is `i1040gi--2025`, and 17 source file(s) read this family | `design/forms/extract/` holds i1040gi--{2024,2025}; cited by crates/btctax-adapters/src/tax_tables.rs, crates/btctax-cli/src/lib.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `i1040sca` (instructions) is archived; the newest is `i1040sca--2025`, and 6 source file(s) read this family | `design/forms/extract/` holds i1040sca--{2024,2025}; cited by crates/btctax-adapters/src/tax_tables.rs, crates/btctax-core/src/tax/advisories.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `i1040sd` (instructions) is archived; the newest is `i1040sd--2025`, and 1 source file(s) read this family | `design/forms/extract/` holds i1040sd--{2024,2025}; cited by crates/btctax-core/src/tax/capital_loss_carryover.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `i1099sa` (instructions) is archived; the newest is `i1099sa--2025`, and 1 source file(s) read this family | `design/forms/extract/` holds i1099sa--{2024,2025}; cited by crates/xtask/src/prompt_check.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `i8275` (instructions) is archived; the newest is `i8275--2024`, and 2 source file(s) read this family | `design/forms/extract/` holds i8275--{2024}; cited by crates/btctax-core/src/tax/advisories.rs, crates/btctax-core/src/tax/form8275.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `i8283` (instructions) is archived; the newest is `i8283--2025`, and 3 source file(s) read this family | `design/forms/extract/` holds i8283--{2024,2025}; cited by crates/btctax-forms/src/map.rs, crates/btctax-forms/tests/full_return_forms.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `i8615` (instructions) is archived; the newest is `i8615--2025`, and 4 source file(s) read this family | `design/forms/extract/` holds i8615--{2025}; cited by crates/btctax-core/src/tax/return_1040.rs, crates/btctax-core/src/tax/return_inputs.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `i8949` (instructions) is archived; the newest is `i8949--2025`, and 9 source file(s) read this family | `design/forms/extract/` holds i8949--{2024,2025}; cited by crates/btctax-cli/tests/export_irs_pdf.rs, crates/btctax-forms/src/fill8949.rs |
| BLOCKED | when the IRS posts it | no TY2026 revision of `i8960` (instructions) is archived; the newest is `i8960--2025`, and 2 source file(s) read this family | `design/forms/extract/` holds i8960--{2024,2025}; cited by crates/btctax-core/src/tax/other_taxes.rs, crates/btctax-forms/tests/golden_packet.rs |
| BLOCKED | with the year's document | the gate *FullReturnParams are bundled (THE compute gate)* is shut for TY2026 and OPEN for [2024] — it refuses BECAUSE the year is 2026: no FullReturnParams — the full return does not compute | btctax-adapters/src/tax_tables.rs full_return_for (probed at [2024, 2025, 2026]) |
| BLOCKED | with the year's document | the gate *the bundled price dataset reaches the year's prices_through* is shut for TY2026 and OPEN for [2024, 2025] — it refuses BECAUSE the year is 2026: usage: cannot export TY2026: the bundled price dataset ends 2026-06-03, before TY2026's prices_through 2026-12-31 — every figure on the packet would be computed from a PARTIAL year. No forms were written. Update the bundled daily-close dataset (`scripts/` — the price updater appends closes) and re-export. | btctax-cli/src/year_readiness.rs price_coverage_or_refuse (probed at [2024, 2025, 2026]) |
| BLOCKED | with the year's document | the gate *the crypto slice can PRINT (Form 8949 + Schedule D maps for the year)* is shut for TY2026 and OPEN for [2024, 2025] — it refuses BECAUSE the year is 2026: Form8949Map::for_year / ScheduleDMap::for_year refuse — export-irs-pdf writes no byte | btctax-cli/src/year_readiness.rs slice_can_print (probed at [2024, 2025, 2026]) |
| BLOCKED | with the year's document | the gate *the §111(a) STATE AND LOCAL INCOME TAX REFUND WORKSHEET revision is TRANSCRIBED* is shut for TY2026 and OPEN for [2024, 2025] — it refuses BECAUSE the year is 2026: no i1040gi revision is TRANSCRIBED for the year (`slr::REVISIONS`), so lines 5 and 6 have no standard-deduction / age-blindness figures and the worksheet cannot be worked. ★ Distinct from ARCHIVED: archiving the extract reds `every_archived_revision_is_transcribed` and demands the transcription; it does not supply it | btctax-core/src/tax/state_local_refund.rs revision_for; raises `RefuseReason::StateAndLocalRefundWorksheetNotComputed` (probed at [2024, 2025, 2026]) |
| **UNMEASURED** | January 2027 package | `f1040` is not bundled for TY2026 — no `forms/2026/f1040.map.toml` and no template, so the form cannot be filled. NO form axis ran — the pair does not compute (**NO DRAFT**) | `forms/2026/YEAR.toml [forms_absent] f1040` = "TY2026 revision not released (the file archived as f1040--2026-DRAFT was the TY2025 form); January 2027 package"; `xtask port-status` row `f1040`; archive holds 2020, 2021, 2022, 2023, 2024, 2025 |
| **UNMEASURED** | January 2027 package | `f1040v` is not bundled for TY2026 — no `forms/2026/f1040v.map.toml` and no template, so the form cannot be filled. NO form axis ran — the pair does not compute (**NO DRAFT**) | `forms/2026/YEAR.toml [forms_absent] f1040v` = "TY2026 revision not released; January 2027 package"; `xtask port-status` row `f1040v`; archive holds 2024, 2025 |
| **UNMEASURED** | January 2027 package | `f4868` is not bundled for TY2026 — no `forms/2026/f4868.map.toml` and no template, so the form cannot be filled. NO form axis ran — the pair does not compute (**NO DRAFT**) | `forms/2026/YEAR.toml [forms_absent] f4868` = "TY2026 revision not released; January 2027 package"; `xtask port-status` row `f4868`; archive holds 2024, 2025 |
| **UNMEASURED** | TY2026 revision not released | `f8889` is not bundled for TY2026 — no `forms/2026/f8889.map.toml` and no template, so the form cannot be filled. NO form axis ran — the pair does not compute (**NO DRAFT**) | `forms/2026/YEAR.toml [forms_absent] f8889` = "TY2026 revision not released; the §223(b) figures ARE published (Rev. Proc. 2025-19), but the form itself is not, and a draft is evidence only"; `xtask port-status` row `f8889`; archive holds 2024, 2025 |
| **UNMEASURED** | January 2027 package | `f8949` is not bundled for TY2026 — no `forms/2026/f8949.map.toml` and no template, so the form cannot be filled. the FORM = **UNWITNESSED**, field map = transfers (0 respelled, 0 added, 0 removed, 0 lines moved, 0 retired, 0 introduced, **UNWITNESSED** meanings changed; 16 boxes UNREAD, 2 line numbers UNREAD) | `forms/2026/YEAR.toml [forms_absent] f8949` = "TY2026 revision not released; draft archived; January 2027 package"; `xtask port-status` row `f8949`; archive holds 2020, 2021, 2024, 2025, 2026-DRAFT |

## we must build — 13 row(s)

| state | when | what | evidence |
|---|---|---|---|
| BLOCKED | with the params | TY2026 declares `status = "preparing"`, not `filable` — nothing about the year may be filed while it does | `forms/2026/YEAR.toml` `status` |
| BLOCKED | with the finals | no `FullReturnParams` are bundled for TY2026 — `full_return_for(2026)` is None, which is THE compute gate: the full return does not compute at all | `forms/2026/YEAR.toml` `tables` = "Rev. Proc. 2025-32 §2.10 + Pub. L. 119-21 (TaxTable TY2026 bundled; FullReturnParams TY2026 transcribed as `ty2026_full_return()` but deliberately NOT inserted into by_year — gate reasons 2 and 3 in tax_tables.rs, FR-47); the record flips to `filable` in the same commit that inserts the params" |
| BLOCKED | after 2026-12-31 | the EXPORT-TIME price gate refuses TY2026: usage: cannot export TY2026: the bundled price dataset ends 2026-06-03, before TY2026's prices_through 2026-12-31 — every figure on the packet would be computed from a PARTIAL year. No forms were written. Update the bundled daily-close dataset (`scripts/` — the price updater appends closes) and re-export. | `btctax_cli::year_readiness::price_coverage_or_refuse(2026)`; `prices_through` = 2026-12-31, bundled dataset ends 2026-06-03 |
| **UNMEASURED** | now | `f1040` is read only through a PINNED literal (`f1040--2025`) in an INSTRUMENT (xtask) — it will keep checking the old revision and stay green; archiving `f1040--2026` reds nothing and changes nothing | crates/xtask/src/dependents_grid.rs:125 (f1040--2025) |
| **UNMEASURED** | now | `f6251` is read only through a PINNED literal (`f6251--2024`) in a SHIPPED crate — it will keep computing from the old revision; archiving `f6251--2026` reds nothing and changes nothing | crates/btctax-forms/tests/f6251_map.rs:16 (f6251--2024) |
| **UNMEASURED** | now | `f8283` is read only through a PINNED literal (`f8283--2024`) in an INSTRUMENT (xtask) — it will keep checking the old revision and stay green; archiving `f8283--2026` reds nothing and changes nothing | crates/xtask/src/forms_extract.rs:910 (f8283--2024) |
| **UNMEASURED** | now | `f8889` is read only through a PINNED literal (`f8889--2025`) in an INSTRUMENT (xtask) — it will keep checking the old revision and stay green; archiving `f8889--2026` reds nothing and changes nothing | crates/xtask/src/line_coverage_check.rs:2239 (f8889--2025) |
| **UNMEASURED** | now | `f8949` is read only through a PINNED literal (`f8949--1999`) in a SHIPPED crate — it will keep computing from the old revision; archiving `f8949--2026` reds nothing and changes nothing | crates/btctax-forms/tests/map_rows.rs:575 (f8949--1999) |
| **UNMEASURED** | now | `f8949`'s form axis reads **UNWITNESSED**: no axis found a change AND at least one could not look, so the TY2026 revision is NOT known to be unchanged | `xtask port-status` row `f8949`: `the FORM` = **UNWITNESSED**, `line numbers UNREAD` = 2, `boxes UNREAD` = 16 |
| **UNMEASURED** | now | `f8995a` is read only through a PINNED literal (`f8995a--2024`) in a SHIPPED crate — it will keep computing from the old revision; archiving `f8995a--2026` reds nothing and changes nothing | crates/btctax-forms/tests/f8995a_map.rs:10 (f8995a--2024) |
| **UNMEASURED** | now | `i1040sd` is read only through a PINNED literal (`i1040sd--2025`) in a SHIPPED crate — it will keep computing from the old revision; archiving `i1040sd--2026` reds nothing and changes nothing | crates/btctax-core/src/tax/capital_loss_carryover.rs:169 (i1040sd--2025) |
| **UNMEASURED** | now | `i1099sa` is read only through a PINNED literal (`i1099sa--2025`) in an INSTRUMENT (xtask) — it will keep checking the old revision and stay green; archiving `i1099sa--2026` reds nothing and changes nothing | crates/xtask/src/prompt_check.rs:452 (i1099sa--2025) |
| **UNMEASURED** | now | `i8615` is read only through a PINNED literal (`i8615--2025`) in an INSTRUMENT (xtask) — it will keep checking the old revision and stay green; archiving `i8615--2026` reds nothing and changes nothing | crates/xtask/src/prompt_check.rs:100 (i8615--2025), crates/xtask/src/prompt_check.rs:87 (i8615--2025), crates/xtask/src/prompt_check.rs:93 (i8615--2025) |

## we must decide — 3 row(s)

| state | when | what | evidence |
|---|---|---|---|
| BLOCKED | owner | S1 is unruled: Un-pause a TY2025 REHEARSAL slice | `design/ROADMAP_STATUS.md` §0a, row S1 |
| BLOCKED | owner | S2 is unruled: Two answers:** (1) every income type / venue / deduction on the real 2026 return (W-2s? Sch C? interest/dividends > $1,500? charitable BTC and size | `design/ROADMAP_STATUS.md` §0a, row S2 |
| BLOCKED | owner | S7 is unruled: Pre-rule the oracle fallback**: *if OTS 2026 is absent or disqualified on a line by 2027-03-15 → extension; by 2027-09-15 the evidence for signing wit | `design/ROADMAP_STATUS.md` §0a, row S7 |

## not ours — 1 row(s)

| state | when | what | evidence |
|---|---|---|---|
| BLOCKED | before filing | S2 names a §170(f)(11)(C) QUALIFIED APPRAISAL for a Bitcoin gift over $5,000 — an external appraiser's work, with a deadline that falls before filing; no code clears it | `design/ROADMAP_STATUS.md` §0a row S2; `design/SPEC_appraisal_trigger_minimal.md`; `crates/btctax-core/src/donation.rs` (Section-B completeness needs `appraisal_date` + `appraiser_qualifications`) |

## what this measured, and what it did not

* year package: `YearReadiness::bundled(2026)` — declared preparing, TaxTable yes, params no, 0 forms bundled, prices through 2026-06-03.
* forms: `Stem::ALL` is 21 fillable forms; 0 bundled for TY2026, 2 served by a PERIODIC alias and therefore NOT blockers [f8275 (from forms/2024/); f8283 (from forms/2025/)], the rest listed above.
* form axes: read cell-for-cell out of `xtask port-status`, the generator `design/TY2026_WORK_LIST.md` is itself pasted from, indexed BY COLUMN NAME over 12 required columns. 15 stems have a numeric row, 6 are in the excused table.
* archive: 29 document families are cited by path in the workspace's Rust source. 11 already have a TY2026 revision archived [f1040s1a, f1040s2, f1040sa, f1040sc, f1098e, f6251, f8949, f8960, f8995a, fw2, iw2w3]; 1 are cited with no prior revision at all and are therefore not gaps [f9999].
* revision shape: 1 family(ies) are read through a per-revision prefix (they notice a new document); 11 (family, revision) pins are literal.
* refusals: `RefuseReason` has 133 variants. 5 distinct variants are raised at a site that READS the tax year (11 sites); every other variant never reads the year, so it cannot fire because the year is 2026. Year-keyed gates: 4 shut for TY2026 while OPEN for another bundled year [FullReturnParams are bundled (THE compute gate), the crypto slice can PRINT (Form 8949 + Schedule D maps for the year), the bundled price dataset reaches the year's prices_through, the §111(a) STATE AND LOCAL INCOME TAX REFUND WORKSHEET revision is TRANSCRIBED]; 0 shut for every bundled year []; 3 open [a YEAR.toml record exists (the Form 1099-DA regime is knowable), a TaxTable is bundled (the crypto slice computes), Schedule 1-A exists on the year's return]. 0 UNCLASSIFIED site group(s).
* refusal sites that read the year, attributed: Pub525ItemizedDeductionRecovery @ 4023/4024; ReturnInputsYearNotStated @ 3444; Schedule1aNotOnThisYearsReturn @ 3492/3494/3498/3511; StateAndLocalRefundWorksheetNotComputed @ 3971/3989/4006; StateLocalRefundFactsWithoutARefund @ 3962
* owner decisions: 3 pending, 2 already RULED, parsed from `design/ROADMAP_STATUS.md` §0a.
* form-axis tags: `2025` -> `2026-DRAFT`, read out of the work list's own regeneration command; 361 source files scanned for archive citations.
