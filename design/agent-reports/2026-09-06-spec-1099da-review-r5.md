# Spec review — SPEC_1099da_broker_reporting.md **r5** (the r4 fold)

Reviewer: Claude (independent, read-only). Date: 2026-09-06.
HEAD: `dde805e0` (branch `main`); the fold under review is `41a060e1`.

Scope: the ONE question — does r5 resolve r4's 0C/1I/6M/6N
(`design/agent-reports/2026-09-06-spec-1099da-review-r4.md`) — with every quote it ADDED byte-exact
and every code cite it ADDED resolving — without introducing a new Critical or Important? Read in
order: the r4 report, its VERIFICATION ledger (11/11 TRUE, not re-derived), `git show 41a060e1`
(the only file touched is `design/SPEC_1099da_broker_reporting.md`, 53 insertions / 32 deletions),
then the spec r5 in full once.

Settled and not re-litigated: r1–r4's own quotes/cites (10/10 and 11/11 TRUE per their ledgers); the
owner's rulings; the 1099-DA import out of scope.

---

## Checklist — one row per r4 finding

| r4 | verdict | evidence |
|---|---|---|
| **I-new-1** — reward lots grouped Covered on a misread of (J) | **RESOLVED** | R1 table row 4 (`:119`) flips `FmvAtIncome`/`CardRewardRebate` to **Noncovered** "for any date"; the Legal-grounding bullet (`:45-54`) spells out (J)'s full "in exchange for … described in paragraph (a)(9)(ii)(B) or (C) … respectively" clause (no more ellipsis) plus both instructions quotes; T2's kill (`:307`) is flipped to `→ Noncovered (r4 I-new-1)`. (J) quote verified byte-exact (a); both instructions quotes verified byte-exact (b). |
| **M-new-1** — "no 1099-DA can exist" overstates the type | **RESOLVED** | R2 (`:231-235`) now reads "btctax models a non-`Exchange` wallet as non-custodial … a custodial venue must be recorded as `exchange:PROVIDER:ACCOUNT` to get a key (`parse_wallet_id`, `eventref.rs:58-73`, also accepts `self:LABEL` …)" — near-verbatim the minimal change r4 asked for. Minor residual: the clause "the live-year advisory says so once" is asserted but not mirrored into T6's task list (see NEW-3). |
| **M-new-2** — wrong binding named (`Lot`, not `Consumed`) | **RESOLVED** | `:129-132` now reads "`Consumed.acquired_at` (`pools.rs:321`, assigned `lot.acquired_at` at `:243` … — the `Lot` itself is not in scope there, the `Consumed` is". Verified: `fold.rs:280-360`'s function only has `c: &Consumed` in scope (no `Lot` binding) — confirmed by reading the function body. |
| **M-new-3** — T2 kill doesn't discriminate the real defect | **RESOLVED** | T2 (`:309`) now reads "the cohort of a sold-out 2026 exchange-purchased lot is `Covered` (I-1 — reds if the row's date came from `state.lots`, which no longer holds the lot)" — matches r4's ask to word the kill against the cohort. R1 (`:136-141`) also now states *why* `leg.acquired_at` is forbidden (equal today only because gift lots sit on a date-free row; breaks the day a non-gift `basis_source` tacks a holding period) — verified against `state.rs:184-187` (`gain_hp_start() = donor_acquired_at.unwrap_or(acquired_at)`). |
| **M-new-4** — S10's third promise has no kill | **PARTIAL** | R1's slice bullet (`:203-206`) now states "Kills, all three directions: … **TY2026 + zero exchange dispositions on the slice arm → fills, no refusal** (the promise S10 makes, held — r4 M-new-4)" — the substance is present. But r4's minimal change said "Add one line to **T1**" (the Plan-section task list); T1 (`:297-303`) was **not** touched by this diff and still enumerates only two of the three slice-arm directions (TY2026→refusal, TY2025→fills). See NEW-2. |
| **M-new-5** — wrong operative fallback rule named | **RESOLVED** | `:163-167` now reads "§1.1012-1(j)(3)(i) treats the EARLIEST-acquired units in that broker's custody as the ones sold — or, where the broker offers only one specific-identification method, (j)(3)(ii)'s last sentence deems that method a standing order (which Notice 2026-20 §4.03 switches off when §4.02 is used)". Verified against the primary source (f): matches both (j)(3)(i)'s FIFO/earliest-acquired rule (transfer-in dates disregarded) and (j)(3)(ii)'s last sentence ("In the case of a broker offering only one method of making a specific identification, such method is treated as a standing order or instruction") verbatim in substance. |
| **M-new-6** — third-row rationale incomplete for exchange→exchange | **RESOLVED** | R1 table row 3 (`:118`) now adds "(an exchange→exchange transfer MAY carry one and be covered at the destination under (G); btctax cannot see it and relocates the lot to `CarriedFromTransfer` (`fold.rs:1094-1097`), so it groups Noncovered and the filer's form governs — r4 M-new-6)". Cite verified: `fold.rs:1093-1097` is exactly the ternary landing on `BasisSource::CarriedFromTransfer`. |
| **N-1** — apostrophe/page-number style note | **N/A** | r4 itself called this "style, not error," informational for a future `cite-check` extension — no spec change was asked or needed. Not touched by the fold; correctly left alone. |
| **N-2** — slice heading says "basis-regime year," body says LIVE | **RESOLVED** | `:198` heading now reads "The crypto slice is CLOSED on a **LIVE** year (N-3)." Agrees with body (`:200-201`) and with `ROADMAP_STATUS.md:73-78` (unchanged, already agreed) — see (h). |
| **N-3** — "every literal reds" hand-count is wrong | **PARTIAL / re-broken** | Spec (`:133`) now says "23 full literals; the 27 `..base_leg()` struct-update sites inherit from them." Machine-recount (e), via exact brace-depth tracking over all 51 `grep 'DisposalLeg {'` hits: **10 non-literal** (1 struct def, 8 test-helper factory fn signatures, 1 doc-comment example), **15 full literals**, **26 real struct-update sites**. Both numbers in the fold are wrong: "23" was never re-derived (carried from r4's own uncorrected estimate) and "27" comes from the VERIFICATION ledger's "27, not 26" correction, which itself double-counted the doc-comment's illustrative `..base_leg()` mention (`kat_forms.rs:43`) as a real code site. See NEW-1. |
| **N-4** — `#[serde(default)]`/`Default` not shown | **RESOLVED** | Code block (`:103-107`) now carries `#[derive(Default)]` on the newtype and a comment line `#[serde(default)] pub broker_reporting: BrokerReporting, // absent = unanswered` on the `ReturnInputs` field, with the note "`ReturnInputs` (which has a MANUAL Default, no struct-level serde default)". Verified against `return_inputs.rs:913-915` (manual `Default` impl, doc comment states it explicitly) and per-field `#[serde(default)]` pattern on neighbouring fields (`:929-936`). |
| **N-5** — `acquisition_at`/`acquired_at` naming collision | **RESOLVED** | Field renamed to `lot_acquired_at` throughout (`:129,135,304`); old name `acquisition_at` no longer appears anywhere in the file (grep confirms zero hits). `:135-136` states "column (b) keeps `leg.acquired_at`" (`forms.rs:151`, verified: `date_acquired: leg.acquired_at`) — "only the cohort reads the new field." |
| **N-6** — header drops r1/r2 report paths | **RESOLVED** | Header (`:3-9`) now cites all four: r1 (`…-review.md`, 15/15), r2 (`…-review-r2.md`, 15/15), r3 (`…-review-r3.md`, 10/10), r4 (`…-review-r4.md`, 11/11) — all four ledger counts verified against the actual VERIFICATION files' final "Verdict" lines. All four files exist at the cited paths. |

**r4 score: 0C/1I/6M/6N → 10 of 13 cleanly RESOLVED, 2 PARTIAL (M-new-4, N-3), 1 N/A (no action needed).**

---

## Verdicts (a)–(i)

**(a) The new (J) quote.** Stripped `26CFR_1.6045-1_broker_reporting.xml` with Python (`re.sub(r'<[^>]+>','',data)`), located paragraph (J) ("A specified security described in paragraph (a)(14)(v) … acquired in a customer's account by a broker providing custodial services for such specified security on or after January 1, 2026, in exchange for cash, stored-value cards, different digital assets, or any other property or services described in paragraph (a)(9)(ii)(B) or (C) of this section, respectively."). Whitespace-normalized comparison against the spec's quoted span (`:46-49`) is **byte-exact** — confirmed with a Python script, `MATCH: True` — and the span does end at "respectively." as the brief anticipated. Apostrophe in "customer's" is ASCII U+0027 in both source and spec (no drift).

**(b) The two new instruction quotes.** `Instructions_1099-DA.txt` is confirmed two-column (verified visually at `:41-57`); reconstructed the right column and confirmed the spec's span ("a covered security is a digital asset acquired after 2025 for cash, stored-value cards, different digital assets, or any property or services the disposition of which the broker is required to report") is an exact contiguous substring of the source's right-column sentence, cut cleanly at a clause boundary before "in an account for which the broker …" (not mid-word, not altering meaning). `:364-366` ("Do not report rewards and staking payments on Form 1099-DA.") verified byte-exact against `sed -n '360,368p'` of the source file — the heading is literally "Rewards and staking payments. Do not report rewards and staking payments on Form 1099-DA."

**(c) Table internal consistency.** All four R1 table rows re-read: rows 1 (Covered, criteria yes) and 2/3/4 (Noncovered) — **no row now sends a reward lot (`FmvAtIncome`/`CardRewardRebate`) to Covered.** Grep for `FmvAtIncome|CardRewardRebate` in the spec returns exactly 2 hits, both now "Noncovered." T2's kill (`:307`) is flipped to match (`→ Noncovered (r4 I-new-1)`). Consistent.

**(d) Code cites added — all six resolve at HEAD.**
- `fold.rs:360` — `legs.push(DisposalLeg {` ✓ (exact line)
- `pools.rs:321` — `pub acquired_at: TaxDate,` on `Consumed` ✓ (exact line)
- `pools.rs:243` — `acquired_at: lot.acquired_at,` ✓ (exact line)
- `forms.rs:151` — `date_acquired: leg.acquired_at,` ✓ (exact line)
- `eventref.rs:58-73` — `parse_wallet_id` ✓ (function spans 58–74; the cited range covers the function's full logical body, off by one line on the closing brace only — pre-existing from r4, not a new defect)
- `fold.rs:1094-1097` — the ternary landing on `BasisSource::CarriedFromTransfer` ✓ (lines 1093–1097 contain the `if`/`else` ternary; the cited 1094–1097 sub-range captures both arms including `CarriedFromTransfer` at 1096)

**(e) Literal counts — WRONG, see N-3/NEW-1 above.** `grep -rn 'DisposalLeg {' crates/ --include=*.rs | wc -l` = **51**. `grep -rn '\.\.base_leg()' crates/ --include=*.rs | wc -l` = **27**, but one of those 27 is the doc-comment illustration at `kat_forms.rs:43` (`/// via struct-update syntax, e.g. \`DisposalLeg { term: Term::LongTerm, ..base_leg() }\`.`), not a real construction site. Exact brace-depth-tracked classification of all 51 hits gives **10 non-literal + 15 full literals + 26 struct-update = 51** (verified twice with independent scripts, same result both times, and spot-checked several full-literal sites by hand-reading the source). The spec's "23 full literals; 27 struct-update sites" does not match either figure.

**(f) M-new-5's §1.1012-1(j)(3)(i) restatement.** Located paragraph (j)(3) in `26CFR_1.1012-1_basis.xml` ("Digital assets held in the custody of a broker"). (j)(3)(i) verbatim: "…the basis and holding period of the units sold, disposed of, or transferred are determined by treating the units held in the custody of the broker as sold, disposed of, or transferred in order of time from the earliest date on which units of the same digital asset held in the custody of a broker were acquired by the taxpayer. For purposes of the preceding sentence, the date any units were transferred into the custody of the broker is disregarded." (j)(3)(ii) last sentence verbatim: "In the case of a broker offering only one method of making a specific identification, such method is treated as a standing order or instruction." The spec's paraphrase (`:163-167`) matches both **exactly** in substance.

**(g) The serde block.** `#[derive(Default)]` + `#[serde(transparent)]` on `BrokerReporting`, plus a comment showing `#[serde(default)]` on the `ReturnInputs` field, are present (`:103-107`). Consistent with `crates/btctax-core/src/tax/return_inputs.rs:913-915`, which impls `Default` **manually** (doc comment: "`Default` is impl'd manually (not derived) because the frozen `FilingStatus` has no `Default`") and uses per-field `#[serde(default)]`, not a struct-level one (`:929` etc.) — exactly what the spec's comment claims.

**(h) N-2 — heading/body/ROADMAP agreement.** Heading (`:198`) now says "LIVE year"; body (`:200-201`) defines LIVE as "basis regime AND ≥1 exchange disposition — a TY2026 vault with only self-custody dispositions still fills"; `ROADMAP_STATUS.md:73-78` (untouched by this fold, already correct since r4) states the identical two-limb definition and the identical self-custody-only carve-out. All three agree.

**(i) New inconsistencies.** Two found, both non-blocking — see NEW-1 and NEW-2/NEW-3 below. No markdown table breakage (checked pipe counts on the two edited table rows: 4 each, correct for a 3-column row). No stray leftover reference to the old `acquisition_at` name. No scope creep: every diff hunk traces to a numbered r4 finding (header; legal-grounding; serde block; R1 table rows 3–4; R1 date paragraph; R1 blind-spot paragraph; R2 self-custody paragraph; R1 slice bullet; T2 bullet) — nothing else in the file changed.

---

## NEW findings

**NEW-1 — MINOR — N-3's "fix" restates a wrong count, and makes one number worse.**
**Where:** `design/SPEC_1099da_broker_reporting.md:133`, and the commit message's "N-3 23 literals / 27
struct-update sites."
**What is wrong:** The claim "23 full literals; the 27 `..base_leg()` struct-update sites inherit from
them" does not match the codebase. Exact count (two independent scripts, brace-depth tracking):
51 total `DisposalLeg {` hits → 10 non-literal (1 struct def at `state.rs:197`, 8 test-helper factory
function signatures, 1 doc-comment example at `kat_forms.rs:43`) + **15** full literals + **26** real
struct-update sites. "23" was carried unchanged from r4's own uncorrected estimate (never independently
re-derived by anyone in this chain). "27" originates from the r4 VERIFICATION ledger's "TRUE (27, not
26)" correction, which itself was wrong: the raw `grep '\.\.base_leg()'` count of 27 includes the
doc-comment's illustrative mention of `..base_leg()`, which is not a real site. The compiler-forcing
guarantee this sentence supports ("every literal reds until it is supplied") is unaffected — it still
holds regardless of the exact count — so this is a Minor, not an Important.
**Minimal change:** "15 full literals; the 26 `..base_leg()` struct-update sites inherit from them."

**NEW-2 — NIT — T1's Plan-section kill list still lists only two of R1's three slice-arm directions.**
**Where:** `:297-303` (T1), vs. `:203-206` (R1, updated by this fold).
**What is wrong:** r4's M-new-4 minimal change said "Add one line to T1: TY2026, zero exchange
dispositions, slice arm → fills (no refusal)." The fold added this as the third of "all three
directions" to R1's own prose, but did not touch T1 at all (confirmed: no diff hunk covers T1's block).
An implementer working strictly from T1's checklist could miss writing this kill test, even though R1
now states it.
**Minimal change:** append ", zero exchange dispositions on the slice arm → fills (no refusal)" to T1's
existing "the slice arm: TY2026 → refusal before any byte naming the exit, TY2025 → fills" clause.

**NEW-3 — NIT — M-new-1's advisory promise isn't mirrored into a task.**
**Where:** `:234-235` ("a venue recorded that way routes as self-custody, and the live-year advisory
says so once"), vs. T6 (`:319-323`).
**What is wrong:** Same class as NEW-2 — a new behavioural promise added to R2's prose with no
corresponding line in T6's task list (T6 only says the advisory "reads the regime, not the constant").
**Minimal change:** one clause in T6: "the advisory also states once that a custodial venue outside the
four adapters must be recorded as `exchange:PROVIDER:ACCOUNT` to get a 1099-DA key."

---

## Build may proceed? **YES**

**0 Critical, 0 Important, 3 new Minor/Nit (1 Minor, 2 Nits), plus 2 PARTIAL carryovers from r4
(M-new-4, N-3) whose substance is present but whose specific literal ask was not fully executed.**

r5 is a clean, small fold. The one Important — I-new-1 — is resolved correctly and well: the (J) quote
is now spelled out in full (verified byte-exact against the CFR XML), both new instructions quotes are
byte-exact, the table row and T2's kill are flipped, and the safety-net argument from r4 (a wrong
grouping can only ever cause a spurious `Mixed` refusal, never a wrong box) still holds under the new
grouping. Five of six r4 Minors and four of six Nits are cleanly resolved with sound, source-verified
prose (M-new-5's §1.1012-1(j)(3)(i)/(j)(3)(ii) restatement checks out exactly against the primary
source; M-new-2/M-new-3's field-scoping explanation is verified against the actual fold function body).

What's left is bookkeeping, not correctness: N-3's literal count is still wrong (now for a new reason —
a flawed "correction" propagated from the VERIFICATION ledger rather than the original estimate), and
two of the newly-added behavioural promises (the third slice kill, the advisory sentence) live in R1/R2
prose but were not mirrored into the T1/T6 task lists that an implementer would actually work from.
None of these change a tax result, an assumption, or a guarantee — they are documentation-completeness
residue, all fixable in one sentence each. No re-review is required before T0; fold the three NEW
items opportunistically (e.g., in the same commit as T0/T1, or standalone) rather than blocking on them.
