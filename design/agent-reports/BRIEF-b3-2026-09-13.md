# BRIEF — the B3 interaction review of 2026-09-13. Range `9f0298a3d..HEAD`.

**99 commits · 188 files · +35,800/−818 · 60 files under `crates/`.**

`design/HARNESS.md` **B3**: *"The LAST review before an irreversible action is scoped to `main..HEAD` and
pointed at INTERACTION, not correctness-per-commit. A stack of range-scoped reviews does not add up to a
branch review."* Today was **twelve parcel-scoped gates**, each green in its own worktree.

## ★★★ Why this is not ceremony — four interaction defects were already found TODAY, by accident

Each slipped every parcel gate and was caught only when two pieces met:

1. **Agent A's work passed 3678/3678 in its worktree and FAILED in main** — a stale `.rlib` from concurrent
   builds, diagnosed only because the gate reported 5 failures (FR-176).
2. **FR-196a — a command and its own test disagreed**: `xtask toml-schema` *derived* a "forced to `user`"
   annotation the code did not perform.
3. **My own stage-2 gate was wired to the wrong input** — it asserted a contradiction for a year whose params
   are not bundled, while the test that shares its predicate filtered correctly. Caught by checking a
   post-restore count, not by review.
4. **A false PASS satisfied by a sibling fix's text** — `assert!(screen.contains("REFUSING"))` passed because
   the same change had introduced the words "see REFUSING above" elsewhere.

**Your remit is that class.** A finding a parcel gate could have caught is out of scope by definition.

## What each parcel already verified — do NOT re-audit these

| parcel | landed | already verified |
|---|---|---|
| FR-164 | oracle year threading | 9 sites, B1 kill re-planted by the controller, live OTS selftest |
| FR-165/174 | CI red 8 days | PDF-less clone 217/217; CI green on all three OSes |
| FR-197/205/206/207 | box-12 table | 33 rows derived from two agreeing regions; AA-revert reds 2 of 1385 |
| FR-190/191/192 | three form-delta axes | 32 tests; all three known collisions caught; clean on a pure renumber |
| FR-209/211 | work list re-derived | disjoint vocabularies; 27 one-cell plants |
| FR-210 | label witness | axis untouched; 12 of 87 fixtures changed, none lost a label |
| FR-196/221/222/196a | §111(a) worksheet + limb (b) | 32 tests; refusal narrowed; `Option` to `push_money_opt` |
| §68 | the refusal | 10 plants; over-inclusive direction asserted |
| FR-225 | the false `computable` | `ReturnVerdict`; 8 plants; 1078/1078 on restore |
| FR-218 | 8949 blank page | owner-verified on paper; 4→2 pages, survivors byte-identical |
| stage 2 A/B | the rehearsals | 14 + 18 tests; Tier B's branch deleted |
| `xtask blockers` | the derived list | 12 tests each with a plant; 3 filesystem plants |

## The seams to point your budget at

1. **`RefuseReason` grew from 128 to 133+ across five parcels** that did not see each other. Is every new
   variant in `verdict_reach`, `attribute()`, the panel's classification, and the reachability census? ★ Two
   parcels each added one; a third renamed a field both read.
2. **Three surfaces now answer "can this return compute?"** — `ReturnVerdict` (FR-225), `xtask blockers`, and
   the two new gates. **Do they ever disagree?** FR-196a says a derived reader and its code already did once.
3. **Two harnesses drive TY2026** (`ty2026_rehearsal.rs`, `ty2026_schedule_a.rs`) plus the `no_abort` gate.
   Do they contradict each other, and does any of them depend on a bundling that another forbids?
4. **The printed chain gained the §68 refusal while `printed.rs` gained no line 18** (FR-233's trap). Is any
   other refusal now guarding a field that does not exist, or a field guarding nothing?
5. **`interview_state.rs`, `year_readiness.rs` and `cmd/answer.rs` were rewritten by one parcel while
   `return_refuse.rs` and `return_1040.rs` were rewritten by two others.** The refusal→answer attribution
   crosses exactly that seam.
6. ★ **Instruments landed today that check other things landed today.** `blockers` reads `port-status`'s
   output; the sha256 template guard reads the archive; the no-abort gate reads the bundle. **Is any of them
   green because it never ran over what the others changed?** That is FR-114's shape and it is the likeliest
   thing here.

## Rules

**READ-ONLY.** Report, do not fix — a fold is authorship and re-earns its own gate. Own worktree,
`CARGO_TARGET_DIR=<worktree>/target-b3`, never `/tmp`. **FOREGROUND every command** (FR-175).
★ `ld.lld: undefined hidden symbol` → `cargo clean -p <crate>`; `SIGKILL` → memory, run nextest and clippy
serially. **Do not commit, do not push.** No subagents.
★★ **Fourteen briefs in this arc were refuted by their implementer, ten of them mine. Stop and report rather
than build on a premise you can disprove** — including anything in the table above.
★ **A clean result is a real result.** If the seams hold, say so and name what you traversed; do not
manufacture findings. Rank by whether a filer could reach it.

Deliverable: Bash heredoc (**not** `Write`) → `design/agent-reports/REPORT-b3-2026-09-13.md`.
