# r2 adjudication (Opus)

# 1. VERDICT

**NOT GREEN — 2 Critical / 8 Important blocking.** The fold is substantively right on the tax law and the arithmetic (all §8 figures, both crossover curves and both exposure peaks survive), but two r1 findings were folded into mechanisms that do not work, and six more are closed in prose with no task, no vector, or no test behind them.

# 2. r1 FOLD AUDIT

**11 of 17 fully closed** — C-1, C-2, C-4, I-2, I-3, I-4, I-7, I-8, I-9, I-10, I-11. Six are partial:

- **C-5** — the independence mechanism was *inverted*: T1 now says derive TMT **from** `c09600` with hand figures as the cross-check. `c09600` is the AMT, not the TMT, and is $0 under every reading on V2/V2b. See C-A.
- **C-3** — the rule half is closed (§3.1 negative line 2b + T2). The vector half r1 asked for (V7) is an empty dash row no checkbox constructs, so deleting the line-2b subtraction leaves the suite green.
- **I-1** — the rule half is closed (§3.4, T2, T7, T9). The one *new* behavior — the attach-with-$0-AMT window — has no vector, no test, and T3's clear path never applies the test at all. See I-2, I-3.
- **I-5 / I-6** — acknowledged in §2/§5/T3/§9, but only the *unanswered* branch is specified, and the plumbing is absent. See C-B, I-1.
- **I-12** — the two §5 guards got an owner (T2); §3.5's `debug_assert` + 8801 KAT did not, and one §5 guard is unimplementable as specified. See I-5, I-6.

Two fold-introduced errors that are not r1 IDs: T5's oracle bullet is wrong on every claim it makes (I-4), and the refusal narrowing reds the oracle harness with no owning task (I-7).

# 3. BLOCKING FINDINGS

### C-A — T1's blocking gate is inoperative on the two vectors it exists to protect
*(T1 bullet 2, §8 header, §9 rows 1 & 7, Tier-1 gate — all three lenses; two rated Critical, one Important. Critical: this is r1 C-5 recurring inside C-5's own fix, and a builder following T1 literally derives V2's TMT as `c09600 + regular = 129,397.50` and pins a wrong Part III into the KATs.)*

**Problem.** `c09600` is Tax-Calculator's **AMT**, not the TMT — confirmed in-tree (`scripts/oracle/gen_goldens.py:254-256` docstring "(AMT c09600, credits c07100)"; `admit()` at :259 rejects on `if amt or credits`) and in the installed engine (`calcfunctions.py:2673`: `c09600 = max(0., line9 - max(0., taxbc - e07300 - c05700))`; `line7`/`line9` are locals exposed by no Records variable). AMT = max(0, TMT − regular), so on V2 (AMT $0) and V2b (AMT $0) — the plan's own named discriminators, and it says so two bullets later — `c09600 ≡ 0` under **both** the correct and the wrong Part III readings. The gate carries zero information exactly where it is needed. It is also unplumbed even where it could work: `_taxcalc_row` (`gen_goldens.py:104-129`) has **no charitable-contribution field** (V1/V2/V2b/V6 are gift-driven), maps `MARS` to 1 or 2 only (MFS/V8 unreachable), and has no state-refund or FTC field; the only task touching the oracle scripts is T5, which runs *after* T1.

**Fix.** Restore r1's ordering in T1: hand-derive V1–V10 line by line into `PART_III.md` **before any code** (§8 already carries r1-verified figures) and commit them as data — a `PART_III.md` table plus a committed JSON/`const` fixture the T2 KATs read, so T1 is green and the "recorded before" property is enforced by git history, not by a red suite. Use `c09600` as a **one-way cross-check only where AMT > 0** (V4, V5, V6), reconciling `TMT = c09600 + max(0, taxbc − e07300 − c05700)`, and state that it is inert for V1/V2/V2b/V3. Add one sentence: the form and its instructions are the authority; a taxcalc disagreement is adjudicated against the PDF, never encoded. Restate §9 row 1 as "V4/V5/V6 disagree with `c09600`, or V2/V2b disagree with the hand-derived `PART_III.md` walk" and the Tier-1 gate likewise. If a true TMT witness is wanted, pair each Part-III-sensitive geometry with a witness that holds the geometry (excess < gain, L17 = 0, L32 skip) while lowering regular tax until AMT > 0.

### C-B — the two new declarations refuse only on *unanswered*; the adverse answer silently understates
*(§2 bullets, T3 bullet 2, §5 "Never understate" — lens 2. Critical under the severity rule: it produces an understated filed tax.)*

**Problem.** §2 says "(equal-for-AMT? unknown ⇒ refuse)" and "(None ⇒ refuse)"; T3 says "as unanswered ⇒ refuse"; §5 says "each refuses when unknown." Nothing says what happens when the filer answers **adversely** — "my AMT carryover differs", "the dwelling is a houseboat". Built literally, those returns compute with no line 2k and no line 3 add-back and file an understated tax. Two things make this live rather than pedantic: (a) §3.1's AMTI formula is `line1 + line2a + line2b` with **no line-3 term**, so there is nowhere to put the §56(b)(1)(C) add-back; (b) the exemplar the plan says to mirror does the **opposite** of refuse — `mortgage_all_used_to_buy_build_improve` is destructured as `_` in the refuse screen (`return_refuse.rs:426`) and the KAT at `return_refuse.rs:993-1006` sets `Some(false)` and asserts `reason(&r) == None` ("No brick: the screen does not refuse a truthfully-answered mixed-use return"). Mirroring the pattern produces exactly the silent fall-through §2 warns about.

**Fix.** State the full ternary in §2 and T3 for both declarations: unanswered ⇒ refuse; **answered-adverse ⇒ refuse as well** (v1 models neither a divergent AMT carryover nor non-qualified-dwelling interest); answered-neutral ⇒ term is 0. Requalify the exemplar cross-reference: it mirrors only the *unanswered ⇒ refuse* half and **diverges** on the adverse half, because a zeroed 8a is conservative while a missing AMT add-back is not. Change §5's bullet to "each refuses unless the answer is the AMT-neutral one." Add a T3 KAT per declaration pinning the `Some(adverse)` ⇒ refuse branch.

### I-1 — the declarations' plumbing is a derived registry with ~10 consumers; §4 names two
*(§4 file map, T3 bullet 2 — lenses 1 and 3, both verified in-tree.)*

**Problem.** §4 lists only `return_refuse.rs` and `attribute.rs`. The real surface: `return_inputs.rs` (the `Option<bool>` fields + the shape test at :490-517), `questions.rs` (`FormQuestion` entries, `QuestionId` variants, `QuestionId::ALL`), **`classifier.rs`** (must call `c.declaration(field, QuestionId::…)` — it destructures `ScheduleAInputs` **by name** at :307-320, and a field destructured but not registered as a declaration silently reads as *answered*: this project's one architectural fault line), the registry refusal loop (`return_refuse.rs:543`), `advisories.rs`, `packet.rs`, `btctax-cli/src/cmd/answer.rs`, `btctax-input-form` `seam.rs` / `spec/sections.rs` / `spec/coverage.rs:460` / `apply.rs`, plus the example fixture and docs. **`spec/mod.rs:110-117` hard-codes `decl_count == 7` and `decls.fields.len()` and asserts the `FieldId ↔ QuestionId` map is total** — two new questions are a guaranteed red until every one of those is updated. Separately unaddressed: `testonly::answer_all_live_declarations` (`testonly.rs:33-39`) auto-answers **`false`** for every id except the mortgage one, across the whole fixture surface including `build_golden_return` and therefore the entire baked corpus — under the corrected ternary that refuses the corpus; under the current text it silently answers "not a qualified dwelling" for every itemized household.

**Fix.** Expand §4 to the full list above. Add a T3 sub-item stating: the two `QuestionId` variants and their **input-level** liveness predicates (`schedule_a.mortgage_interest_1098 > 0`; `capital_loss_carryforward_in.short > 0 ∨ .long > 0`), that each is registered via `c.declaration(...)` in `classifier.rs` with a mutation ("drop the registration and the unanswered-⇒-refuse KAT must red"), the `spec/mod.rs` counts and the `coverage.rs` mutate-and-diff KAT as required work (say whether the leaves get form `Field`s or `EXEMPT` entries), and the `answer_all_live_declarations` answer for each new id (`true` for both, matching the mortgage precedent) with the fixtures/goldens that move. Acknowledge the over-ask (every 1098 or carryforward filer is now asked an AMT question) and accept it as §2.7 did. Add to §10 the second behaviour change: stored returns with a 1098 or a carryforward now **refuse** until answered.

### I-2 — T3's clear path never applies the line-7-vs-line-10 test §3.4 says it must
*(T3 bullet 3 vs §3.4 consequence 3 — lens 3; verified.)*

**Problem.** T3 reads "Screen clears ⇒ AMT $0. Screen trips ⇒ compute; line 7 ≤ line 10 ⇒ proceed; else refuse." §3.4 consequence 3 three pages earlier says that is wrong: there is a window `1040 L16 − FTC < line7 ≤ 1040 L16` where AMT is $0 yet the form must be attached, and *"'Proceed with no form' (T3) … [is] wrong there."* The window is reachable **on the clear path**: `amt_should_file_6251` compares against `regular_tax_l16` with no Sch-3-L1 subtraction (`return_1040.rs:1424-1431` passes `ar.regular_tax`), and amt.rs's own header argument that the FTC "cancels from both sides" is about the *AMT-owed* comparison, not the *attach* test, where line 10 = L16 − FTC and line 7 is un-reduced. On a clear, `line11 ≤ breakpoint` ⇒ `TMT ≤ 0.26 × line11 ≤ L16`, so line 7 lands in `(L16 − FTC, L16]` for a suitably tight clear. An FTC ≤ $600 is a live input (`ftc_ceiling` = 300, ×2 for MFJ — `return_refuse.rs:212-215`).

**Fix.** Make T3's rule uniform: compute Form 6251 lines 7–10 whenever Schedule 3 line 1 > 0 (or unconditionally, demoting the screen to a pure fast path for FTC = 0), and branch on line 7 ≤ line 10 in **both** arms. Alternatively state and prove "screen clear ∧ FTC = 0 ⇒ line 7 ≤ line 10" and route every FTC-bearing return through the computation. Pin V9 as a KAT on the **clear** path, not only the trip path. (Honest note: this is a pre-existing under-attachment, not a Tier-1 regression — but the plan claims to fix the boundary and does not.)

### I-3 — V7–V10 are briefs, not vectors; C-3's and I-1's only new-behavior tests therefore do not exist
*(§8 rows V7–V10, T1, T2 KAT list — all three lenses; I rate Important, not Minor, because two r1 blockers close on these rows.)*

**Problem.** Every input cell is "—". T1 says "*derive* every §8 vector's TMT" — deriving presupposes a defined return; no bullet says **construct**. T2 then requires "KATs: every §8 vector", which is satisfiable with the rows still empty, so: delete the line-2b subtraction (C-3) and the suite stays green; implement the attach test as `AMT > 0` instead of `line 7 > line 10` (I-1) and every named test still passes — T3's only mutation is "revert to the blanket refusal", which catches a *narrowing*, not the *loosening*. V9 is a search problem, not a table fill: it must land a whole-dollar line 7 strictly inside a band at most $600 wide.

**Fix.** Add a T1 bullet: "construct V7–V10 (inputs, not just purpose) and record their full rows in §8, then derive/verify." V7 needs no oracle (a pure subtraction from line 1). Give V9 an explicit acceptance criterion: line 7 strictly inside `(L16 − FTC, L16]` with the FTC at its §904(j) ceiling. Add explicit KATs with mutations: T2 — "`state_refund_taxable` > 0; delete the line-2b subtraction and it must red"; T3 — "V9 REFUSES in Tier 1 with AMT $0; replace `line7 > line10` with `amt > 0` and it must red"; T9 — the mirror for the attach decision. These two mutations, not the blanket-revert, are what hold r1 C-3 and I-1.

### I-4 — T5's oracle-domain bullet is wrong on every claim it makes
*(T5 bullet 2, §4 `gen_goldens.py:257` row — lenses 2 and 3; verified in `gen_goldens.py`.)*

**Problem.** (a) The predicate is at **:259** and is `if amt or credits:` — a **combined** check over `c09600` **and** `c07100`, the exact hazard the fold correctly fixed for `main.rs:704` one bullet earlier; lifting it wholesale admits the nonrefundable-credit households the corpus deliberately excludes. (b) It is not the binding gate: a **second** btctax-side gate at `gen_goldens.py:280-283` rejects any household whose paper `1040.line17 != 0` — and L17 is precisely where AMT lands — so lifting :259 alone admits no AMT-bearing household; a **third**, the harness `refused` gate at :266/:277, is what actually excludes the Tier-1 population and is cleared for free by T3. (c) The stated rationale is not "btctax can't do AMT" but L24-comparability (":255-257 … not L24-comparable, full stop"; `ots_direct` "is frozen at T8 and surfaces no AMT line of its own"), which btctax gaining AMT does not change.

**Fix.** Rewrite the bullet: the Tier-1 win is T3's un-refusal plus wider `corpus.py` caps (W-2 $270k / LTCG $20k are far below the $1,218,700 phase-out start), admitting screen-tripping, zero-AMT, no-attachment households that already pass :259 and :283. Keep the `credits` conjunct; relax only the `c09600` half, and defer that to **Tier 2** paired with the `1040.line17 != 0` gate and with whatever the `golden_packet.rs` L24 cross-foot needs when L17 > 0. Add as explicit open items: what the L24 cross-foot compares against when AMT ≠ 0, and what happens when taxcalc reports `c09600 > 0` while btctax computes line 7 ≤ line 10 (bake-time hard error?).

### I-5 — §3.5's discharge artifacts are owned by no task, and one is unbuildable
*(§3.5 last paragraph, §9 row 5 — lenses 2 and 3. This is r1 I-12's exact defect recurring for a different item.)*

**Problem.** §3.5's no-new-8801 argument is explicitly "**Conditional on** §2's two new declarations and §5's exhaustiveness guard," discharged by "a `debug_assert` that every AMT adjustment applied is a §56(b)(1) exclusion item, plus an 8801-recompute KAT asserting $0." Neither appears in T1–T9; §9 row 5 cites the same unowned pair as its mitigation. Worse, the 8801 KAT is not buildable as scoped: there is no Form 8801 code anywhere in the tree, and §2 declares Form 8801 out of scope — "recompute 8801" requires building the thing §2 excludes.

**Fix.** Give both an owning T2 checkbox, next to the two source-scan guards. Replace the 8801 recompute with something buildable carrying the same guarantee: a KAT asserting that for each §8 vector the set of applied AMT adjustments is exactly {line 2a, line 2b, MFS kicker} and every member is a §56(b)(1) exclusion item — which is what makes 8801 Part I lines 18/21 zero.

### I-6 — §5's exhaustiveness guard is unimplementable for 11 of §2's 12 items
*(§5 last bullet, T2 bullet 5, §9 row 3 — lens 2; lens 1 rated it Nit. Important, because §3.5 is conditioned on this guard and as specified it will be silently narrowed to the one item that can pass.)*

**Problem.** §5 specifies "a source-scan test asserting §2's out-of-scope list is still **refused**." §2 itself says the list is "refused upstream **or uncapturable**", and `amt.rs:38-39` confirms only §57(a)(5) PAB interest is actually refused; for ISO, §1202, §4952, depreciation, NOL/ATNOL, K-1, §56(a)(6), depletion, IDC, long-term contracts and pre-1987 installment sales the guarantee is that **no input exists** — a differently-shaped test. A text scan also cannot assert a refusal.

**Fix.** Split into the two guarantees §2 already distinguishes: (i) a *behavioural* test asserting §57(a)(5) PAB interest still refuses; (ii) an *input-surface* test asserting the uncapturable eleven still have no `ReturnInputs` leaf — say whether this extends `spec/coverage.rs`'s mutate-and-diff KAT (the existing "a new leaf appeared" mechanism) or stands alone, and name `classifier.rs`'s by-name destructure as the structural half. Mirror the split in T2 and §9 row 3. Keep a literal source scan only for the "no AMT constant outside `AmtParams`" rule.

### I-7 — the refusal narrowing reds the oracle harness, and no task owns it
*(T3 bullet 4 blast radius, §4 — lens 2; verified.)*

**Problem.** T3's enumeration lists report / harvest / what-if / conservative-promote / TUI and `attribute.rs`, but `crates/btctax-oracle-harness/tests/smoke.rs:98` pins `const EXPECTED_REFUSED: &[&str] = &["mfj_high_income_niit_and_addl_medicare"]`; :101-113 asserts that household is refused and :153-156 asserts the swept refusal set equals it exactly ("a change here means the AMT screen's behavior moved — update `EXPECTED_REFUSED` deliberately, don't paper over it"). That anchor is a screen-tripping, zero-AMT case — exactly the population T3 makes **proceed** — so all three assertions flip. The anchor-exemption branch at `gen_goldens.py:266-276` also becomes dead code and its printed rationale false. §5 requires `make check` green from the first commit.

**Fix.** Add `crates/btctax-oracle-harness/tests/smoke.rs` to §4 (Tier 1) and a T3 bullet: `EXPECTED_REFUSED` becomes empty (or is retargeted to a household that still trips line 7 > line 10) and `the_amt_screen_anchor_is_reported_refused_in_default_mode` is retargeted, not deleted. Add the `gen_goldens.py:266-276` anchor-exemption branch to T5 — decide removal vs retarget.

### I-8 — Tier 2 threads AMT into the printed packet but not the compute chain
*(§4 assigns `return_1040.rs` to Tier 1 only; T8 — lens 3.)*

**Problem.** `AbsoluteReturn` (`return_1040.rs:836+`) carries `regular_tax`, `tax_after_credits`, `schedule_2_other_taxes`, `total_tax`, `amount_owed`, all computed today with no AMT term. §4 gives Tier 2 only `printed.rs`, the forms crate and the CLI. A Tier 2 that fills the PDF but never adds AMT to `AbsoluteReturn.total_tax` understates the balance due by exactly the AMT while the emitted packet shows it — the filer pays the wrong amount. T8's "KAT: V5's balance due with AMT included" is the only thing that would catch it, and only if it reads the absolute chain.

**Fix.** Add a Tier-2 `return_1040.rs` row to §4 (AMT → Sch 2 L2 → L3 → 1040 L17 → L18 → L24 → amount owed) and make T8's KAT assert **both** chains (absolute `amount_owed` and the printed L24) so a one-sided fix reds.

# 4. NON-BLOCKING

- **Minor — §3.1 line 1 misquote.** `// "if zero or less, enter -0-"` is Form 6251 **line 6**'s rule; line 1 has a negative branch (`L15 = 0 ⇒ 1040 L11 − L14`, "enter as a negative amount"). *Downgraded from lens 1's Important:* `AbsoluteReturn.taxable_income` is floored at 0, the branch needs deductions ≥ AGI so the add-back can never reach the $133,300 exemption, and r1 itself filed it non-blocking. Still fix it, since T2's "line 1 ≤ 0" KAT will otherwise encode the wrong rule — and add r1's requested one-line unreachability proof.
- **Minor — `RefuseReason::AmtOwed` does not exist** (§4, T3, T9). The variant is `AmtScreenTriggered` (`return_refuse.rs:161`, `return_1040.rs:1433`, `attribute.rs:144`/`:348`). *Downgraded from lens 3's Important:* the intent ("keep the existing variant, narrow its trigger") is unambiguous and the fix is a find-and-replace. Add one sentence deciding whether the name is kept (recommended — the anchor note at `attribute.rs:142` stays valid) now that its meaning narrows to Who-Must-File condition 1.
- **Minor — §10's SemVer is wrong three ways.** `RefuseReason` (`return_refuse.rs:33`), `AbsoluteReturn` (`return_1040.rs:836`) and `Schedule2Lines` (`printed.rs:293`) are all plain derives, not `#[non_exhaustive]`, so adding variants/fields is breaking. Either mark all three in the Tier-1 commit (cheap — no users) or say Tier 1 is MAJOR for `btctax-core`.
- **Minor — §5's "no literal AMT constant outside `AmtParams`" reds against code T2 must keep.** `AmtParams` (`tables.rs:239-254`) holds dollar thresholds only — no rates — while `amt.rs:111,123` carry `dec!(0.25)`/`dec!(0.26)`. Scope the rule to *dollar* constants or add rate fields; note the $4,652 subtrahend is `0.02 × breakpoint_28pct`; confirm the `#[cfg(test)]` exemption for boundary KATs.
- **Minor — Part III's rounding order is unstated.** `preferential_tax` rounds **once, to cents** (`compute.rs:92`) while §5 requires whole-dollar **per line**, and Form 6251 prints 31/34/37 as separate boxes summed at 38. *Downgraded from lens 3's Important:* §5's per-line rule is already determinate; say in T2 that the helper is reused only for the band split (`at_0/at_15/at_20`) and the per-band tax is rounded per line, and add a KAT on a vector where the two orders differ by $1.
- **Minor — the report/TUI visibility row has no owning task and no valid path.** §4's "print AMTI / exemption / TMT / AMT" is Tier-1 scope but in no T1–T5 checkbox and not in the Tier-1 gate; `crates/btctax-cli/src/cmd/report.rs` does not exist (it is `cmd/tax.rs` + `render.rs`), and no TUI file is listed though T3 names TUI (`btctax-tui-edit/src/edit/tax_inputs.rs:768 focus_refusal`). Give it to T4 with a golden.
- **Minor — §4's `return_1040.rs` row is self-contradictory:** `screen_absolute(ri, &AbsoluteReturn, params)` takes `ar` immutably (`return_1040.rs:1395-1399`) and cannot populate `AbsoluteReturn.amt`. Say `assemble_absolute` computes and stores it; `screen_absolute` reads `ar.amt`. Line 10's operands are already on hand (`ar.regular_tax`, `ar.foreign_tax_credit`).
- **Minor — T2's "fix the existing screen's missing kicker" gives no insertion point.** `amt_should_file_6251` (`amt.rs:87-125`) is written in the *screening worksheet's* line numbers and has no line-4 analogue; name the variable and ordering (`line5`, after the state-refund subtraction, before the exemption test, so it also feeds the phase-out branch) and require the MFS boundary KATs on **both** `amt.rs` and `form6251.rs`.
- **Minor — T1's "Watch them fail" cannot be green.** KATs against a `tentative_minimum_tax` API T2 has not created do not fail, they fail to compile, against §5's green-from-first-commit. The C-A fix (commit figures as data, T2's KATs read the fixture) resolves this.
- **Nit — §1's prose** still frames Tier 2 as "AMT is owed"; §3.4 corrects it to line 7 > line 10, a strictly larger set. One clause fixes it.
- **Nit — line 7 vs line 9 naming.** Form 6251 labels **line 9** "Tentative minimum tax"; the plan uses `line7 = TMT`. Harmless while FTC = 0 but V9 is defined by FTC > 0 and T1's KAT is named `tentative_minimum_tax`. Rename to `line7_tentative_tax` / `line9_tentative_minimum_tax` or state the convention once.
- **Nit — §3.3's `preferential_tax(REGULAR bottoms, pref)`** is 2 args and plural; the signature is `preferential_tax(bp: &LtcgBreakpoints, bottom: Usd, pref: Usd) -> PrefSplit` (`compute.rs:57`). Write out that `bottom` = line 20 = QDCG worksheet line 5 as figured for the regular tax.
- **Nit — path/line citations:** `oracle-harness/src/main.rs:704` → `crates/btctax-oracle-harness/src/main.rs:704` (line correct); `gen_goldens.py:257` → the docstring is :254, the predicate :259, the `c09600` read :223.
- **Nit — `FOLLOWUPS.md` §G-4 still carries the pre-r1 framing** ("AMT is $0 / Who Must File is not met"; "byte-identical to today's") that an executor reads first. Add one line pointing at PLAN §3.4 and §1/T4.

# 5. RECOMMENDATION

**Needs r3.** Not build-ready and not a trivial fold: C-A and C-B each require a real decision (what T1's independence mechanism actually is; what the adverse answer does), and I-1/I-3 add task and vector work that changes the shape of T1–T3. Fold all ten blocking items plus the Minors, then run **one targeted r3 scoped to the fold only** — verify (i) T1's pinned-figures mechanism is executable and `c09600` is used only where AMT > 0, (ii) both declarations state the full ternary and the registry surface is enumerated with the `classifier.rs`/`spec/mod.rs`/`testonly.rs` items named, (iii) V7–V10 have inputs and each of C-3/I-1 has a mutation-backed KAT, (iv) T3's clear path applies line 7 vs line 10, (v) T5's three gates are stated correctly, (vi) §3.5's and §5's guards have owners and buildable mechanisms, (vii) `smoke.rs` and the Tier-2 compute chain appear in §4 with owning tasks. Everything else in the plan — the tax law, the arithmetic, the tier split, §3.3, §3.4, §3.5's argument — is settled and must not be re-audited.
