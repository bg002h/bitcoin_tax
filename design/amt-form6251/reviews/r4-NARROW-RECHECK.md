# Narrow re-check of the r3 fold (Sonnet + Opus)

Scoped by r3 §5 to §2 rounding, §6 guard (ii), §11 SemVer, T3 smoke.rs.

## Lens 1 — verdict BLOCKING

- **§2 rounding: FIXED** — All five prescribed clauses landed verbatim (PLAN.md:78-93): Form6251 carries exact cents in the absolute chain (§8's figures are that layer, T2's KATs pin to the cent); the printed form rounds per line per printed.rs's discipline, pinned by T7's V5 golden (§9 T7 bullet: 'byte-reproducible golden for V5'); the line-7-vs-line-10 comparison is stated to use exact values; the '$1 difference' KAT is re-scoped to the printed layer. Verified against source: printed.rs:1-14 module doc states Reading A ('each printed line is round_dollar'ed at the line... each printed total sums the already-rounded lines above it'); design/full-return/ROUNDING_AUTHORITY.md documents 'Reading A' exactly as cited; conventions.rs:30-37 confirms round_dollar is MidpointAwayFromZero (half-up) as claimed. No residual issue.
- **§6 guard (ii): PARTIAL** — 3 of 4 sub-clauses landed: the mechanism now named (a leaf_map-blocklist KAT) is genuinely capable of asserting absence — verified against coverage.rs:67-75 (leaf_map walks only existing/serialized leaves) and :262-279 (EXEMPT-list staleness assertions), confirming the earlier coverage.rs mutate-diff mechanism really could not observe a never-added leaf, and the new blocklist-on-leaf_map design can; the guard is re-keyed to the form's numbering ('every Part I line 2c-2t and line 3...', PART_III.md-enumerated); the count 'eleven' is dropped. NOT landed: r3's explicit instruction to 'Give it its own T2 sub-bullet so the mechanism is named rather than gestured at.' T2's task list (PLAN.md:251-253) is byte-identical to the pre-r3-fold text and still only says 'the two §6 exhaustiveness guards' buried inside the general KATs bullet -- exactly the 'gestured at' phrasing r3's fix was written to eliminate. Confirmed via `git show eb74c33:design/amt-form6251/PLAN.md` diffed against HEAD: T2's bullet did not change in this fold.
- **§11 SemVer: NEW-DEFECT** — The prescribed content landed correctly and checks out against source: 'Tier 1 is MAJOR for btctax-core, all three types stay plain derives' is stated (PLAN.md:366); Schedule2Lines's cross-crate struct-literal claim is verified at btctax-forms/tests/full_return_forms.rs:430 (confirmed E0639 risk is real -- it's the only Schedule2Lines{} literal in the repo); RefuseReason's no-wildcard match is verified at attribute.rs:22-26 (doc comment quoted near-verbatim) and the hand-written fallback list at attribute.rs:348 (RefuseReason::AmtScreenTriggered is indeed there). AbsoluteReturn 'every literal is in-crate' also holds -- repo-wide grep for 'AbsoluteReturn {' finds exactly two literal constructions, both in btctax-core (return_1040.rs:1287, printed.rs:1270). However, the fold introduced a new defect: it added a correct, ternary-aware 'Behaviour changes to disclose' paragraph (PLAN.md:385-388, correctly stating the adverse branch 'refuses permanently') directly above a pre-existing, untouched 'Second behaviour change to disclose' paragraph (PLAN.md:390-391) that still says only 'refuse until the new declarations are answered' -- omitting the adverse-permanent-refuse nuance the fold just added one paragraph above. The section now contains two overlapping, mutually inconsistent disclosure statements.
- **T3 smoke.rs: FIXED** — All five prescribed clauses present verbatim in T3's bullet (PLAN.md:267-278) and verified byte-accurate against crates/btctax-oracle-harness/tests/smoke.rs: EXPECTED_REFUSED = &[] is prescribed (current source has it as &["mfj_high_income_niit_and_addl_medicare"] at :98, confirming this is genuinely the pre-fix state to be changed); the_amt_screen_anchor_is_reported_refused_in_default_mode is prescribed to invert to assert the anchor proceeds (confirmed at :101, currently asserting out["refused"]==true, and indexes EXPECTED_REFUSED[0] at :102 -- confirming the 'would panic on empty slice' claim is real); sweep_check_reconciliation is prescribed to assert emptiness (confirmed at :119-157, currently asserts refused == EXPECTED_REFUSED at :153); admitted >= 10 moving by one is prescribed (confirmed at :149, current admitted count assertion); 'do not delete' is correctly reattached to the test function, not the constant. Also reconfirmed the underlying premise via grep: no foreign/e07300 field anywhere in scripts/oracle/gen_goldens.py or corpus.py, and gen_goldens.py:259's 'if amt or credits' gate matches the citation exactly.

### [Important] §6 guard (ii) / T2

**Problem:** r3 I-3 explicitly required the leaf_map-blocklist mechanism to get its own named T2 sub-bullet ('so the mechanism is named rather than gestured at'); T2's task list (PLAN.md:251-253) is unchanged from before the fold and still only says 'the two §6 exhaustiveness guards' inline in the general KATs bullet -- exactly the gesture r3 objected to.

**Fix:** Add a dedicated T2 sub-bullet naming the mechanism, e.g.: 'leaf_map blocklist KAT (§6(ii)): reuse spec/coverage.rs's leaf_map on the maximal fixture and assert no key matches the literal blocklist of the uncovered Part I lines' field-name patterns' -- matching r3's literal instruction to give it its own sub-bullet, not fold it back into the generic KATs list.

### [Minor] §11 SemVer

**Problem:** §11 ends with a stale 'Second behaviour change to disclose' paragraph (PLAN.md:390-391) that duplicates and contradicts the fold's own new, ternary-aware 'Behaviour changes to disclose' paragraph (PLAN.md:385-388) immediately above it -- the stale paragraph omits the adverse-permanent-refuse nuance the new paragraph just added.

**Fix:** Delete the stale trailing paragraph (PLAN.md:390-391); the corrected paragraph immediately above (385-388) already carries the complete, ternary-aware disclosure.

## Lens 2 — verdict BLOCKING

- **§2 rounding: PARTIAL** — The rounding DECISION is right and every citation checks out: printed.rs:1-19's header states exactly the per-line-round + cross-foot-the-rounded-lines discipline §2 attributes to it; ROUNDING_AUTHORITY.md:23 defines Reading A as "round each reported line, sum the rounded lines"; conventions.rs:37 `round_dollar` = MidpointAwayFromZero (half-up) via DOLLAR_ROUNDING at :28 (note the file is crates/btctax-core/src/conventions.rs, not tax/conventions.rs); compute.rs:57 `preferential_tax` ends in `round_cents(...)`, i.e. rounds once to cents, so §2's "reused only for the band split" is sound. Exact-cents-for-Form6251 is consistent with §8's cent-pinned vectors, §4's exact AbsoluteReturn chain, and T3's line-7>line-10 trigger. Two residues: the re-scope of the rounding-order KAT was not propagated to T2 (F1), and the sketch's `#[non_exhaustive]` is unrecorded against §11 (F5). RULING on the question posed: NOT a contradiction — §11's argument turns entirely on PRE-EXISTING downstream construction/match sites, and a brand-new type has none — but it is unmotivated under a MAJOR bump and collides with T7.
- **§6 guard (ii): PARTIAL** — The mechanism swap is the right idea and does close r3 I-3's core objection: a blocklist over `leaf_map` keys CAN observe absence, where mutate-and-diff structurally cannot, and §6's supporting citations verify (leaf_map at coverage.rs:68 with its doc at :67; the stale-exemption panics at :264-281). But the fix is placed where it cannot be built (F2) and scoped to include the two lines §3 says DO have leaves (F3). One residual weakness worth a sentence, not a finding: leaves nested inside an absent Option or an empty exempt Vec (`schedule_c`, `int_1099`, …) never appear in leaf_map's output, so the blocklist only bites on leaves the maximal fixture realizes.
- **§11 SemVer: PARTIAL** — The DECISION is correct and both justifying claims verify exactly as stated. (a) `Schedule2Lines` is built by struct literal in another crate — `let lines = Schedule2Lines {` at crates/btctax-forms/tests/full_return_forms.rs:430, imported from `btctax_core::tax::printed` at :12-15 — so `#[non_exhaustive]` is a hard E0639. (b) `RefuseReason` is matched with no `_` arm from another crate: attribute.rs:22-23 carries the guarantee doc verbatim, `pub fn attribute` at :24, `match r {` at :26, and the hand-written fallback list at :348 is precisely the `RefuseReason::AmtScreenTriggered` entry. The `AbsoluteReturn` parenthetical also verifies: every literal is in btctax-core (return_1040.rs:1287,3760; printed.rs:1270,2132,2358) — grep finds none elsewhere. The decision should NOT flip back. Two fold residues: the Tier-2 label no longer follows from the new decision (F4), and a pre-fold paragraph survived uncorrected (F6).
- **T3 smoke.rs: PARTIAL** — The substance of the inversion HOLDS and is provable today: the anchor's btctax paper values already reconcile against both oracles through the SAME `oracle_diff` engine the harness `--check` uses — golden_packet.rs:1457 uses `mfj_high_income_niit_and_addl_medicare` as the 8959-L18 teeth witness, and :1471-1479 requires `diffs_for(h)` to be CLEAN before perturbation. gen_goldens.py:245-247 confirms it is the sole refused-but-baked anchor with $0 actual AMT on both oracles, so once T3 un-refuses it, `refused == []` and `all_reconciled` are the right assertions and `admitted >= 10` still passes. What is wrong is the MODE the plan prescribes them in (F7), plus a doc comment nobody sweeps (F8).

### [Important] §2 rounding / T2

**Problem:** §2's rounding block re-scopes the rounding-order KAT to the printed layer — "The 'two orders differ by $1' KAT belongs to **that** layer", pinned by T7's byte-reproducible V5 golden (Tier 2) — but T2's KAT bullet (PLAN.md:251-253) still lists "the rounding-order KAT" among `btctax-core/src/tax/form6251.rs`'s Tier-1 KATs. r3 I-1's fix said to re-scope that KAT; the fold applied it in §2 and left the pointer in T2. In Tier 1 nothing prints a 6251 (T6/T7 are Tier 2) and, per §6's fail-closed rule, only line 7 ≤ line 10 returns proceed, so there is no rounded 6251 line for a Tier-1 KAT to compare orders on. A builder working from T2's checklist must either write a vacuous test or invent the whole-dollar layer §2 just abolished — which is exactly the wrong-filed-dollar risk I-1 closed (V5's L31 = 54,442.50 ⇒ AMT $26,270 vs $26,271).

**Fix:** Delete "the rounding-order KAT" from T2's bullet at PLAN.md:253 and add it to T7 in §9, beside the byte-reproducible V5 golden: "a KAT that the printed 6251 cross-foots over already-rounded lines (Reading A), not `round_dollar(exact_total)`."

### [Important] §6 guard (ii)

**Problem:** §6 (ii) prescribes "a KAT reusing `leaf_map` on the maximal fixture" and orders it owned as "its **own T2 sub-bullet**" (and T2:253 lists "the two §6 exhaustiveness guards"). That KAT cannot be written where the plan puts it. `leaf_map` is a PRIVATE fn at /scratch/code/bitcoin_tax/crates/btctax-input-form/src/spec/coverage.rs:68, inside a module declared `#[cfg(test)] mod coverage;` at /scratch/code/bitcoin_tax/crates/btctax-input-form/src/spec/mod.rs:7-8 — so it is invisible outside that file, and a `cfg(test)` item does not exist at all when the crate is compiled as a dependency. `maximal_fixture` (:88) is private the same way. T2's file is `btctax-core/src/tax/form6251.rs`, in btctax-core, which is UPSTREAM of btctax-input-form. This is r3 I-3's own fix reproducing I-3's failure mode one level up: an instruction that fails on contact.

**Fix:** Locate the KAT in /scratch/code/bitcoin_tax/crates/btctax-input-form/src/spec/coverage.rs itself, beside `every_in_scope_leaf_is_covered_by_exactly_one_field_or_exempt` (:154), where `leaf_map` and `maximal_fixture` are in scope, and own it in **T3** — which already edits `coverage.rs` per §4 (PLAN.md:152) and T3 (PLAN.md:266). Leave only guard (i), the PAB refusal KAT, in T2's list.

### [Important] §6 guard (ii)

**Problem:** The re-keyed guard scope contradicts §3. §6 (ii) asserts that every Part I line "2c–2t **and line 3**" other than the PAB line "still ha[s] **no** `ReturnInputs` leaf", to be enumerated as a blocklist from PART_III.md. But §3 (PLAN.md:104-105) assigns line **2k** to `capital_loss_carryforward_in` and line **3** to `mortgage_interest_1098` — both real leaves, and both present in the very fixture the guard runs on: `maximal_fixture` sets `mortgage_interest_1098 = dec!(1)` as a deliberate liveness primer (coverage.rs:~105), and `capital_loss_carryforward_in` is an EXEMPT_PREFIX (coverage.rs:244) whose own liveness assertion (:275-281) requires it to match ≥1 fixture leaf. Those two lines are guarded by §3's ternary, not by absence, and a blocklist cannot encode "refuses upstream" — it only tests absence. A builder enumerating the stated range from PART_III.md writes a test that reds on the maximal fixture, on precisely the two lines §3 exists to handle.

**Fix:** Scope (ii) explicitly: "every Part I line 2c–2t and line 3 **except 2g** (guard (i)) **and except the two §3 declaration-covered lines, 2k and 3**, has no `ReturnInputs` leaf." Add one sentence that for 2k and 3 the guarantee is §3's ternary plus T3's per-declaration refuse KATs; if a leaf assertion is still wanted there, blocklist only the AMT-twin name pattern (e.g. `*_amt*`), never the regular-side leaves.

### [Minor] §11 SemVer

**Problem:** §11's closing "**Tier 2 MINOR.**" no longer follows from §11's own new decision. With `Schedule2Lines` staying a plain struct (verified: four public fields, no `#[non_exhaustive]`, crates/btctax-core/src/tax/printed.rs:294-304), §4's Tier-2 row adds `Schedule2Lines.line2/line3` — adding public fields to an exhaustive, externally-literal-constructible struct is a BREAKING change (it E0063s the literal at btctax-forms/tests/full_return_forms.rs:430 and is semver-major). Under the abandoned `#[non_exhaustive]` draft it would have been minor; the label was not updated when the decision flipped.

**Fix:** Change to "Tier 2 is MAJOR too — adding `Schedule2Lines.line2/line3` to a plain (non-`#[non_exhaustive]`) struct is breaking; free under `no-users-yet`", or state the bump explicitly as major-for-the-field-add.

### [Minor] §2 rounding (struct sketch) vs §11

**Problem:** §2's sketch carries `#[non_exhaustive] pub struct Form6251` (PLAN.md:65) while §11 now spends two paragraphs rejecting that attribute. Ruling: NOT a contradiction — §11's case rests entirely on PRE-EXISTING downstream sites (a cross-crate struct literal, a cross-crate no-wildcard match), and a brand-new type has neither, so Tier 1 compiles. But the distinction is nowhere recorded, and it is unmotivated: §11 makes Tier 1 MAJOR for btctax-core, and `#[non_exhaustive]` on a type that ships in that same major bump buys nothing for this release. It does cost something: btctax-forms builds `*Lines` structs by literal in its tests (full_return_forms.rs:430 is the precedent §11 itself cites), and T7 — "the emitter … byte-reproducible golden for V5" — lives in btctax-forms. The first `Form6251 { … }` literal in that golden is the same E0639 §11 documents.

**Fix:** Either drop `#[non_exhaustive]` from the §2 sketch (simplest — the major bump already covers the release), or add one sentence to §11: "`Form6251` may carry `#[non_exhaustive]` because no downstream literal exists; T7's V5 golden must therefore obtain its `Form6251` from core (computed vector / a `testonly` builder), never a struct literal."

### [Minor] §11 SemVer

**Problem:** §11 ends with a stale, uncorrected duplicate of the disclosure the fold just fixed. PLAN.md:390-391 ("**Second behaviour change to disclose:** stored returns carrying a 1098 or a capital-loss carryforward now **refuse** until the new declarations are answered.") repeats verbatim the clause already corrected at :385-388 ("— or, if answered adversely, refuse permanently"), i.e. the exact wording the r3 Minor calls out as false for the adverse branch. §11 now states both the corrected and the false version, and the false one is last.

**Fix:** Delete PLAN.md:390-391.

### [Minor] T3 smoke.rs bullet

**Problem:** The prescribed inversion asserts something true, but in a harness mode that cannot express it. `the_amt_screen_anchor_is_reported_refused_in_default_mode` calls `run(&[], raw_household(name)["inputs"])` — DEFAULT mode, whose output object is `{"refused": false, "lines": {…}}` (crates/btctax-oracle-harness/src/main.rs:156-163). `all_reconciled` and `reproduction_ok` exist only in `--check` mode (:466-473), which reads a whole `GoldenHousehold` (inputs **plus** both oracles' figures, :27-33), not the bare `["inputs"]` object. A builder inverting in place, as the bullet says, writes `assert_eq!(out["all_reconciled"], json!(true))` against `Value::Null` and gets a red test with a confusing message.

**Fix:** Say what the inverted test becomes: switch it to `run(&["--check"], raw_household(name))` (whole household, not `["inputs"]`), rename off `_in_default_mode`, and assert `refused == false` && `all_reconciled == true` && `reproduction_ok == true`. Optionally keep a one-line default-mode assertion that `lines` is now present.

### [Nit] T3 smoke.rs bullet

**Problem:** `EXPECTED_REFUSED`'s doc comment (crates/btctax-oracle-harness/tests/smoke.rs:91-97) states the rationale that T3 falsifies — "btctax's real export path refuses it too, so the harness is right to report it out-of-domain … This is exactly the D-2 refusal signal T10 rejects candidates on" — and no task sweeps it. T4 owns the "every doc comment whose stated rationale is 'the AMT screen refuses this'" sweep but enumerates only the two J6 warnings and full_return_forms.rs:425-429; smoke.rs is T3's file.

**Fix:** Add to T3's smoke.rs bullet: rewrite the `EXPECTED_REFUSED` doc comment to record that the constant is now empty by design and what emptiness proves (T3 un-refused the screen-tripping zero-AMT population).

