# Independent review — UX-P4-12(i) "reassure on table-less-year finalize" (`bd73968`, diff `c8e0d8a..HEAD`)

## What I verified (re-derived from current source, not the summary)

**1. The I-11 guard is genuinely KEPT.** `input_form_store::commit` (`/scratch/code/bitcoin_tax/crates/btctax-cli/src/input_form_store.rs:280-306`) is byte-identical in behavior: both NoTables arms (absent tables at :287-289 and the year-mismatch arm at :290-294) return before any write. The TUI's `commit_tax_inputs` NoTables arm (`/scratch/code/bitcoin_tax/crates/btctax-tui-edit/src/main.rs:1309-1332`) writes only the draft side-table, never `return_inputs`. The KAT's `!return_inputs::exists(sess.conn(), 2099)` runs against a **fresh `Session::open` after `drop(app)`** (main.rs:10275-10280), so it pins the on-disk absence of the engine-visible row, not an in-memory artifact.

**2. The draft persists and the message is truthful.** The arm calls `edit::persist::form_save_draft` **unconditionally** (main.rs:1316-1317) — not the debounced `flush_tax_inputs_draft` (which would no-op: the KAT's form has `dirty: false` from `fresh()`, confirmed at form.rs:276-297). Trace: `form_save_draft` (persist.rs:101-107) → `save_draft` (input_form_store.rs:122-127) → `parked_flag` read + `set_draft_row` upsert + `sess.save()` (encrypt + atomic write — reaches disk). The `saved` flag gates the message on `.is_ok()`, so "SAVED as a draft and persist across sessions" is asserted only when the disk write succeeded; the fallback message says "Saving the draft failed — retry to keep your inputs" and makes **no** saved claim — truthful, and actionable (the flow stays open with the working copy live). The KAT's `draft_exists(2099)` after drop+reopen genuinely proves cross-session persistence.

**3. No poisoning / no downstream regression.** `resolve.rs` has zero references to the draft table (grep for `return_inputs_draft|input_form_store|draft` is empty) — `report`/`export`/`resolve` for 2099 are unchanged by the draft's existence. The only non-TUI draft consumers are the four CLI committed-row writers via `coherence_clear_or_refuse` (cmd/tax.rs:60,170,478; cmd/answer.rs:110), where a WIP draft is superseded-with-note — pre-existing semantics for any autosaved draft; nothing new. Later finalize-once-tables-exist works via the existing `load` §6.1 draft-shadows path → `commit` → `Committed` + `delete_draft` (the neighboring clean-commit KAT exercises exactly this shape). The parked edge case (a parked draft for a table-less year, reachable only via CLI-import-then-park) is handled identically to the pre-reviewed autosave: `save_draft` preserves `parked` (NI-1) and overwrites with the working copy that was itself loaded from that draft — no new clobber hazard.

**4. Borrow/ordering soundness.** `ri` is a local clone extracted at main.rs:1246-1257 (the `form` borrow ends there); `form_commit` borrows it transiently; it is fully live in the NoTables arm. `app.session.as_mut()` is disjoint from and ends before `app.tax_inputs_form.as_mut()`. The `unwrap` is safe: line 1266 already unwrapped `app.session` as `Some` and nothing clears it before the match arm.

**5. Mutation-honesty — I ran both mutations myself, not just reasoned:**
- (a) Replacing the `form_save_draft` call with `let saved = true;` → KAT **FAILS** (the `draft_exists(2099)` assert). Verified nothing else writes the draft in the test path: `fresh()` sets `dirty: false`, the `s` handler flushes only on `Left/Right/Tab` (main.rs:1191-1193), and drop doesn't flush.
- (b) Reverting to the bare `"year {year} has no full-return tables (2024 only)"` → KAT **FAILS** (the reassurance assert). The triple `contains("SAVED as") && contains("draft") && contains("finalize")` is non-vacuous: all three appear only in the happy-path message; the fallback would also fail it (correctly — `draft_exists` corroborates the happy path is what runs). Both tests pass on the unmutated tree; tree restored after each mutation (only the file I mutated).

**6. Docs.** FOLLOWUPS (i) DEFERRED→DONE describes the mechanics accurately (draft autosaved/flushed-on-quit/reloaded — all true per main.rs:1035,1050,1192 and `load` §6.1); the KAT name matches; SPEC §4(i) supersession note is accurate; the doc-comment on the NoTables arm matches the code. The render test `tax_inputs_notables_status_is_visible_in_flow_render` still passes (ran it) and stays meaningful — the new message contains its "no full-return tables" substring. No stale "(2024 only)" TUI string remains; the CLI messages (admin.rs:472, cmd/tax.rs:485,559) are different surfaces and correct as-is.

## Findings

**CRITICAL — none.**

**IMPORTANT — I-1: the persisted user-decision record contradicts the implemented decision.** The FOLLOWUPS DONE block cites `[[full-return-store-before-tables-policy]]` as the record of the USER DECISION, but that memory doc (`/home/bcg/.claude/projects/-scratch-code-bitcoin-tax/memory/full-return-store-before-tables-policy.md`, modified 19:07:28Z — **26 minutes before** commit `bd73968` at 19:33Z) records the *opposite* operative prescription: "the TUI editor's commit must **align to the CLI's store-then-gate-at-export** … the gate moves to COMPUTE/EXPORT, not the store step … this REVERSES the reviewed I-11 guard … **Do NOT let a later review re-instate I-11's store-time block**." The MEMORY.md index line repeats it ("align to CLI (gate at compute/export, not store)"). The implemented and FOLLOWUPS-recorded resolution is KEEP I-11 + fix the message. As it stands, a future session reading memory will find a live user mandate to relax I-11 — the exact "review/session flips a user-mandated guard" hazard this project's records exist to prevent — and the FOLLOWUPS citation points at a doc that prescribes the opposite of what it's cited to support. **Fold:** update the memory doc + MEMORY.md index to the refined decision (drafts already satisfy "store before tables"; I-11 kept; only finalize gates; message fixed), or annotate the FOLLOWUPS citation as superseding the memory's "how to apply" section. (The memory is outside the repo, so nothing in-diff changes; but the record must be reconciled before this closes.)

**MINOR — M-1: the reassurance is truncated in the actual render.** The NOTICE line (draw_edit.rs:2244-2256) is a `Paragraph` with **no** `.wrap()` inside a fixed `Length(5)` block, so it clips at `width−2`. The happy message is 162 chars (+2-space indent). At the app's own tested width (120×40): the filer sees "…your inputs are SAVED as a draft and persist across sessio" — the "finalize once {year}'s tables are published" clause is **never** visible even at 120 cols. At 80 cols it clips at "…your inputs are SA" — the reassurance is effectively lost. The old 47-char message always fit. The KAT pins `app.status` (the full string) and the render test only pins the first clause, so no test observes the on-screen loss. Not gating because the truncation behavior is pre-existing and systemic for this line (the legend line itself is ~105 chars, so the flow's de-facto design width is ≥110, where the core "SAVED as a draft" clause is visible), but the fix's deliverable IS this message — recommend shortening it to ≤~114 chars (e.g. "{year} has no tables yet (v1: TY2024) — inputs SAVED as a draft; finalize when {year}'s tables publish.") and strengthening the render assert to `contains("SAVED as a draft")`.

**NIT — N-1:** after a successful NoTables draft-save, `form.dirty` is left set, so the close hint still reads "[Esc/q] save & close" instead of "(autosaved)" although the draft now matches the working copy — inconsistent with `flush_tax_inputs_draft`, which clears `dirty` on a clean write. Harmless (the later flush is an idempotent re-save), cosmetic only.

**Process note (not a finding against the diff):** while I was running the targeted tests, `git status` showed concurrent uncommitted modifications appearing in `crates/btctax-cli/tests/tax_profile.rs` (an M-1/r3-M1 scan-comment fold) and transiently `crates/btctax-cli/src/render.rs` — another session is mutating this shared worktree during the review. I restored only the single file I mutated and left those untouched; be aware my test runs raced that work (the modified files don't affect the tui-edit KATs, which behaved exactly as predicted in all runs).

## VERDICT

**NOT GREEN — 0 Critical / 1 Important.** The code change itself is correct, guard-preserving, truthful, and genuinely mutation-proven (I re-killed both mutants). What must be folded: **I-1** — reconcile the user-decision record (memory doc + MEMORY.md index vs. FOLLOWUPS/SPEC) so no live instruction to reverse the kept I-11 guard survives. M-1 (message truncation) and N-1 are recorded, non-gating; M-1 is worth taking in the same touch since the message string is the deliverable.
