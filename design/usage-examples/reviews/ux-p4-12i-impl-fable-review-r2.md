# Independent re-review r2 — UX-P4-12(i) fold of r1 (`c2597ad`, range `bd73968..c2597ad`)

## What I verified (re-derived from current source at HEAD `c2597ad`; tree clean before and after)

**1. I-1 is genuinely reconciled — no live mandate to reverse I-11 survives anywhere in memory.**
- The memory doc (`/home/bcg/.claude/projects/-scratch-code-bitcoin-tax/memory/full-return-store-before-tables-policy.md`, modified 19:44:22Z — after the r1 review) now records the refined decision in description, body, and "How to apply": KEEP the I-11 guard, drafts already cover all-year authoring, fix only the message. Its only "align to the CLI" occurrences are (a) inside an explicit prohibition ("do NOT let a later review 'align to the CLI'…") with the reversal framing labeled "the ORIGINAL framing … SUPERSEDED", and (b) a statement that SPEC §4(i)'s "default: align to CLI" **is marked superseded** — which I confirmed is true in the repo (SPEC lines 288–292 carry the `SUPERSEDED (2026-07-19, user decision)` block).
- `MEMORY.md:36` now reads "KEEP the I-11 finalize-guard (don't relax/align-to-CLI), only the refusal message was fixed. Don't let a review re-poison a table-less year." — the correct polarity.
- I grepped the entire memory dir for `align to CLI`, `align-to-CLI`, `re-instate|reinstate`, `gate at compute`, `gate moves to`, `reverse|reverses`, `relax`, `I-11`. Every hit is either in the reconciled doc/index (all with KEEP polarity), or in unrelated files (`self-transfer-fee-treatment-c.md`, `full-return-expansion-roadmap.md` "reverses opus's hobby-only", `auto-pseudo-reconcile-roadmap.md` resolver-tier wording, `post-v070-product-cycle.md` "decision reverse-map"). Zero operative prescriptions to relax/reverse I-11 remain. The record is internally consistent with the FOLLOWUPS (i) DONE block and the SPEC §4(i) supersession, which cross-cite each other and the memory slug coherently.

**2. M-1 is actually fixed, and the fix is mutation-proven end-to-end (I re-ran the mutation myself).**
- The happy NoTables message (`main.rs:1330-1331`) is exactly **104 chars** for a 4-digit year; the fallback is 95. The NOTICE line renders `format!("  {s}")` (`draw_edit.rs:2248`) inside a `Borders::ALL` `Paragraph` still with **no** `.wrap()` (`draw_edit.rs:2257-2262`) — inner width at the flow's tested 120×40 backend is 118, and the message ends at column 106, so the entire line including "finalize when tables publish." renders with 12 columns of headroom. It also fits at r1's de-facto minimum design width (~110 cols → inner 108 ≥ 106). The "Kept ≤ ~104 chars" comment is accurate.
- The render KAT `tax_inputs_notables_status_is_visible_in_flow_render` (`main.rs:10428-10431`) now asserts `rendered.contains("SAVED as a draft") && rendered.contains("finalize")` in addition to the older `contains("no full-return tables")` (still satisfied by the new wording). **Non-vacuousness verified empirically, not just by reasoning:** I mutated the message back to the r1-era 162-char string and ran both KATs — the render KAT **FAILED** while the status-string KAT `tax_inputs_commit_non_2024_…` stayed green (as it must: `app.status` holds the full untruncated string). This simultaneously proves (a) a too-long message really does clip "finalize" off the no-wrap line and red the test, and (b) no other "finalize" occurrence anywhere in the 120×40 frame can mask the clip (had one existed, the mutant would have survived). The author's clip-reasoning is correct: a no-wrap `Paragraph` truncates at the block's inner width, and although `render_editor` concatenates rows with no separators, the clipped tail never enters the buffer at all — and the `│` border cell at the row seam precludes accidental cross-row matches anyway. Tree restored; suite re-verified green.
- The fallback message ("…; DRAFT SAVE FAILED — retry to keep your inputs.") makes no saved claim, fits the line (97 rendered chars), and would fail the happy-path triple (`SAVED as`/`draft`/`finalize` — note "DRAFT" is uppercase and "finalize" is gone from it entirely), so the KAT cannot pass through the failure path. The `saved` gate itself (`form_save_draft(...).is_ok()` at `main.rs:1316-1317`) is unchanged from the r1-verified shape.

**3. N-1 is fixed correctly.** `form.dirty = false` (`main.rs:1322-1324`) sits inside the `app.tax_inputs_form.as_mut()` block, strictly after the disjoint `app.session.as_mut()` draft-save completes, gated on `saved` — the failure path leaves `dirty` set (correct: the draft does NOT match the working copy). `ri` is a clone taken synchronously at 1246-1257 with no intervening mutation of `form.working`, so "the draft now matches the working copy" is truthful when `saved`. The mirror claim holds: `flush_tax_inputs_draft` (`main.rs:909-914`) clears `dirty` only on `Ok` in exactly the same way.

**4. No regression; the guard is still kept.** `git diff bd73968..c2597ad --stat`: the fold commit touches only `crates/btctax-tui-edit/src/main.rs` (+2 review docs at `2e519f9`/`4f8ce60`; the `tax_profile.rs` hunk is `21a62a1`, a different task's r3-M1 fold — I verified it is semantics-preserving for the scan predicate, since any `use serde_json::to_value` line contains the kept `serde_json::to_value` substring; out of this review's scope). `input_form_store.rs` is untouched in the range. The commit KAT still asserts, against a fresh `Session::open` after `drop(app)`: `!return_inputs::exists(2099)` (I-11 kept) AND `draft_exists(2099)` AND the reassurance triple (`main.rs:10277-10293`). **Validation run myself: `cargo nextest run -p btctax-tui-edit` 342/342, then full `make check` 2059/2059 passed + clippy, and `cargo fmt --check` clean** (noting per the repo's own caveat that make check excludes the CI-only fmt/msrv/pii/net-isolation jobs; fmt I checked directly, and the diff is one message-string/test/doc-comment change with no dependency or I/O surface).

**5. Truthfulness of "finalize when tables publish".** Unchanged from r1's verified path: draft-shadows-committed on `load` (§6.1) → `commit` → `Committed` + `delete_draft`, exercised by the neighboring 2024 clean-commit KATs ("a clean commit deletes the draft", `main.rs:10233-10236`). The fold touched none of it.

## Findings

**CRITICAL — none.**

**IMPORTANT — none.**

**MINOR — M-1(r2): the fold left two repo-side records stale (record-consistency residue, not an operative mandate).** (a) The FOLLOWUPS (i) DONE block (`/scratch/code/bitcoin_tax/FOLLOWUPS.md:~2360-2364`) still quotes the superseded 162-char status verbatim as the shipped message ("…your inputs are SAVED as a draft and persist across sessions; finalize once {year}'s tables are published.") — that string no longer exists in the product after this very fold. (b) `design/usage-examples/CONTINUITY_post_v070.md:115-118` still records "(i) DEFERRED (verdict SOUND) … ⚠️ USER INPUT NEEDED — see FOLLOWUPS UX-P4-12(i) RESOLUTION (3 options)" although (i) is decided, implemented, and r1-folded. Neither is the I-1 hazard class — the CONTINUITY text flags the I-11 conflict rather than mandating reversal, and its own pointer lands on the FOLLOWUPS DONE block, which states the truth — so this does not gate. Fold both into the next docs/continuity-seam touch (update the FOLLOWUPS quote to the shortened message; flip the CONTINUITY (i) line to DONE).

**NIT — N-1(r2): the r1-N1 fix is untested.** I deleted the `if saved { form.dirty = false; }` block and the entire 342-test tui-edit suite stayed green — no assertion observes the close-hint state after a NoTables save. Cosmetic surface only (the "[Esc/q] close (autosaved)" hint), so nit-severity, but given this project's own untested-guard lesson, a one-line `assert!(!form.dirty)` in `tax_inputs_commit_non_2024_…` (where the form handle is already in scope at `main.rs:10265-10269`) would kill the mutant for free.

**NIT — N-2(r2):** the memory doc's abridged quote reads "… finalize when the year's tables publish" but the shipped string is "finalize when tables publish" (no "the year's"). Ellipsized paraphrase recording the right decision — align the fragment on the next memory touch.

## VERDICT

**GREEN — 0 Critical / 0 Important.** I-1 is genuinely reconciled: the memory doc and MEMORY.md index now prescribe KEEP-I-11 with the align-to-CLI framing clearly marked superseded, a whole-directory grep finds no surviving reversal mandate, and the record is consistent with FOLLOWUPS (i) DONE and the SPEC §4(i) supersession block. M-1 is fixed and the strengthened render assertion is empirically non-vacuous (I re-killed the long-message mutant: "finalize" clips off the 118-col no-wrap NOTICE line and reds the KAT). N-1 is correctly `saved`-gated with sound borrows. The I-11 guard, draft persistence, and all downstream behavior are unchanged; `make check` 2059/2059 + clippy + fmt clean, run by me. The two recorded Minors/Nits (stale FOLLOWUPS message quote + stale CONTINUITY "(i) DEFERRED" line; the untested dirty-clear; the memory quote fragment) are non-gating residue for the next docs seam.
