# Fable independent review — IMPLEMENTATION_PLAN_usage_examples.md r0 (persisted verbatim)

*Persisted 2026-07-16 verbatim before folding, per STANDARD_WORKFLOW §2. Reviewer: Fable (independent;
author Opus). Verdict: NOT GREEN — 0C / 8 Important / 6 Minor / 3 Nit. Fold → re-review to 0C/0I.*

---

# Plan review — `IMPLEMENTATION_PLAN_usage_examples.md` (r0, Fable independent)

**VERDICT: 0 Critical / 8 Important / 6 Minor / 3 Nit — NOT GREEN. One fold round required.**

The P0 core (Task 0.1) is verified sound: the seam code compiles, the tests prove the seam, and no test passes without it. The Importants cluster in doc-source misattribution, the T-P0.6 stub landmine, the xtask-side binary-resolution/cross-platform contract for the golden test, and four spec requirements with no task step.

## Verified correct (terse)

- `main.rs:66` `let now = OffsetDateTime::now_utc();` — confirmed, inside `run() -> Result<ExitCode, CliError>`, and the **sole** production clock read in btctax-cli + its non-TUI dependency graph (only other workspace hit, `btctax-core/src/persistence.rs:430`, is inside `#[cfg(test)] mod tests` at :382). `resolve_now()?` type-checks.
- `CliError::Usage(String)` at `lib.rs:55`, Display `"usage: {0}"`; `run_to_exit` (`main.rs:38-46`) prints `error: …` to stderr and returns exit 2. T-P0.3's `err.contains("BTCTAX_NOW")` holds because the Usage message embeds the var name.
- `resolve_now()` as given compiles: `time` has `parsing` feature; `OffsetDateTime` already imported; `Rfc3339` import correct; `env::var_os` + `to_str` + `OffsetDateTime::parse(s, &Rfc3339)` all valid; banner text matches R-P0.4 verbatim.
- The tests prove the seam: `config --set-forward-method hifo` with no `--exchange` passes `wallet: None` → skips the known-account validation (`cmd/reconcile.rs:968-1000`), so a fresh-vault election succeeds; `effective_from` defaults to made-date (no backdate blocker); `verify` renders `  recorded {} effective {}` (`render.rs:~2257`). Without the seam, config's made-date would be 2026-07-16, so T-P0.2's `contains("2025-05-01")` **fails without the fix** — genuine seam proof. Malformed/empty env tests hit the seam before dispatch → exit 2 regardless of subcommand.
- `MethodLotArg::Hifo` → clap value `hifo` (`cli.rs:942-946`); `--vault` global (`cli.rs:20`); init's `--key-backup` required (`cli.rs:39`); bin name `btctax` → `CARGO_BIN_EXE_btctax` valid **in btctax-cli tests**; `tempfile` is a btctax-cli dev-dep.
- Ordering gates hold: Task 0.3 pins "no golden until P0 green"; Tasks 1.3/1.4 land golden + `regen==committed` test in the same commit; Task 2.1 scans the J6 manifest only with exact-`{name}` match (r1 Minor honored).
- `persistability` branches (`optimize.rs:469-484`), bulk-void preview (`session.rs:~1117-1140` → `main.rs:2005`), `fill_full_return`'s 14 push sites + three non-Option gates, `admin.rs` `{seq}_{name}.pdf` + `manifest.txt`, `income import --year --file` (`cli.rs:355-366`, `main.rs:236-239`), `FormArg` kebab values (`cli.rs:900-912`), TUI sites (`lib.rs:247,256`; `tui-edit/main.rs:2609`; export.rs correctly "via its caller" — it has no direct read), `.gitattributes` `eol=lf`, Makefile targets, FOLLOWUPS slugs UX-P0-1/P1-1/P1-2/P3-1 — all verified.

## Important (8)

**I1 — Task 0.1 Files: the btctax.1 DESCRIPTION/ENVIRONMENT source is misattributed, and editing it mid-task reds the suite.** The plan says the `cli.rs` root doc-comment is "the `btctax.1` DESCRIPTION/ENVIRONMENT source." False: `render_root` (`crates/xtask/src/docs.rs:144-159`) stitches the hand-authored `ROOT_DESCRIPTION`/`ROOT_FILES`/`ROOT_EXAMPLES` consts in docs.rs; there is currently no ENVIRONMENT section at all (`BTCTAX_PASSPHRASE` lives in DESCRIPTION prose). Worse, if a worker edits any cli.rs doc-comment in Task 0.1 per the Files list, Step 5's `make check` fails `gen_docs_is_deterministic` (docs.rs:353 — committed-match half) because man-page regen is scheduled in Task 0.2 Step 6. **Fix:** drop both doc entries from Task 0.1's Files (its steps never touch them anyway); Task 0.2 owns all man-page text, targeting the docs.rs consts (extend `ROOT_DESCRIPTION` or add a `ROOT_ENVIRONMENT` const), with regen in the same commit.

**I2 — Task 0.2 Step 1: the T-P0.6 sketch is a passing empty test labeled "failing", with an unexecutable TDD step.** The sketched `#[test] fn backdated_now_yields_contemporaneous_classification()` has a comment-only body — it compiles and **passes immediately**, the exact untested-guard landmine, provided verbatim in the plan. Step 2 "Run → FAIL" is impossible even with a real body: T-P0.6 is a characterization KAT of behavior that already exists after Task 0.1 (the seam) plus pre-existing core logic — nothing in Task 0.2 implements product code that could flip it red. The comment also references "the generator harness" (a P1 deliverable — ordering leak into P0) and gives `persistability(made, sale, wallet)` — the real signature is `persistability(wallet: &WalletId, sale_date: TaxDate, selection_made: TaxDate)` (`optimize.rs:469-472`). **Fix:** pin the surface now — a CLI-level KAT in `btctax_now_seam.rs` using `mod fixtures;` (precedent: `end_to_end.rs:1`) + `coinbase_buy_sell_send` (2025 sale, non-broker): import → select/accept under backdated vs. postdated `BTCTAX_NOW`, assert the contemporaneous vs. needs-attestation wording differs between the two runs. Restate Steps 2-4 honestly: "Run → PASS (born-passing disclosure KAT); mutation-check by inverting the expected classification → RED → revert."

**I3 — Tasks 1.1/1.4: the binary-resolution contract for xtask-side tests is unpinned, and the naive reading doesn't compile or false-greens.** `env!("CARGO_BIN_EXE_btctax")` is only set when compiling btctax-cli's own tests — it does not exist for xtask compilation (Task 1.1's "CARGO_BIN_EXE/--bin-dir resolution" is half-impossible). And `cargo test -p xtask` alone never rebuilds the btctax binary, so `examples_golden_matches_committed` can run a **stale** binary → a false-green in-tree drift gate, defeating the I6 born-green rationale. **Fix:** pin it in Task 1.1's interface: the xtask tests (and the `examples` subcommand when `--bin-dir` is absent) run `Command::new(env!("CARGO")).args(["build","-p","btctax-cli","--bin","btctax"])` (nested-cargo-in-test is the established trybuild pattern), then resolve `<target-dir>/debug/btctax` honoring `CARGO_TARGET_DIR`. Freshness then holds by construction.

**I4 — Task 1.4: the byte-exact golden test lands in the workspace suite, which CI runs on windows-latest — and journey stdout embeds joined paths.** `export-irs-pdf` prints written paths via `.display()` (`main.rs:609-620`, `:645-648`): `./irs/f8949.pdf` on Unix becomes `./irs\f8949.pdf` on Windows, so `regen==committed` **cannot** pass on the Windows leg of `cargo test --workspace --locked` (ci.yml test matrix: ubuntu/macos/windows). A red CI leg at P1-close is a blocking finding by the project's own rules. (`.gitattributes` `eol=lf` covers line endings; separators remain.) **Fix:** `#[cfg(unix)]`-gate the regen/determinism tests (and Task 1.1's binary-spawning unit test) with a comment naming the Windows path-separator divergence; the ubuntu CI `examples` job + unix test legs remain the gate. Same decision applies to Task 3.3's TUI goldens if any frame renders a path.

**I5 — Task 1.1: the §3.3 stderr-scope and front-matter requirements have no producing step.** The capture interface is `$ cmd` + stdout + exit code only. But every export journey (J1, J2, J6) unconditionally emits the NO-AUTHORISATION notice on stderr (`main.rs:625-634`), J6's donation leg can emit 8283 warnings, and the R-P0.4 banner fires on every pinned command. SPEC §3.3 mandates labelled stderr blocks where pedagogically relevant, "never silently dropped," plus front-matter stating the pinned-env convention and the honest passphrase sentence (§13 d/e). No task produces any of this. **Fix:** add a `Step`-level stderr mode (e.g. `run_with_stderr` emitting a labelled `stderr:` fence) to Task 1.1's interface, and add the §3.3 front-matter items (env convention + honest sentence) to Task 1.1 Step 3 / Task 1.3.

**I6 — Task 1.2: C-fullreturn/C-business oracle-consistency is asserted in the doc but gated by nothing.** SPEC §4.2 requires sourcing amounts from `kitchen_sink_household()` and the doc will state "the non-donation figures remain the oracle vector." The only planned test is "imports without a hard blocker" — a hand-typo'd wage figure would sail through green and ship a false claim in a distributed doc. **Fix:** add to Task 1.2 an equality test: parse the committed TOML → assert `ReturnInputs == kitchen_sink_household().0` (`testonly.rs:165`), and assert C-business amounts against the same vector.

**I7 — Tasks 2.1/2.2/2.3: per-task "Commit." contradicts the spec-mandated one-commit P2 atom.** SPEC §9 (rescoped per I6 fold): "P2 then lands the CI `examples` job + the forms-census + the subcommand report in **one commit**." Task 2.3 restates this, but Tasks 2.1 and 2.2 each end with their own "Commit." — executing as written violates the mandate Task 2.3 cites. **Fix:** change 2.1/2.2's closing step to "stage (no commit — the P2 atom commits in Task 2.3)."

**I8 — Merge/tag/release: the release-attached PDFs are missing from the tail.** SPEC §2 pins both artifacts' PDFs as "built in CI, release-attached" — the plan's release step covers notes/tag/publish but never attaches `btctax-examples.pdf` + the TUI PDF to the GitHub release. **Fix:** add an attach step (build via `make examples` + the TUI target; `gh release upload`).

## Minor (6)

- **M1 — Task 1.3:** "flip its `.gitignore` untrack in the SAME commit" is a false premise — `git check-ignore docs/examples/examples.md docs/examples-tui/j1.txt` exits 1 (nothing ignores them; only `docs/pdf/` is ignored). Delete the step, or restate as "verify not ignored."
- **M2 — Task 0.1, T-P0.1:** `out.contains("recorded")` also matches the unconditional "Lot selections recorded: 0" line (`render.rs`), so the "still renders the election" assert is vacuous. Tighten to `contains("recorded 2025-05-01")`.
- **M3 — Task 2.1 Step 2:** the census's J6-manifest *source* is unpinned. Cheapest deterministic source: parse the committed golden's J6 "Full-return packet" stdout block (`main.rs:640-648` prints every `{seq}_{name}.pdf` line + `manifest.txt`) — no binary run needed. Pin one mechanism.
- **M4 — Task 1.4:** SPEC §7's third determinism proof — regen with/without a machine price cache present — is absent (only double-regen + cross-HOME are listed). Add it.
- **M5 — Merge/tag/release:** the v0.7.0 bump reds `regen==committed` by design (Cargo.toml-sourced front-matter pin, SPEC §7) — the bump step must regenerate both goldens in the same commit; say so.
- **M6 — Task 0.1 `run_in`:** omits the scrubbed-HOME pin the plan's own Global Constraints list. Harmless for the seam tests, but scrubbing `HOME` in `run_in` is one line and keeps the §3.3 contract uniform.

## Nit (3)

- **N1 — Task 1.2:** the brace list `{self_transfer,…,fullreturn_inputs.toml,donation}.csv` literally expands to `fullreturn_inputs.toml.csv`; write the TOML path separately.
- **N2 — Tasks 1.1/2.1:** new xtask deps go unlisted — `tempfile` (generator vaults), and `btctax-core` if census option 1 is built xtask-side (constructing `PrintedReturn{header, filing_status, forms: PrintedForms{…}}` needs core types; the "or a btctax-forms test-support module" alternative avoids it — note which).
- **N3 — Task 2.1:** "build an all-arms `PrintedForms`, push through `fill_full_return`" — the fn takes `&PrintedReturn` (`btctax-forms/src/packet.rs:36`); the fixture must wrap the forms in a `PrintedReturn` with a synthetic `ReturnHeader` + `FilingStatus`.

---

**The single most important thing to fix:** Task 0.2's T-P0.6 — replace the verbatim empty-body "failing" KAT (which passes as written, the project's named untested-guard failure, sitting inside the highest-priority phase) with the concrete `mod fixtures;` + backdated/postdated `optimize accept` test, and drop the impossible "Run → FAIL" step. Everything else folds mechanically; that one is the landmine a worker will step on first.
