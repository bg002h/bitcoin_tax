# Independent spec re-review — SPEC_post_v070_product_cycle.md (r2)

**Reviewer:** Fable (independent general design reviewer — authored the r1 general review; did not
author the spec or either fold).
**Date:** 2026-07-18. **Branch:** `feat/post-v070-product-cycle`.
**Artifact:** `design/usage-examples/SPEC_post_v070_product_cycle.md` @ r2 (`bdd8cbd`).
**Inputs read:** the r2 spec (all sections, all `[G-*]`/`[T-*]` fold tags); my r1
(`spec-post-v070-fable-review-r1.md`, I-1..I-8 / M-1..M-6 / N-1..N-4); the tax-correctness r1
(`spec-post-v070-fable-taxcorrectness-r1.md`, C1/C2, I1–I5); source re-verified at every anchor the
folds lean on (cli `main.rs`, `eventref.rs`, `session.rs`, cli `resolve.rs`, cli `cmd/tax.rs`,
core `project/resolve.rs`, core `whatif.rs`, core `tax/return_1040.rs`, `btctax-tui/src/tabs/tax.rs`,
`btctax-tui/src/unlock.rs`, `cli.rs` donation args, `tui-edit/src/edit/form.rs`, `xtask/src/examples.rs`,
`docs/examples/examples.md`).

**Charter:** (1) verify each r1 Important (I-1..I-8) is genuinely resolved, with special attention to
the soundness of the NEW clauses (four-surface disclosure incl. the `--write-carryover` gate and the
TUI threading; the per-flag sign table; the validator-mirrors-resolver mandate; the `events list`
resequencing); (2) hunt problems the folds themselves introduced.

---

## 1. Resolution of r1 Important findings

| r1 | Verdict | Basis |
|----|---------|-------|
| I-1 (placeholder-profile false negative) | **RESOLVED** | §3.1 predicate is now `pseudo_active() OR provenance == PseudoPlaceholder` (spec:55–64); the hook is in scope (`resolve_and_screen` returns provenance, verified `crates/btctax-cli/src/cmd/tax.rs:282–296`; inject verified `crates/btctax-cli/src/resolve.rs:121–127`); KAT (b) (spec:97–98) pins the channel and reds under a count-only mutation. Residual: the pinned banner TEXT is false on exactly this channel — filed as **NEW-2**, an interaction of the I-1 and I-2 folds, not a failure of the I-1 fold itself. |
| I-2 (vault-wide signal vs banner truthfulness) | **RESOLVED** | Vault-wide kept; per-year row narrowing explicitly forbidden with the pool-displacement rationale (spec:63–66); the optional after-year-end exclusion is correctly marked optional; the text is now a vault-level statement (spec:67–71). The statement is true for the synthetic (count>0) channel I-2 was about. Its falsity on the placeholder channel is **NEW-2**. |
| I-3 (silent TUI Tax tab) | **RESOLVED** | The viewer tab is now surface 3 of UX-P4-1 (spec:82–85), phase-1 owned (§7.1, spec:267), KAT'd via KAT (a) + the TUI golden. Scope is properly bounded: one function (`render_tax_content`, verified `crates/btctax-tui/src/tabs/tax.rs:55–121`; total at `:93`, charitable at `:116–120` — anchors accurate), signal already in `Snapshot`. I additionally verified the count-only signal is CORRECT there, not a repeat of I-1: `resolve_all_screened` enumerates only stored-profile/ReturnInputs years (`crates/btctax-cli/src/session.rs:489–520` — `years = tax_profile::years ∪ return_inputs::years`), so ladder arm 3 (placeholder) can never populate `snap.profiles` (`crates/btctax-tui/src/unlock.rs:171–219`); a pseudo-on/count-0/profile-less year renders NOT COMPUTABLE (TaxProfileMissing) in the TUI, not a fictional number. The spec does not record this reasoning — see NEW-5 (Minor). |
| I-4 (validation predicate under-specified) | **RESOLVED** | The predicate is pinned tightly enough to implement: refuse iff the resolver would adjudicate the append as a NEW `DecisionConflict`, judged against the live real (non-voided, non-synthetic) persisted decision log (spec:112–115); both sanctioned flows carved out by name; `void` extras pinned (core `resolve.rs` revocability classes verified — non-revocable = SupersedeImport/RejectImport/VoidDecisionEvent, `crates/btctax-core/src/project/resolve.rs:423–440`); the pseudo-OFF projection clause (spec:122–126) closes T-I2; the mirrors-resolver mandate names two acceptable mechanisms (shared helper / shadow projection) — the WHAT is precise, the HOW is legitimately a PLAN choice; the KAT runs both directions with concrete cases (spec:132–135). Not hand-wavy. Residuals: the `set-fmv` exemption sentence is over-broad (**NEW-3**) and `classify-raw` is unenumerated (**NEW-6**). |
| I-5 (negative-guard incompleteness) | **PARTIAL** | The table (spec:143–150) has the right architecture — per-flag, never in `parse_usd_arg`, refusals tax-justified (§1012/§1016/§301(c) rationale), zero allowed, `--other-net-capital-gain` correctly allow-negative, `--donor-acquired` covered in 3.3(b). But the audit is still incomplete against its own "~25 sites" universe — see **NEW-1**: the what-if/harvest ad-hoc profile trio (`--income`/`--magi`/`--carryforward-in`, six `parse_usd_arg` sites) has no row; the tax-profile row's site range is truncated; and the sat-denominated `--sell` (a different parser, same class) accepts a negative that renders a fictional loss. |
| I-6 (appraiser-TIN shape) | **RESOLVED** | `--appraiser-tin` accepts EIN-shape OR SSN-shape (spec:163–165), matching the field's contract (re-verified: `cli.rs` help "Appraiser TIN/SSN/EIN (Part III §6695A…)", sibling `--appraiser-ptin` present); PTIN gets its own shape or an explicit exclusion (spec:169); donee-EIN normalizes hyphenless, refuses SSN-shape, and the refusal message covers the optional/treaty-charity case (spec:166–168); validation is pinned at the `set_donation_details` choke point so the TUI-edit flow is covered (flow verified at `crates/btctax-tui-edit/src/edit/form.rs:1328–1420`). KAT includes the accept direction (EIN-shaped TIN accepted; hyphenless EIN accepted). |
| I-7 (warn-threshold formula) | **RESOLVED** | The threshold is now price-based — `FMV > 100 × (outflow_sats / 1e8) × recent-dataset-close` — with the basis-based formula explicitly repudiated ("NOT cost-basis — a $0/low-basis long-held-BTC gift is the common case", spec:171–175); the no-price fallback is explicit (skip the warn, with the "silent death" rationale stated); refuse correctly rejected. Feasibility verified: sats on the `TransferOut` event, `session.prices()` exists (`crates/btctax-cli/src/session.rs:399`). The KAT covers both the warn, the legitimate-no-warn, and the no-price path. |
| I-8 (events-list phase contradiction) | **RESOLVED** | UX-P4-11 is resequenced into/before phase 1 (§3.6 spec:208–212; §7.1 spec:267–268), with the dependency stated (the 3.2 unified hint names the verb). The M-3 riders came with it: row universe, columns, decided rows with decision refs, pseudo-decidable rider, stable ordering, man page + `make docs` regen (spec:214–221). The phase-1 weight this adds is acceptable: the binary-docs infra is single-sourced from clap doc-comments, so the man page is mechanical, and the verb is read-only/additive — this does not unsoundly bloat the correctness cluster. The end-to-end KAT (ref pasted verbatim is accepted; pseudo-defaulted event listed as decidable) genuinely closes the trap. |

r1 Minors/Nits — all folded and verified sound: M-1 → the rewritten §3.1 KATs (but see NEW-7 on the
golden clause); M-2 → §3.5 (exit map corrected to "2 = any error", stale doc-comment named; ordering —
see NEW-4); M-3 → §3.6 row universe + docs deliverable; M-4 → surface 2 (dual-report suffix; the
placeholder-renders-through-delta parenthetical re-verified against `cmd/tax.rs:306` — the dual block
is provenance-gated to ReturnInputs, so the claim is accurate); M-5 → §4 M-1 blast-radius enumeration
+ J6 regen; M-6 → §9.4 crate-qualified paths + the harvest-arm story (`InvalidTarget → NoLots`
verified core `whatif.rs:140–142/530/534`); N-1 → banner pointers (report for rows / verify for the
advisory — but see NEW-2); N-2 → BTC unit; N-3 → delegated to the PLAN with a stated default + the
T-U-P4-12 invariant sentence; N-4 → the warn-and-write rationale is stated in §4.

## 2. NEW findings (introduced or exposed by the folds)

| ID | Severity | Location | Finding |
|----|----------|----------|---------|
| NEW-1 | **Important** | §3.3(a) sign table (spec:143–150) | **The per-flag audit is still incomplete against its own site universe.** (i) The what-if/harvest **ad-hoc profile flags** `--income`, `--magi`, `--carryforward-in` are six live `parse_usd_arg` sites with no table row (`crates/btctax-cli/src/main.rs:347/348/351` for `what-if sell`, `:421/422/425` for `what-if harvest`) — math-bearing (they build the `AdhocProfile` that feeds the marginal computation) and reachable via the same `=`-form bypass; a PLAN author working from the table's site citations will never visit them. (ii) The tax-profile row cites `main.rs:852-885` but the fields run to `:905` — note `--w2-ss-wages`/`--w2-medicare-wages`/`--schedule-c-expenses` (`:887/:895/:905`) are ALREADY in-arm guarded (`:890/:898/:908`); the audit should record them as guarded rather than omit them. (iii) Same class, different parser: `--sell` (what-if sell `main.rs:305`, optimize consult `:224`) parses through `parse_sell_arg` (`crates/btctax-core/src/whatif.rs:481–488`, bare `i64::from_str` — no sign guard), and a negative sell survives downstream: the pool-coverage check `sum < req.sell_sat` (`whatif.rs:234`) cannot fire against a negative, `method_selection` picks nothing, and proceeds go NEGATIVE (`:242`) → `what-if sell --sell=-100000000` renders a fictional LOSS estimate. (The harvest `Gain(X)/Tax(X)` negative target is already guarded via `InvalidTarget`, `whatif.rs:140–142` — no row needed.) **Fix:** add rows for the ad-hoc trio (same per-field posture as the tax-profile row is fine), extend the tax-profile row's range with the already-guarded note, and add a `--sell > 0` row (refuse ≤ 0). |
| NEW-2 | **Important** | §3.1 banner text (spec:67–71) vs KAT (b) (spec:97–98) | **The pinned banner text is false on the very channel the I-1 fold added.** On the `PseudoPlaceholder` channel (pseudo on, `count == 0`, no stored profile) KAT (b) mandates the banner fire — but every clause of the pinned text is then wrong: "this vault has pseudo-reconciled (deliberately-synthetic) entries" (it has none — every decision is real), "see '[PSEUDO]' rows in 'btctax report'" (no rows exist), "the [PseudoReconcileActive] advisory in 'btctax verify'" (count-gated — absent, as §9.1 itself notes), "resolve them before filing" (nothing to resolve; the remedy is to set a profile / import inputs / turn the mode off). The I-2 fold's own standard — "the banner text must be a TRUE vault-level statement" — is violated, and the remedy pointers are dead ends exactly where the user most needs them. **Fix:** channel-aware wording — keep the pinned text for `pseudo_active()`, add a placeholder-channel variant (e.g. "figures are estimated on a synthetic $0 placeholder profile — set a tax profile (`btctax tax-profile`) or import full-return inputs (`btctax income import`), or turn pseudo mode off"), or one combined text each clause of which is true for both channels. KAT (b) should assert the placeholder variant's wording. |
| NEW-3 | **Important** | §3.2 set-fmv exemption (spec:117–121) | **The exemption sentence contradicts the master predicate and revives the original trap on one verb.** The master rule ("refuse iff the resolver would adjudicate the append as a NEW `DecisionConflict`") correctly implies: a SECOND `set-fmv` on a valid Income target → no conflict → allowed; a `set-fmv` naming an **unknown or non-Income target** → the resolver raises a Hard `DecisionConflict` and EXCLUDES the decision (verified, core `project/resolve.rs` pass 1d: unknown target → DecisionConflict; wrong-type target → DecisionConflict) → refuse. But the exemption paragraph says "The refusal applies only to the **first-wins verbs**: ClassifyInbound, ReclassifyOutflow, ReclassifyIncome" — read literally, `set-fmv` gets NO record-time validation at all, so `set-fmv <typo-ref> 500` records exit-0 and detonates at the next `verify` — the exact UX-P4-3 bug, surviving on the verb that feeds ordinary income. The KAT's refuse direction ({unknown ref, wrong-type ref, …}) does not say which verbs it covers, so it cannot arbitrate. **Fix:** pin "set-fmv is exempt from the DUPLICATE refusal only; existence/type validation (which the resolver would also adjudicate as a new conflict) applies to it like every verb," and add `set-fmv <bad-ref>` to the KAT's refuse direction. |
| NEW-4 | **Important** | §3.5 ordering (spec:201–202) × §3.1 clause 4 | **The pinned ordering blesses persisting a carryover from a hard-blocked ledger.** §3.5 places the `ExitCode::from(1)` return AFTER the `--write-carryover` block and says "the ordering still matters for the non-pseudo NOT-COMPUTABLE case" — i.e. the spec's intent is that a requested write-back RUNS when the delta outcome is `NotComputable`. For a ReturnInputs-provenance year (the only kind `write_back_carryover` accepts, `crates/btctax-cli/src/cmd/tax.rs:478`), a non-pseudo `NotComputable` delta means HARD ledger blockers (the derived profile exists, so it is not TaxProfileMissing; `compute_tax_year` checks hard blockers first). And `write_back_carryover` has NO delta-outcome or hard-blocker gate — verified end-to-end (`tax.rs:444–517`): coherence check → `resolve_and_screen` (input + compute-dependent screens, neither consults `state.blockers` — `screen_compute_dependent` verified at `crates/btctax-core/src/tax/return_1040.rs:584–650`) → `assemble_absolute` over the blocked state → `screen_absolute` (QBI/AMT/TI≤0 only) → persist. So `report --tax-year Y --write-carryover` on a hard-blocked vault prints "NOT COMPUTABLE", then persists a year+1 carryover derived from a ledger the engine refuses to answer for, then exits 1 — the T-C1 class (unflagged persisted input from an unanswerable state) minus the pseudo mechanism, plus incoherent UX. Today's code has the same hole with exit 0 (pre-existing), but this spec is the design of record for BOTH clauses and explicitly reasoned about this exact case — it must not pin the unsound resolution. **Fix:** add to 3.1 clause 4 (or 3.5): the write-back also REFUSES when the year's delta outcome is `NotComputable` (fail-closed, message names the blocker), with a KAT (hard-blocked non-pseudo vault + `--write-carryover` → nonzero, year+1 inputs byte-identical). The exit-1 placement question then dissolves for that case — the block refuses loudly before the return either way. |
| NEW-5 | Minor | §3.1 surface 3 (spec:82–85) | The spec threads only `snap.state.pseudo_active()` on the TUI surface — half the §3.1 predicate — without saying why that is complete there. It IS complete today (verified: `resolve_all_screened` enumerates only stored-profile/RI years, so the placeholder arm never reaches `snap.profiles`; the TUI renders NOT COMPUTABLE for that channel), but the asymmetry is undocumented and silently becomes a hole if the viewer ever gains the CLI's placeholder-estimate path. Add one sentence recording the reasoning and the trip-wire ("if the TUI ever resolves a bare year on demand, the full OR-predicate applies"). |
| NEW-6 | Minor | §3.2 verb list (spec:117–121) | `classify-raw` (the escape-hatch verb, `reconcile.rs` `classify_raw`) appends the same first-wins payload kinds but is absent from the enumeration — state explicitly whether it gets record-time validation or is deliberately exempt (if exempt, the record-then-conflict trap survives via that verb; likely acceptable for a power-user escape hatch, but say so). |
| NEW-7 | Minor | §3.1 KAT (a) (spec:94–96) | The operational golden guard ("the existing tax-report golden's byte-diff across the fix shows only banner/suffix line insertions") presumes a pseudo-active report run in the committed examples golden. None exists — verified: zero `pseudo` occurrences in `docs/examples/examples.md` and no journey enables the mode in `crates/xtask/src/examples.rs` — so across the fix the committed golden's diff is EMPTY. That is itself the right §1 guard for the non-pseudo path, but it witnesses nothing about banner-only insertions. Restate as two clauses: (i) the committed examples golden is byte-identical across the fix (no journey is pseudo-active); (ii) the NEW pseudo-fixture KAT asserts its dollar figures are byte-identical pre/post-fix with only banner/suffix lines added. |
| NEW-8 | Nit | §3.5 (spec:203–204) × §3.1 clause 4 | "A pseudo-active report stays [exit] 0" and KAT (d)'s nonzero sit one flag apart; add a cross-reference ("…unless `--write-carryover` is passed — 3.1 clause 4 refuses") so the two clauses cannot be read as conflicting. |

**Checked and explicitly clean** (recorded so the r3 re-review can skip them): the write-carryover
refuse is exit-code-coherent (a refusal `Err` maps to 2 = "command failed (any error)" — the man-page
contract as folded keys on non-zero, so no contradiction; KAT (d) correctly says "nonzero" without
pinning 1 vs 2); KAT (d) genuinely reds under mutation (drop the refuse → the year+1 inputs file
differs); the §3.1 mutation clause covers each surface's emit independently; surface 2's parenthetical
(placeholder path renders through the delta block) is accurate — the dual block is provenance-gated to
ReturnInputs (`cmd/tax.rs:306`), so the placeholder component of the predicate is correctly inert
there; the `events list` phase-1 move does not unsoundly bloat phase 1 (man page is single-sourced
from clap doc-comments — mechanical); the §2 severity map correctly hoists T-C1/T-C2 to Critical
clauses of UX-P4-1 with all four surfaces phase-1-owned; §7's phase list matches §1's ownership
claims; every §9 anchor the folds added (§9.1 dual-report/TUI/write-carryover, §9.3 ManualFmv
last-wins + void revocability + pseudo-fill phases + `session.project()` stored-config, §9.4 crate
paths, §9.6 blast radius) re-verified accurate against the working tree; the harvest negative target
is already guarded (`InvalidTarget`); the sign-table's only allow-negative (`--other-net-capital-gain`)
is correct, and every refuse-negative row is tax-sound (no legitimate negative basis/FMV/fee/price/
proceeds exists).

## 3. Verdict

**0 Critical / 4 Important (NEW-1, NEW-2, NEW-3, NEW-4) / 3 Minor / 1 Nit — NOT GREEN.**

All eight r1 Importants are genuinely folded (seven RESOLVED, I-5 PARTIAL — subsumed by NEW-1), and
the r2 architecture is right: the four-surface disclosure with a fail-closed persistence gate, the
resolver-mirroring validation predicate, and the resequenced discovery verb are the correct shapes.
The four remaining blockers are all second-order fold defects, each with a one-to-two-sentence fix:
complete the sign table's site audit (NEW-1), make the banner text true on the placeholder channel it
now fires on (NEW-2), scope the set-fmv exemption to duplicates only (NEW-3), and refuse the carryover
write-back on a NOT-COMPUTABLE delta rather than pinning write-then-exit-1 (NEW-4). Fold these and
re-review; none threatens the spec's spine.
