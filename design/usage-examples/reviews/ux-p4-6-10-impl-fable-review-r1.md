# Independent review — Phase 3 "Report surfaces" (UX-P4-6 + UX-P4-10), branch `feat/post-v070-product-cycle` (r1)

Scope reviewed: commits `20b2a58` (UX-P4-6) + `027a89d` (UX-P4-10), diff at `phase3.diff`, verified against current source. Validation: `make check` run by me — **2044 passed / 0 failed / 8 skipped**, including `examples_golden_matches_committed`; `cargo run -p xtask -- docs` regenerated the man pages with **zero drift** against the committed `docs/man/btctax-report.1` (the hand-visible `-h` line change is generator-canonical). Caveat per project memory: `make check` is nextest+clippy only, not the CI-only jobs.

## What I traced and confirm clean

- **§1 dollar-invariant holds.** The exit-1 is pure post-print control flow in the `--tax-year` arm (`main.rs:200-202`); the pending line is display-only in `render_report` (`render.rs:310-318`). No computation path touched; no golden dollar figure moved (both `examples.md` `report --tax-year` blocks are Computed and unchanged; the only `[exit 1]` in the golden is the pre-existing J-verify one at line 229).
- **Exit-code predicate reads ONLY `outcome`.** Traced `report_tax_year` (`cmd/tax.rs:269-462`): the absolute-refusal renders inside the `dual_report` *string* (`tax.rs:332-339`) and never feeds `outcome` (`tax.rs:303`); pseudo yields `Computed` via the placeholder profile + projection-level synthetic defaults, disclosed only through `pseudo_contributed`. Both exit-0 non-triggers hold structurally.
- **No persist-then-exit-1 path.** All three `NotComputable` causes (`compute.rs:246-276`: hard blocker / `TaxTableMissing` / `TaxProfileMissing`) are independently refused inside `write_back_carryover` before any write — hard blocker by clause 4b (`tax.rs:529-539`), missing table by the early bundled-tables check (`tax.rs:482-487`), missing profile via 4b's recompute — each an `Err` → exit 2 (spec-sanctioned "nonzero, persists nothing"). Placing the exit-1 after the write-carryover block creates no ordering hazard; the same resolver + same state make an outcome/write-back divergence impossible.
- **Full exit map is exactly the documented contract**: 0 = fallthrough `main.rs:957`; 1 = verify blockers `:116` + report NotComputable `:201`; 2 = any `Err` `:44` / panic `:34`. The `--year` display path and plain `report` are untouched by 10 (else-branches at `main.rs:203-211`).
- **Pending-line sources are consistent by construction.** `sigma_pending` is *derived* in `finalize` as Σ(principal+fee) over `pending_reconciliation` (`fold.rs:1312-1316`), so sat-source and count-source cannot disagree on a projected state; `sigma_pending > 0` with an empty queue is impossible, and gating on `sigma_pending` is the spec's own §4 wording. `fmt_btc` is the shared integer-math 8dp formatter. `render_report` has exactly one production caller (`main.rs:210`) — screen-only, no CSV/form writer contamination. The BTC unit is spec-mandated `[G-N2]`.
- **Pending-line KATs are mutation-honest**: always-shown dies on the reconciled-hides-it assert (a `0.00000000` line would match `"Pending:"`), sat-unit dies on `"0.03000000 BTC"`, count-from-sigma dies on `"1 unreconciled transfer"`, hardcoded-singular dies on the 2-transfer plural KAT. Exit-code KATs spawn the real binary and `assert_eq!` on `.status().code()` — genuine process codes.
- **Docs**: clap long-about ↔ man page single-sourcing verified by regen; the two stale `tax_report.rs` doc-comments (the §3.5-cited `:780`-region "exit 0" claims) were corrected; the remaining "exit 0" at `tax_report.rs:950` describes the no-`--tax-year` display path and is still accurate.

## CRITICAL

None.

## IMPORTANT

**I1 — The plan-mandated KAT for the dual-report exit-0 non-trigger is missing.** The plan (IMPLEMENTATION_PLAN, Phase 3 3b) is explicit: "KAT asserts the process exit code, **including the two deliberate exit-0 non-triggers (SPEC §3.5):** a dual-report whose absolute total is refused but whose delta computed → exit 0; a pseudo-active report WITHOUT `--write-carryover` → exit 0." `crates/btctax-cli/tests/report_exit_code.rs` ships three KATs — NotComputable→1, Computed→0, pseudo→0 — and **no test anywhere** (CLI lib- or binary-level; I swept all of `crates/btctax-cli/tests/`) exercises `screen_absolute` refusing while the delta computes, let alone asserts exit 0 for it. The file's module doc-comment correspondingly narrows the contract to the pseudo non-trigger only, silently dropping the `[T-M5]` case both SPEC §3.5 and the plan pin. The behavior is correct *today* (traced above), but this non-trigger is precisely the regression the KAT was ordered to hold: a later "improvement" extending the exit-1 predicate to the absolute refusal would sail through the suite. Per the rubric this is a missing case, and per the project's own untested-guard pattern the guard isn't done until the mutation dies. **Fold:** add a fourth binary-level KAT — TY2024 `income import` (ReturnInputs provenance; fixture base exists at `tests/fixtures/examples/fullreturn_inputs.toml` and the `dual_report_renders_absolute_return_with_section_6_labels` scenario in `tax_report.rs:1511+` shows the setup) with a `screen_absolute` trip (e.g. QBI over the §199A(e)(2) threshold, or the AMT screen used by `oracle-harness/tests/smoke.rs:93`) and a computable delta → assert process exit 0; optionally also assert the "NOT COMPUTABLE" refusal text renders in the dual block so the test proves it hit the intended state. Update the module doc to name both non-triggers.

## MINOR

**M1 — Binary-level exit-1 is pinned for only one of the three `NotComputable` kinds.** `report_notcomputable_exits_one` covers `TaxProfileMissing`; the hard-blocker (`TaxYearNotComputable`) and `TaxTableMissing` kinds reach exit 1 only via the shared `matches!` on the variant, asserted nowhere at the process level (the `carryforward_mismatch_advisory_rendered` comment now *narrates* "binary exits 1 (UX-P4-10)" for `TaxTableMissing` but the test is lib-level and doesn't assert it). A kind-narrowed predicate would survive the suite. Cheap fix: the existing `write_buy_receive_2025` fixture *without* `pseudo_set_mode` and without a profile is already a hard-blocker vault — one more `assert_eq!(run_report(..), 1)` covers the kind `verify` most directly mirrors. Fold with I1 or file to this phase's burndown.

## NIT

**N1 —** No vault-level (projection-through-render) test pins the pending line; both KATs hand-construct `LedgerState`. The derivation invariant is held separately by `fold.rs`/conservation tests and the hand-built states match it, so this is belt-and-suspenders only.

## VERDICT

**NOT GREEN — fold required: I1** (add the plan-mandated dual-report absolute-refused-but-delta-computed exit-0 KAT + widen the module doc; M1 rides along cheaply). The implementation itself is correct on every path I traced; the gap is a mandated guard, not a behavior defect.
