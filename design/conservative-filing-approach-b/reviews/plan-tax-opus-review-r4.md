# Plan review — US-federal-tax-correctness lens (Opus) — ROUND 4

**Artifact:** `design/conservative-filing-approach-b/IMPLEMENTATION_PLAN.md` @ `5900abe` (branch `feat/conservative-filing-b`)
**Also verifies:** the POST-GREEN **BG-D4 amendment** in `SPEC.md` (2026-07-21) — the clamp bound corrected from `clamp(net, …)` to `clamp(net − documented, …)`.
**Against:** r3 tax (`plan-tax-opus-review-r3.md`, 0C/1I/1M/1N) + r3 arch (I-1 four-site FoldCtx) + amended SPEC + current source at HEAD. Fresh independent lens — the clamp arithmetic re-derived across every corner rather than trusting the fold notes.

Source re-verified (cite by symbol): `make_disposal_legs` takes `total_net_proceeds` and the non-dual arm sets `proceeds` = pro-rata share of net, `gain = round_cents(proceeds - basis)` (fold.rs:122/:193-201) — so the clamp's `net_proceeds_share` and the gain's `proceeds` are the **same value** (internally consistent); `net = round_cents(proceeds - fee_usd)` (fold.rs:634); `consume_fee` TreatmentC sums `gain_basis` across FIFO fee fragments into `FeeCarry` (fold.rs:346-352 — T5's site); `rehome_onto_disposal_leg`/`_lot`/`_removal_leg` (fold.rs:291/:305/:313) all consume that ONE `FeeCarry`, so a single evaporation at the source flows to all three; `make_removal_legs` basis = `c.gain_basis` (fold.rs:256 — T6's site); `verify(vault_path, pp) -> Result<VerifyReport, CliError>` is vault+passphrase only (inspect.rs:146 — T11-3b threading is real work); `Coverage` has **no** serde derive today (conservative.rs:174 — T1 real); `basis_methodology` carries the "…re-homed … under §1011, **never the estimate**" sentence (conservative.rs:161 — T11 premise grounded); FoldCtx built at fold.rs:413/:463/:520 + transition.rs:60 (**four** sites — arch r3 I-1).

## Verdict

**GREEN — 0 Critical / 0 Important / 0 Minor / 0 Nit.** The r3 blocker (tax I-1) is genuinely and correctly resolved in BOTH the SPEC and the plan; the amended clamp is tax-correct at every corner I re-derived; the §6 KAT matches; the r3 Minor and Nit landed; the four-site FoldCtx fold (arch r3 I-1) is threaded and source-confirmed. A fresh adversarial pass over the whole plan + amended SPEC surfaces no remaining tax-correctness or honesty gap. The gate can close.

---

## Verified resolved (r3 findings + BG-D4 amendment → status)

- **tax I-1 (BG-D4 clamp omitted `documented_share` → estimate-ENABLED loss) → RESOLVED, and tax-correct.**
  Amended formula (SPEC:123-134; plan T4 Produces :442-453 + Step 3 :517-529):
  `estimate_basis = min(estimate_share, max(net_proceeds_share − documented_share, $0))`, `reported = documented_share + estimate_basis`, `gain = net − reported`. Re-derived every corner:
  - **documented = 0** (plain promote): `estimate_basis = min(est, max(net,0))` — identical to the old `clamp(net,0,est)`. Below-floor → gain $0; above-floor → real gain. All plain-promote KATs unchanged. ✓
  - **fee-carry band** (est 12k, documented 30, net 8k): `min(12k, max(7970,0)) = 7970`; reported 8000; **gain $0** — not the old −$30 estimate-enabled loss (documented-only would be a $7,970 gain, so the −$30 was 100% but-for the estimate). ✓
  - **just-above-floor band** (`est ≤ net < est + documented`): reported = net, **gain $0** (the crowd-out that also bit here is closed). ✓
  - **documented ALONE > net** (`fee_usd > proceeds` → net < 0): `estimate_basis → $0`, reported = documented, `gain = net − documented < 0` = a genuine §1001(b) documented loss — attribution intact. ✓
  - **well above floor** (`net ≥ est + documented`): `estimate_basis = est`, reported = full documented+estimate, real gain. ✓
  The estimate is monotonically clamped to reduce gain to zero but never below; the documented component is always honored first and retains its full loss-creating effect. **Fact:** filing $0 instead of −$30 is strictly anti-understatement and now MATCHES the 8275's "limited so as not to report a loss from the estimate" narrative (T13:1260, amount = as-filed leg.basis) — the examiner-mismatch the old formula created is gone. **Reachability of `documented_share > 0` on a promoted lot** (the amendment's premise) confirmed constructible: a promoted (oldest) tranche fully relocated as self-transfer principal, the fee then drawn FIFO from a newer *documented* lot (Lot A exhausted), `rehome_onto_lot` (fold.rs:845) adds documented fee to the survivor → `usd_basis > filed_basis` → `documented_share > 0`; T5's evaporation is keyed on promote-set membership so it correctly does NOT strip that documented-lot fee.

- **BG-D4 §6 KAT match → RESOLVED.** SPEC §6 (:510-513) now reads "a relocated-with-fee-then-promoted tranche sold below floor files `gain = $0` … a genuine documented loss (documented ALONE `> net`) still reaches negative — attribution intact." Plan T4 KATs match: `relocated_with_fee_then_promoted_sold_below_floor_files_zero_gain_not_an_estimate_enabled_loss` asserts `gain == Usd::ZERO` (:467-474, flipped from the r3 mis-pin `< 0`); `sold_just_above_floor_band_still_files_zero_gain` ADDED (:483-487); `a_genuine_documented_loss_still_reaches_negative` asserts `gain < Usd::ZERO` (:476-481). Mutation-to-kill names "bare `net` (no `− documented_share`) reds [relocated/just-above-band]" and "dropping `− documented` for documented-alone>net reds genuine-loss" (:538-546). The old mis-pinning KAT name survives only in the r3 review file (history), not the plan.

- **SPEC POST-GREEN AMENDMENT line → CORRECT.** SPEC status (:13-18) records the amendment, the flawed old formula, the worked corner, and the §6 KAT flip, and cites plan-review-Opus r3 tax I-1. The 5-round prose invariant (estimate-gain ≥ 0, estimate basis ≥ $0) was always right; the amendment makes the FORMULA implement it. Honest.

- **tax M-1 (drift advisory: filed/unfiled unbranchable + one-direction pin) → RESOLVED.** T11 Step 3b (:1124-1132) reframes the overstated-basis case as **conditional COPY** ("if this position is not yet filed, void + re-promote to $X; if already filed, the filed number stands — advisory only") — no engine filed-year branch. KAT `verify_drift_advisory_is_direction_aware…` (:1086-1101) pins the conditional phrase (`"void" && "re-promote" && "not yet filed"`) AND the below-direction (`!promote_drift_advisory(&ev, &lo).is_empty()`) AND fold-uses-stored (`usd_basis == 12_000`).

- **tax N-1 (T9 KAT did not pin the `{delta:0, deduction:Some}` permission) → RESOLVED.** `a_computing_removal_flagged_year_carries_the_deduction_delta` (:900-908) now asserts `ComputedTax { delta_usd, deduction_delta_usd: Some(d), .. } if *delta_usd == Usd::ZERO && *d != Usd::ZERO` — both pinned, so a mutation emitting `{delta: nonzero, deduction: Some}` dies.

- **arch r3 I-1 (FoldCtx has FOUR sites) → RESOLVED (cross-lens, tax-relevant).** Verified four sites in source. T4 Files (:411-420) name all three fold.rs sites (`fold`:413, `pools_before`:463, `state_as_of`:520) + transition.rs:60, each from `res.promotes`; the optimizer KAT `the_optimizer_sees_the_clamped_promoted_basis_not_a_phantom` (:506-514) pins that `state_as_of` uses the clamped basis (an empty set would hand the optimizer un-clamped promoted basis → wrong tax-minimization advice); N-1 note pins the `consume_fee`-param at site :362.

---

*Fresh-pass items checked and NOT filed (no defect):* the clamp's `net_proceeds_share` == the leg's own `proceeds` field (same remainder-takes-rest split, fold.rs:133-140) — no off-by-remainder divergence; the post-clamp disposal-fee re-home (fold.rs:654) can drive the last leg negative only via **documented** fee (T5 strips estimate at the `FeeCarry` source, so all three re-home sites inherit) → genuine loss, no estimate leak past the clamp; rounding a spurious ±1¢ `documented_share` still nets `reported = net` when `net − documented ∈ [0, est]`, so no cent-scale estimate-enabled loss; T6 removal uses documented-only evaporation (no clamp — a removal recognizes no gain, nothing to clamp into), and its fee channel (rehome_onto_removal_leg) also inherits T5, so the estimate funds neither the §170(e) deduction nor the §1015 carry on any surface; T9 consent saving = tax($0-basis) − tax(clamped) is now MORE honest under the amendment (the phantom −$30 is no longer quoted); the amendment strictly reduces §6662(e)/(h) exposure (files a higher gain than a loss). The amendment holds; the gate can close.
