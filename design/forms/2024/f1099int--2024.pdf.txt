https://www.irs.gov/pub/irs-prior/f1099int--2024.pdf

# f1099int--2024.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  ee7697c9b29374596fd9645b157b7312d121ae3c2f76bebf68908c3fc2b739e6
# bytes   538774
#
# Fetch:    curl -sL -o f1099int--2024.pdf https://www.irs.gov/pub/irs-prior/f1099int--2024.pdf
# Verify:   sha256sum f1099int--2024.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/f1099int--2024.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
#
# Archived 2026-09-06 for the interview build (design/SPEC_interview.md r2 §7 T2): the
# information returns the document screens (R4) transcribe their boxes from, so a box
# caption is checkable against the form's own text instead of a hand-list.
# REVISION, READ OFF THE DOCUMENT'S OWN TEXT (never assumed from the filename):
#   "(Rev. January 2024)" (extract:24); footer "Form 1099-INT (Rev. 1-2024)" (extract:55).
# PERIODIC (continuous-use): irs-prior has no --2025 edition (404); the January 2024
# revision is the one in force for TY2025, so it is archived under 2024, its revisions year.
