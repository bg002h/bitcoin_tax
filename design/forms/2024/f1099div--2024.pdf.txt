https://www.irs.gov/pub/irs-prior/f1099div--2024.pdf

# f1099div--2024.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  4ea1de804ff1db921753bd6319beeccfe4647299e03d79816e04d852a9cd1435
# bytes   542425
#
# Fetch:    curl -sL -o f1099div--2024.pdf https://www.irs.gov/pub/irs-prior/f1099div--2024.pdf
# Verify:   sha256sum f1099div--2024.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/f1099div--2024.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
#
# Archived 2026-09-06 for the interview build (design/SPEC_interview.md r2 §7 T2): the
# information returns the document screens (R4) transcribe their boxes from, so a box
# caption is checkable against the form's own text instead of a hand-list.
# REVISION, READ OFF THE DOCUMENT'S OWN TEXT (never assumed from the filename):
#   "(Rev. January 2024)" (extract:26); footer "Form 1099-DIV (Rev. 1-2024)" (extract:56).
# PERIODIC (continuous-use); no --2025 edition exists.
