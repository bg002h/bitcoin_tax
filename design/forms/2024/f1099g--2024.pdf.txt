https://www.irs.gov/pub/irs-prior/f1099g--2024.pdf

# f1099g--2024.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  fe46acb40d53442cca67fba55f79aa7683ab8d351e109096c3d6000f60e6226f
# bytes   523268
#
# Fetch:    curl -sL -o f1099g--2024.pdf https://www.irs.gov/pub/irs-prior/f1099g--2024.pdf
# Verify:   sha256sum f1099g--2024.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/f1099g--2024.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
#
# Archived 2026-09-06 for the interview build (design/SPEC_interview.md r2 §7 T2): the
# information returns the document screens (R4) transcribe their boxes from, so a box
# caption is checkable against the form's own text instead of a hand-list.
# REVISION, READ OFF THE DOCUMENT'S OWN TEXT (never assumed from the filename):
#   "(Rev. March 2024)" (extract:25); footer "Form 1099-G (Rev. 3-2024)" (extract:45).
# PERIODIC (continuous-use); no --2025 edition exists. ★ MARCH, not January: the
# dispatch brief expected "Rev. January 2024" for all six continuous-use 1099s, and the
# 1099-G pair reads March. The document decided it, not the brief.
