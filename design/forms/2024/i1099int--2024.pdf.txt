https://www.irs.gov/pub/irs-prior/i1099int--2024.pdf

# i1099int--2024.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  6f13ff025d4ab270ac53d9f1b60f7f354b1e558366b9d6bfd49cc60bf57fbc63
# bytes   196656
#
# Fetch:    curl -sL -o i1099int--2024.pdf https://www.irs.gov/pub/irs-prior/i1099int--2024.pdf
# Verify:   sha256sum i1099int--2024.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/i1099int--2024.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
#
# Archived 2026-09-06 for the interview build (design/SPEC_interview.md r2 §7 T2): the
# information returns the document screens (R4) transcribe their boxes from, so a box
# caption is checkable against the form's own text instead of a hand-list.
# REVISION, READ OFF THE DOCUMENT'S OWN TEXT (never assumed from the filename):
#   "(Rev. January 2024)" (extract:7).
# PERIODIC (continuous-use); no --2025 edition exists. ★ Titled "Instructions for Forms
# 1099-INT and 1099-OID" (extract:1-2) — the IRS publishes one booklet for the pair.
