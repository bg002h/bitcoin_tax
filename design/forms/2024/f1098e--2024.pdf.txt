https://www.irs.gov/pub/irs-prior/f1098e--2024.pdf

# f1098e--2024.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  5ff3e77936678d73625f5fe74583a4988632924039875d4e2537416a41569b3b
# bytes   482193
#
# Fetch:    curl -sL -o f1098e--2024.pdf https://www.irs.gov/pub/irs-prior/f1098e--2024.pdf
# Verify:   sha256sum f1098e--2024.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/f1098e--2024.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
#
# Archived 2026-09-07 folding the interview T2 seam review (C1): the archive is PER REVISION
# and serves TY2024, TY2025 and TY2026, so `box_census::revision_in_force(stem, tax_year)`
# resolves which edition a row's own tax year governs instead of pinning one.
# REVISION, READ OFF THE DOCUMENT'S OWN TEXT (never assumed from the filename):
#   "2024", printed on the form face (extract:24) beside "Form 1098-E" (extract:26).
# ANNUAL cadence.
