https://www.irs.gov/pub/irs-prior/fw2--2026.pdf

# fw2--2026.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  61eca7c81f16d3965819fe1f31be4fe68c1b2887a81f51172f1d2ed2b2b9f087
# bytes   2150352
#
# Fetch:    curl -sL -o fw2--2026.pdf https://www.irs.gov/pub/irs-prior/fw2--2026.pdf
# Verify:   sha256sum fw2--2026.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/fw2--2026.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
#
# Archived 2026-09-07 folding the interview T2 seam review (C1): the archive is PER REVISION
# and serves TY2024, TY2025 and TY2026, so `box_census::revision_in_force(stem, tax_year)`
# resolves which edition a row's own tax year governs instead of pinning one.
# REVISION, READ OFF THE DOCUMENT'S OWN TEXT (never assumed from the filename):
#   "2026", printed on the form face beside "W-2 Wage and Tax Statement" (extract:80).
# ANNUAL cadence.
# ★★ THE PREAMBLE WAS REWRITTEN. This edition does NOT carry "See Publications 1141, 1167,
# and 1179" — its Attention block ends "See IRS Publication 1141 …" (extract:26) and
# "See IRS Publication 1223 …" (extract:27-28). The box census bounds each edition's face
# block on that edition's OWN preamble sentence for exactly this reason.
# ★★ THE GRID MOVED: box 14 became 14a "Other" and 14b "Treasury Tipped Occupation Code(s)"
# (extract:59,71) — a new box, and the tips-occupation code Schedule 1-A line 4a reads.
