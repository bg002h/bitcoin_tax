https://www.irs.gov/pub/irs-prior/f1098e--2026.pdf

# f1098e--2026.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  bffb9b5ec4f06d47391a5fa7b773d1c4197c587428ace03a96914a9038389602
# bytes   495874
#
# Fetch:    curl -sL -o f1098e--2026.pdf https://www.irs.gov/pub/irs-prior/f1098e--2026.pdf
# Verify:   sha256sum f1098e--2026.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/f1098e--2026.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
#
# Archived 2026-09-07 folding the interview T2 seam review (C1): the archive is PER REVISION
# and serves TY2024, TY2025 and TY2026, so `box_census::revision_in_force(stem, tax_year)`
# resolves which edition a row's own tax year governs instead of pinning one.
# REVISION, READ OFF THE DOCUMENT'S OWN TEXT (never assumed from the filename):
#   "2026", printed on the form face (extract:50) beside "Form 1098-E" (extract:52).
# ANNUAL cadence.
# ★ This edition prints the IRS's own revision-to-year rule (extract:2-5), under the heading
# "Which Revision To Use for Which Year": "the year of the revision date is the first year for
# which issuers are to use the form to report amounts."
# That sentence is what `box_census::revision_in_force` implements.
