https://www.irs.gov/pub/irs-prior/i8283--2025.pdf

# i8283--2025.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  2de015f75bcf2fa9222f8ce6f5ab394d1845ec64ec6cb53ce4e60a4c88150073
# bytes   214214
#
# Fetch:    curl -sL -o i8283--2025.pdf https://www.irs.gov/pub/irs-prior/i8283--2025.pdf
# Verify:   sha256sum i8283--2025.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/i8283--2025.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
