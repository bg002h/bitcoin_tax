https://www.irs.gov/pub/irs-prior/iw2w3--2025.pdf

# iw2w3--2025.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  bfecbc99720cc8fbedb50f6e698ac1f6fa758dbfa5df775715446b7cc30bae2b
# bytes   523710
#
# Fetch:    curl -sL -o iw2w3--2025.pdf https://www.irs.gov/pub/irs-prior/iw2w3--2025.pdf
# Verify:   sha256sum iw2w3--2025.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/iw2w3--2025.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
#
# Archived 2026-09-06 for the interview build (design/SPEC_interview.md r2 §7 T2): the
# information returns the document screens (R4) transcribe their boxes from, so a box
# caption is checkable against the form's own text instead of a hand-list.
# REVISION, READ OFF THE DOCUMENT'S OWN TEXT (never assumed from the filename):
#   "2025 / General Instructions for / Forms W-2 and W-3" (extract:1,3-4).
# Annual. This is the W-2s instructions document: the IRS numbers it iw2w3, not iw2.
