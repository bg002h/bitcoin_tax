https://www.irs.gov/pub/irs-prior/f1099b--2025.pdf

# f1099b--2025.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  7e334cea66c2b2be3d88615a56db288a2805cb8705076b3b282a4eabbbd788ad
# bytes   601719
#
# Fetch:    curl -sL -o f1099b--2025.pdf https://www.irs.gov/pub/irs-prior/f1099b--2025.pdf
# Verify:   sha256sum f1099b--2025.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/f1099b--2025.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
#
# Archived 2026-09-06 for the interview build (design/SPEC_interview.md r2 §7 T2): the
# information returns the document screens (R4) transcribe their boxes from, so a box
# caption is checkable against the form's own text instead of a hand-list.
# REVISION, READ OFF THE DOCUMENT'S OWN TEXT (never assumed from the filename):
#   "2025", printed on the form face beside "Form 1099-B" (extract:24).
# Annual cadence.
