https://www.irs.gov/pub/irs-prior/i1098et--2025.pdf

# i1098et--2025.pdf — IRS primary source, NOT committed (publicly available; keeps the repo small).
# sha256  68c0f34312f15295ac882e3b4148c0cc93df1f5cf31de8c8f9793258672bafb1
# bytes   161690
#
# Fetch:    curl -sL -o i1098et--2025.pdf https://www.irs.gov/pub/irs-prior/i1098et--2025.pdf
# Verify:   sha256sum i1098et--2025.pdf   # must equal the sha256 above
#
# ★ A DIFFERENT hash does not mean a corrupt download — it means the IRS REVISED this
# document. That is a change to the authority: review it, never silently absorb it.
#
# The committed text layer is design/forms/extract/i1098et--2025.txt — that is what the
# conformance tests read, so they run with no PDF and no network.
#
# Archived 2026-09-06 for the interview build (design/SPEC_interview.md r2 §7 T2): the
# information returns the document screens (R4) transcribe their boxes from, so a box
# caption is checkable against the form's own text instead of a hand-list.
# REVISION, READ OFF THE DOCUMENT'S OWN TEXT (never assumed from the filename):
#   "2025 / Instructions for Forms / 1098-E and 1098-T" (extract:1,3-4).
# Annual. ★ The 1098-Es instructions are the COMBINED 1098-E/1098-T booklet: there is no
# i1098e document. The MANIFEST instructions join for a 1098-E row is i1098et.
