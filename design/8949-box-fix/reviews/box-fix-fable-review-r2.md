# Round-2 re-review — year-aware Form 8949 box fix

Reviewer: independent Fable (did not author). Round: r2 (re-review after the r1 fold).

## R1 finding dispositions

**IMPORTANT-1 (TUI footnote steers TY2025 filer to A/B/D/E) — RESOLVED.**
`crates/btctax-tui/src/tabs/forms.rs:172-182`: the footnote is now branched on `year >= DIGITAL_ASSET_8949_FIRST_YEAR` — the exact constant the core box assignment uses (re-exported from `btctax-core/src/lib.rs`; core predicate at `forms.rs:132-134`), and the same `year` parameter that feeds `form_8949(&snap.state, year)` at forms.rs:69, so table and note cannot diverge. No drift: single source of truth, no local `2025` literal. The 2025 era gets "Review box I/L … may require G/H/J/K (1099-DA)"; pre-2025 gets "Review box C/F … may require A/B/D/E (1099-B)" — and the pre-2025 text correctly dropped the old "1099-DA" mention (no 1099-DA existed pre-2025). The editor crate (`btctax-tui-edit`) renders through this same `render` fn, so it inherits the fix. Goldens verified in-file: `docs/examples-tui-walkthrough/j2/03-forms.txt` is a "Forms — 2025" pane showing the I/L note; `j6/04-forms.txt` is a "Form 8949 — 2024" pane showing the C/F note. Repo-wide grep for "Review box" finds no other emission surface. No code path steers a TY2025 filer to a securities box.

**IMPORTANT-2 (vacuous TUI box test) — RESOLVED.**
- F0 (`tests.rs:1560-1565`) pins all four `form8949_box_tag` arms by exact string equality. The realistic revert — flipping the `I`/`L` arms back to `"C"`/`"F"` — now fails F0 outright (and removing the arms fails to compile: exhaustive match). Non-vacuous.
- F1 (`tests.rs:1575-1612`) is now row-scoped: `buffer_row_containing(&buf, "0.50000000 BTC")` isolates the single 8949 data row (fixture has exactly one disposal, no donations, and Schedule D lines carry no BTC-denominated text — the needle is unique in the buffer). The `!row.contains('F')` discriminator is sound for this fixture: the row's cells are "LT", the box letter, "0.50000000 BTC", dates "2023-01-01"/"2025-06-15", and "30000.00"/"20000.00"/"10000.00" plus border glyphs — no legitimate 'F' anywhere, so a revert that renders F on a 2025 row fails F1 too. Not flaky. `buffer_row_containing` (tests.rs:415-428) is correct (per-row symbol concatenation, first match; ASCII needle unaffected by wide/continuation cells). Doc comment updated to Box L.
- The 2024→C rendered direction is positively pinned by the j6 golden (`ST   C   0.05000000 BTC` data row, enforced by the walkthrough golden test), plus F0's C/F arms.
- Residual note (non-gating): F1 has no *positive* assertion that the rendered 2025 row shows "L" (`contains('L')` would be vacuous against "LT"). A contrived mutant that bypasses `form8949_box_tag` in render and hardcodes `"C"` escapes F0/F1 — but it would fire the dead-code lint on the now-unused `pub(super)` fn (clippy gates CI), and the r1 fix asked for the unit test *and/or* the row assertion; both delivered. I don't file this.

**MINOR-1 (Form8949Row field docs) — RESOLVED.** `forms.rs:63-76` (enum), `:78-80` (const), `:87-102` (fields): all rewritten year-aware and accurate — C/F↔A/B/D/E/1099-B pre-2025, I/L↔G/H/J/K/1099-DA from 2025, i8949 quotes match what r1 verified against the bundled PDFs. No new misstatement ("G–L" for the 2025 grid is correct).

**MINOR-2 (stale cross-file comments) — RESOLVED** for all four cited sites: `common/mod.rs:29-33` (new comment's claim that `row.box_` is inert to the fill layer matches r1's verified point 3), `fill8949.rs:5-7`, `printed.rs:60-64`, `packet.rs:431-434` — all accurate, including the correct 2025 Schedule D "Box C or Box I" / "Box F or Box L" line text. The 2024-dated fixtures hand-setting C (`printed.rs:1814`, `full_return_forms.rs:1993/2311`) are now era-*consistent* with their dates. Two same-class sites the sweep missed are filed below as new residue.

**NIT-1 — RESOLVED.** `pre_2025_…` now asserts `rows.len() == 2` (kat_forms.rs:151), and the new `post_2025_st_leg_is_box_i_and_lt_leg_is_box_l` KAT builds genuine 2026-dated disposals and calls `form_8949(&st, 2026)` asserting I/L — the `>=` half is genuinely pinned, not just the 2025 equality.

## New findings

### NEW-IMPORTANT-1 — CLI PDF-export [I5] advisory is year-blind: a TY2024/TY2017 export prints 2025-only guidance and misdescribes its own output
`crates/btctax-cli/src/main.rs:792-801` (report field: `crates/btctax-cli/src/cmd/admin.rs:399`, computed unconditionally for the export's `tax_year`)

The advisory is a single hardcoded string behind `if report.broker_reported_rows > 0` with no year gate. `export-irs-pdf` supports TY2017/TY2024 (cli.rs:198, SP3/SP3b shipped it), and `box_needs_review` is set for exchange wallets in every era — so a 2024 or 2017 export with any exchange disposal prints: "MAY have issued **1099-DA** broker basis reporting — those would belong on a SEPARATE Form 8949 under **Box G/H/J/K**. This export files EVERY Bitcoin row under **Box I/L** (not-reported default)…". Three defects for that year: (1) no 1099-DA existed pre-2025 — the relevant broker form is the 1099-B; (2) boxes G–L do not exist on the 2024/2017 revisions (r1 verified from the bundled PDFs: 2024 Part I is (A)(B)(C) only) — the filer is steered to checkboxes that aren't on the form; (3) the sentence "files EVERY Bitcoin row under Box I/L" is a false statement about the very PDFs just written — the 2024/2017 maps check Box C/F (correctly, read-back-pinned). This is the exact mirror of r1's IMPORTANT-1 (era-wrong advisory text on a box-fix surface), graded identically: not Critical because the filled PDFs are correct; Important because it's real user-facing tax guidance inside this review's declared scope ("every surface in the repo that emits or documents a Form 8949 box"). Pre-existing — an r1 miss (r1 point 6 blessed this advisory from the 2025 example only) — but the whole-surface rule gates it regardless of provenance. No existing test pins the wording (`kats.rs:368` pins only the counter), so the fix breaks nothing.
**Fix:** branch the advisory on `tax_year >= DIGITAL_ASSET_8949_FIRST_YEAR` (2025+: current text; pre-2025: 1099-B / "Box A/B (ST), D/E (LT)" / "files EVERY Bitcoin row under Box C/F"), sweep the two doc precursors (`btctax-forms/src/lib.rs:354-356` "SP1 files EVERY … Box I/L", `admin.rs:156`), and pin **both** eras with a stderr test — per the project's untested-guard pattern.

### NEW-MINOR-1 — doc residue the fold left behind (aggregate; same class as r1 MINOR-2, non-gating)
- `crates/btctax-tui/src/tabs/forms.rs:30`: fn doc still reads `Values: "C" (short-term) / "F" (long-term)` on the now-four-letter `form8949_box_tag` — this very fold touched the function (added arms, made it `pub(super)`) but not its doc; the CLI twin's doc was rewritten.
- `crates/btctax-cli/tests/export.rs:121`: test header says "(Part I / **box C** / box_needs_review true)" while the fold changed the assertion three lines below to `"I"` — invites a future "fix" of the assertion back toward the doc.
- `crates/btctax-core/tests/kat_forms.rs:77`: the leading doc line "ST leg → Part I / box C; LT leg → Part II / box F." still states the pre-fix mapping unconditionally atop the `ty2025_…box_i…box_l` test (the appended lines correct it, but the first line should read as pre-2025-scoped or be deleted).
- `crates/btctax-core/src/tax/printed.rs:787` and `crates/btctax-forms/src/fill8949_full.rs:3-4`: two more era-unconditional quotes of the Schedule D "Box C / Box F checked" text — same class MINOR-2 swept, uncited by r1.
**Fix:** one wording sweep, no behavior change.

Everything else checked clean: cli.rs man-page docs (152-160, 198-201) are year-correct both eras; `docs/examples/examples.md` box mentions are the 2025 I5 example (correct) and W-2/1099 boxes (unrelated); no serde/Debug leak path for `Form8949Box` (every emission goes through the two exhaustive tag fns or the map-driven fill); the exchange-flag KAT is a genuine 2025 fixture; `lib.rs` re-export compiles and `pub(super)` reachability from `tabs::tests` is sound (suite green at 2085).

## Verdict

**NOT-GREEN — 0 Critical / 1 Important** (the year-blind I5 export advisory at `crates/btctax-cli/src/main.rs:792-801`), plus 1 Minor doc-residue aggregate. All five r1 findings are genuinely resolved — the fold itself introduced no defect — but the whole-surface sweep this fix's scope demands turns up one more era-wrong box-guidance emitter, and it is the same severity class r1 gated on.
