# App-side polish batch (walkthrough residue) — select-lots fold review r2 — NOT GREEN (NEW-1)

_Fable, independent. Reviews the uncommitted select-lots fold (delegate post-2025 candidate build to
`Session::available_lots_before`). All five r1 findings RESOLVED; re-review surfaced 1 NEW Important on the
SAME function's pre-2025 arm, plus non-gating Minors/Nits._

---

VERDICT: NOT GREEN — all five r1 findings are RESOLVED, but the re-review surfaced 1 NEW Important on the same function (pre-2025 arm), plus non-gating Minors/Nits.

**C1: RESOLVED** — the post-2025 build delegates to `Session::available_lots_before` (crates/btctax-tui-edit/src/main.rs:4125-4140) → `optimize::available_lots_before` (crates/btctax-core/src/optimize.rs:272, now `pub`) → `fold::pools_before` (crates/btctax-core/src/project/fold.rs:450-481). `pools_before` replays the canonical timeline with `fold`'s exact ordering (fold.rs:456-458) and stops immediately BEFORE the target EventId with the §7.4 boundary seed already fired (fold.rs:468-477) — so a lot acquired after the event, or a fragment relocated in by a later self-transfer (fresh lot_id via `bump_split`, fold.rs:801-806), does not exist yet: membership correct by construction. This is the identical `PoolSet` the real fold holds when processing the target, and `selection_feasible` (crates/btctax-core/src/project/pools.rs:107-153) validates against `pools.get(key)` with `key = pool_key(date, wallet)` — the same key `available_lots_before` filters on (optimize.rs:319-324). Crucially, the principal draw runs FIRST within `fold_event` for all four op kinds (Dispose fold.rs:624 before `consume_fee` :641; SelfTransfer :788 before :824; Gift :1099 before :1114; Donate :1176 before :1191), so the offered pool is exactly the validated pool even with a Treatment-(b) fee. `item.date`/`item.wallet` match the fold's `eff.date()`/`eff.wallet` (main.rs:4760/4769, 4787/4794, 4814/4822). Candidate set == accepted set; no offered pick can be rejected.

**I1: RESOLVED** — no `find` over `state.disposals` remains (main.rs:4117-4141). The EventId is a fold boundary, and the fee mini-disposition is not a timeline event (it is generated inside `fold_event` of the same event), so exactly one timeline event carries the id — no shadowing is possible. Boundary semantics with a shared-EventId fee are correct per the consumption order above.

**I2: RESOLVED** — the branch is date-gated only (main.rs:4098); post-2025 Gift/Donate/SelfTransfer reach the delegation. `pools_before` stops on the id regardless of op kind, and all removal/self-transfer principal draws go through the same `consume_principal`/`pool_key` path (fold.rs:788, 1099, 1176). Self-transfer items pass the raw TransferOut id (main.rs:4816), which IS the timeline eff id — the same id `ctx.selections.get(&eff.id)` keys on, so it is self-consistent with selection application.

**I3: RESOLVED** — the new comment (main.rs:4118-4124) and the FOLLOWUPS "J9 app-limit — DONE" entry (FOLLOWUPS.md:2618-2629) accurately describe the delegation, explicitly supersede the hand-rolled add-back and name its defect, and scope the guarantee to what the form OFFERS (true). Note: the overclaim survives in committed history (e59768c's message, "Provably never over-offers"); the r2 fold's commit message should state the correction.

**I4: RESOLVED** — the core primitive is held by 5 discriminating KATs (optimize.rs:1781 canonical-not-load-order kills the acquired-after class; :1828 cross-wallet; :1878 Path-B seeded; :1953 Path-A relocated; :1993 deterministic) — re-ran: 5/5 pass. The TUI wiring (including the `Session` wrapper: `open_app` → `do_unlock` holds a real session) is pinned by the J9 driver's `rows.len() == 2` assert (main.rs:14527-14534), which discriminates against a revert-to-residue mutation (HIFO fully consumes lot-b → residue shows 1 row) — re-ran `btctax_tui_edit_walkthrough_goldens_match_committed`: pass. No per-kind branches remain in the TUI to mutate. Residual gaps are Nit-level (below).

## New findings

**Important NEW-1 — the PRE-2025 arm of the same function retains the identical doomed-offer membership defect (pre-existing; blocks under the project's severity rules).** main.rs:4098-4116 offers final `snap.state.lots` filtered only by `acquired_at < TRANSITION_DATE && basis_source != SafeHarborAllocated`, cross-wallet — final-state membership and amounts, not at-disposal. Two infeasible classes pass: (a) a lot acquired AFTER the disposal but before 2025; (b) a later self-transfer's relocated fragment (fresh lot_id, fold.rs:801-806; original pre-2025 `acquired_at`, fold.rs:808 — r1-C1's own "no acquired_at filter detects it" mechanism, on the other branch). Concrete forcing scenario: Buy X 1.0 BTC 2023-01; Sell D 0.5 2023-06; Buy Y 0.3 2024-01; Sell D2 0.3 2024-06. Form for D offers X=0.2 (final residue; true at-D was 1.0) and Y=0.3; the ONLY Σ==principal combination is X=0.2+Y=0.3 (form validation passes, form.rs:1342) → persisted → at D's replay Y does not exist in any pool → `selection_feasible` "does not exist" (pools.rs:118-136) → hard `LotSelectionInvalid` → tax NotComputable until voided (guided recovery via Arm 2, main.rs:4931-4938). The form cannot produce a valid selection for D at all. Verified pre-existing: e59768c only restructured this filter into an if/else labeled "unchanged" (r1 confirmed same), and the batch's claims are post-2025-scoped — no false claim — which is why this is Important, not Critical. It nonetheless meets "real defect, missing case" and blocks per the workflow (Important cannot be parked as a follow-up). The fix is nearly free: delegate the pre-2025 arm to the SAME `sess.available_lots_before` call — for a pre-2025 date `pool_key` yields Universal (optimize.rs:319; docs :253), the filter then passes the whole pre-boundary Universal pool cross-wallet (exactly the intended offer, with correct membership AND at-disposal amounts), the SafeHarborAllocated exclusion becomes automatic (no seed lots exist pre-boundary), and every listed item has `Some(wallet)` (wallet-less events are blocker-filtered, main.rs:4756/4783/4806) — so the `(Some(w), Some(sess))` arm covers it and the branch can simply be deleted.

**Minor M-1** — `validate_select_lots` still has no per-row cap (form.rs:1318-1355, Σ==principal only): a user typing MORE than a row's displayed Remaining persists a pick the engine rejects → hard blocker with guided void recovery. User-error-triggered, immediately surfaced (Arm 2), pre-existing; the batch's "never offer" claim is about the offer, which is true. Fix: per-row `sat ≤ remaining_sat` at form validation.

**Minor M-2** — `Session::available_lots_before` (session.rs:411-426) calls `load_events_and_project`, running a FULL projection whose `LedgerState` is discarded, then `available_lots_before` re-resolves and partial-folds again — per Enter keypress. Acceptable at personal-ledger scale (goldens test incl. 8 journeys runs in ~3s); `load_all` + `config()` directly would drop the wasted fold.

**Minor M-3** — `.unwrap_or_default()` (main.rs:4128) conflates a session load error with an empty pool: on a vault read failure the user sees "No lots available for wallet …; check Holdings" (main.rs:4154). Unlikely (in-memory conn), but misleading degradation.

**Nit N-1** — `_ => Vec::new()` (main.rs:4139) is dead-defensive (Browse requires a held session; wallet-None items are pre-filtered) and untested; fine as defense.

**Nit N-2** — the pre-existing "#2: feasibility-honest candidate-lot gate" comment (main.rs:4091-4096) says "(no method-order fallback)" — false: `consume_principal` DOES fall back to method order alongside the hard blocker (fold.rs:49, :64-67; pools.rs:80-87). Its "feasibility-honest" branding is also what NEW-1 falsifies.

**Nit N-3** — the TUI's `.filter(|l| l.remaining_sat > 0)` (main.rs:4130) is redundant (optimize.rs:324 already filters).

## Confirmed on the new-defect hunt
Borrow correctness: `snap` (immutable borrow of `app.snapshot`) is last used in the pre-2025 arm; `app.session.as_ref()` is a second shared borrow; the `app.status`/`app.select_lots_flow.as_mut()` mutations follow both borrows' NLL end — sound, and compiles (tests ran). Golden byte-identity is EXPECTED, not suspicious: for J9's single no-fee sale, the old add-back reconstructed exactly this disposal's consumption (proportional `take_from` basis restores 15000), so both builds yield lot-a 60M/15000 + lot-b 40M/24000; the committed-goldens test passes. Determinism holds (`pools_before` output sorted by lot_id, then stable `sort_by_key(acquired_at)`).

Validation on the current tree: `available_lots_before_*` KATs 5/5 pass; `btctax_tui_edit_walkthrough_goldens_match_committed` passes; `make check` 2071 trusted as given.

Bottom line: the fold genuinely resolves all of r1 (C1, I1-I4) for the post-2025 path — that work is done and well-anchored. The single blocking item is NEW-1: the pre-2025 arm of the same function still hands the user doomed picks, and the now-landed delegation fixes it by deletion of the branch. Fold that (plus, ideally, M-1's per-row cap) and re-review.
