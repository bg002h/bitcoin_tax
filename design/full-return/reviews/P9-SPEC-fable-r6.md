# Fable — independent review of SPEC_form_questions.md (P9), round 6

*Persisted VERBATIM before folding. Author = Opus (`07e9996`); reviewer = Fable. Tree at HEAD `07e9996`, working tree clean before and after review; `make check` green at HEAD (1730/1730, 7.2s, exit 0). Every disputed claim this round was decidable by reading shipped code; all cited file:line references re-verified against HEAD.*

**Verdict.** r6 is the strongest revision yet: every r5 Important is genuinely folded, no r1–r5 fold has silently regressed, the census survives an independent re-sweep, the build order has no red-ending step, and the author's rejection of my predecessor's I-1 fix is **correct on the merits** — "treat a stale row as absent" would bypass the R3-M6 preservation block (`cmd/tax.rs:58-95`) and silently drop a `Computed` carryover, the exact fail-open understatement that block exists to prevent, and `income clear` (cli.rs:374 → `clear_return_inputs`, `cmd/tax.rs:137-144` → a bare `DELETE` at `return_inputs.rs:116-121` that never deserializes) genuinely works on every stale row. But the class kept its streak — **seven for seven** — and it recurred exactly where the author's own rejection argument pointed: the clear-then-import remedy unbricks every consumer, and then leaves the vault in precisely the carryover-less state whose avoidance justified the rejection, with the one command that restores it (`report --tax-year <N-1> --write-carryover`) **named nowhere in the remedy chain** — and the shipped note that names it (`cmd/tax.rs:88-95`) is structurally unreachable after a `clear`, because it only fires when a row exists to preserve. The remedy was traced for the refusal and not for the data. **NOT GREEN — 0C / 1I / 2M / 5Nit.**

---

## Adjudication of the rejected r5 I-1 fix: **THE AUTHOR IS RIGHT** — with one Important residue his own argument demands

**1. "Treat as absent" silently drops a Computed carryover and understates tax — verified, exactly as the author says.** The stale-row read at `cmd/tax.rs:63` is not incidental: lines 58–95 are the §4 R3-M6 preservation block, whose own comment states the direction — *"losing the REIT/PTP loss carryforward OVERSTATES the QBI deduction ⇒ understates tax"* — and which preserves `Computed`-provenance charitable carryover items and the QBI REIT/PTP carryforward whenever the incoming TOML supplies none. Catching `StaleReturnInputs` and "proceeding as if no row existed" skips that block entirely: the whole-blob upsert (`return_inputs::set`, an `ON CONFLICT … DO UPDATE`) replaces the row, the computed carryovers vanish, and even the disclosure note at :88 (the *only* place in the program that tells the user to re-run `--write-carryover`) is skipped, because it lives inside the same `if let Some(existing)`. My predecessor's fix would have re-armed a silent fail-open loss inside P9's own migration policy. Rejection sustained.

**2. `income clear` genuinely works on a stale row — traced.** `Income::Clear` (cli.rs:374) → `clear_return_inputs` (`cmd/tax.rs:137-144`): `Session::open` + `return_inputs::delete` (a bare `DELETE`, `return_inputs.rs:116-121`) + `save`. No path deserializes the blob; it cannot hit `row_to_inputs` and cannot refuse. Works on stale, corrupt, or future-versioned rows alike.

**3. The consumer inventory — every reader of a stale row has clear-then-import as an acceptable exit.** I inventoried every production call into the module: `resolve.rs:85` (report/TUI resolution — TUI converts per-year errors to `Uncomputable`, verified by r5 at `session.rs:502-514`), `answer.rs:191` (get-then-set), `admin.rs:443` (export — fail-closed, writes no bytes), `tax.rs:63` (import), `:154` (`income show`), `:240` (dual report), `:412`/`:426` (`write-carryover`, both years). For each, the exit is `clear` + `import` of the named year, and the refusal detail's `{year}` is correct per-row (write-carryover on a stale year+1 refuses naming year+1).

**4. ★ The carryforward chain is rebuildable — the remedy is NOT a brick.** The chain has depth 1 in v1: `write_carryover` requires `full_return_for(year)` (TY2024 only) and writes onto year+1's existing row (`tax.rs:426-438`). So the day-one-all-stale vault recovers fully: `clear` + `import` 2024 and 2025 (TOMLs are in the repo — owner premise: no real data), then `report --tax-year 2024 --write-carryover` recomputes the discarded carryovers from 2024's inputs and writes them onto 2025's v2 row. Every `Computed` carryover is a pure function of the prior year's inputs, which survive in TOML. Nothing is unrecoverable.

**5. But the spec never says step 4.** That is I-1 below — the r6 recurrence of the class.

---

## Fold verification — every r5 finding, plus the r1–r4 regression spot-check

| r5 finding | folded? | evidence |
|---|---|---|
| I-1 (remedy doesn't work) | **✓ folded by REJECTION + alternative — adjudicated above; author right.** Refusal text names `clear` then `import` and discloses the discard (§2.6 code block); named tests incl. the clear-first mutation (§2.6/§3.5); step 11 LIMITATIONS entry; §4 bullet rewritten. Residue → **I-1 below.** |
| I-2 (§3.5 asserted the deleted mortgage refusal) | **✓** | §3.5 rewritten: "FOUR, not five"; mortgage `n` assertion is now compute + 8a=0 + box checked + advisory fires; the "(when itemizing)" trigger appears only as a quoted error. No compute-dependent trigger anywhere in r6. |
| I-3 (`DualStatusAlienUnsupported` scheduled nowhere) | **✓** | Step 8 names it with test + mutation; §3.5's value-refusal table row; §4 bullet amended ("REFUSES AS UNSUPPORTED, with a named test and a named mutation"). |
| M-1 (advisory text false under standard deduction) | **✓** | §3.4: two branched texts, `forgone_interest` documented as a ceiling ("up to", never "the amount you lost"). |
| M-2 (step 6 ships advisory before its content is true) | **✓** | Step 6 merges value behaviour + advisory as "one mechanism"; regression test under both `Auto`-standard-wins and `ForceItemize`. |
| Nit-1 (step-12 pointers) | ✓ | All five §2.1 pointers now say step 11 (lines 160/187/188/189/191). |
| Nit-2 ("permanently refused") | **✗ NOT folded — third consecutive round** (r4 Nit-1 → r5 Nit-2 → here) | Line 478 still reads "would have **permanently refused** the dormant-HSA holder". → **Nit-1 below.** |
| Nit-3 (refusal-shadowed trigger) | ✓ | §2.9 parenthetical: belt-and-braces, no unreachable-print test, doc-comment update scheduled. |

**r1–r4 regression spot-check — no silent regressions.** r4 I-1: the 8949 Box I/L census row is present and true at HEAD (`fill8949.rs:86-95`, never C/F per :4-6); my independent re-sweep of every `FieldValue::Check` write in `btctax-forms/src` (fill8949 ×1, form1040_full ×3, schedule_a ×1, schedule_b ×1, schedule_c ×1, schedule_d_full ×2, form8283 ×1; form1040.rs is the non-packet short form; pdf.rs is the enum writer) matches the census exactly — **no box missing**. r4 I-2: the mortgage refusal stays deleted; no compute-dependent trigger anywhere in r6 (§2.7 table, §3.2 strikethrough row, §3.5 rewrite, step 6 — checked all four). r4 I-3: I walked all 12 steps; none ends red (see M-1 for a doctrinal, non-red ordering wart). r3 I-5: classifier guarantee stated at honest strength in §3.3 **and** §4 ("a human EDITS", `_`-prefix residue grep-able). r3 I-7: both spouse prompts gated on `spouse.is_some()` (§3.2), with the deliberate question-vs-prompt scope asymmetry recorded. r2 C-1: dormant-HSA holder computes (`Some(false)` proceeds, §2.4; §4 named regression test 1). §2.9 citations re-verified at HEAD (`return_1040.rs:1462-1466` circular predicate; `answer.rs:94-97` gated liveness; `printed.rs:936/:943` `unwrap_or(false)`). P8a I1/I3 remain open at HEAD and genuinely subsumed (§3.1 liveness `Mfj || spouse.is_some()`; `HeaderError::Unanswered`). Existing value-guards `ForeignTrust`, `HsaPresent`, `DependentSpouseUnsupported` all carry tests at HEAD (`return_refuse.rs:1360/:1349/:1436`), so §3.5's new-guard-only mutation table is complete for the guards P9 adds.

---

## Important

### I-1 — The remedy unbricks the refusal but never names the command that restores what `clear` discards: after the sanctioned `clear` + `import`, year+1's row silently lacks its Computed carryovers, and the one message that names `--write-carryover` is unreachable post-clear

**What.** §2.6's remedy is `income clear <year>` then `income import`, with the discard disclosed ("which DISCARDS any carryover this row's prior reports computed onto it"). But disclosure is not restoration. A filer who follows the remedy verbatim on 2025's row ends with a v2 row whose `Computed` charitable carryover and QBI REIT/PTP carryforward are gone; 2025 then resolves through `resolve.rs:85` with an overstated QBI deduction ⇒ **understated tax** — the exact R3-M6 direction the author invoked to reject my predecessor's fix. The restoration command exists and works (`report --tax-year 2024 --write-carryover` recomputes onto 2025's row — traced, adjudication point 4), but it is named **nowhere** in §2.6's refusal text, §3.5's named tests, step 11's LIMITATIONS entry, or §4's bullet. The only place in the program that tells a user to re-run `--write-carryover` is the import-time preservation note (`cmd/tax.rs:88-95`) — which fires only when a stale row *existed to preserve from*, i.e. never after a `clear`. And the M4 `carryforward_consistency` advisory cannot catch it either: it covers only the capital-loss `Carryforward` (`compute.rs:436-448`), not charitable or QBI. So the sanctioned exit ends in a silently wrong year+1 with no advisory, no note, and no named next step.

**Why it blocks.** §4 claims "★★ AND THE REMEDY WORKS — verified, not asserted." The remedy works for the *refusal*; it does not work for the *data whose protection is the section's own stated reason for rejecting the simpler fix*. The author's argument — "requiring `clear` makes discarding the computed carryovers an explicit act" — is only honest if the filer is also told the act is reversible and how. This is r5 I-1's shape one square further along: the exit was traced, the exit's aftermath was not. Seven for seven.

**Evidence.** `cmd/tax.rs:63-95` (preservation + the note that names `--write-carryover`, unreachable when no row exists); `cmd/tax.rs:395-450` (`write_carryover` — restoration works, TY2024→2025, both rows required); `compute.rs:436-448` (M4 covers capital-loss only); SPEC §2.6 refusal text / named tests, §3.5 mutation list, step 11, §4's remedy bullet — no occurrence of `write-carryover` as a remedy step anywhere.

**Fix (spec-level, ~4 sentences).** (1) The §2.6 refusal detail and step 11's LIMITATIONS entry end with: *"…then, if this row carried a computed carryover, re-run `report --tax-year <year-1> --write-carryover` to rebuild it."* (2) Add the named test: seed year N with inputs, write-carryover onto year N+1, mark both rows stale, run the full remedy (`clear`+`import` ×2, `write-carryover`), assert year N+1's carryover equals the pre-stale value — delete the rebuild sentence's trigger (or the write-back) ⇒ the test fails. (3) Amend §4's bullet to "the remedy works **including carryover restoration**". (4) Note in §2.6 that the rebuild is always possible while the policy's premise holds (TOMLs exist; chain depth 1 in v1) — which is one more reason the expiry follow-up (step 12(b)) is load-bearing.

## Minor

### M-1 — Steps 9 and 10 are ordered so the classifier must classify three fields whose class forbids classification

Step 9 builds the classifier ("every `bool`… must be classified as (A) or exempted with class + statutory reason"); step 10 deletes the three class-(D) fields. At step 9's close the classifier must bind `ssn_valid_for_employment` ×2 and `box13_retirement_plan` — but §2's table gives class (D) no lawful disposition except "consume or delete", so the compliant classifier cannot be written: the implementor must either invent a forbidden "(D), pending deletion" exemption entry or reorder on their own authority. It self-heals (step 10's deletion breaks the destructure and forces cleanup) and cannot ship a wrong number, hence Minor, not the red-step Important of r2/r3/r4 — but the build order contradicts the spec's own doctrine for exactly one step. **Fix:** swap steps 9 and 10 (deletion + `serde_ignored` first; neither depends on the classifier), or add one sentence licensing the transient entry.

### M-2 — §3.5's "a naive assert fails on four of eight" is fixture-contingent: `ForeignAccounts` answered `y` on the natural minimal fixture *also* refuses (`ScheduleBForeignCountryMissing`), making it five

The per-question property test builds a return where the question is live, blanks it, then answers `y`. For `ForeignAccounts`, `Some(true)` ∧ blank `foreign_country_names` fires the §3.2 7b refusal — and nothing in §3.5 tells the fixture to supply country names, so the "four of eight" count and the naive-assert claim are wrong for the fixture the paragraph itself implies. Low risk (the operative instruction — assert on the *specific* unanswered reason — is robust either way, unlike r5 I-2 there is no deleted refusal to resurrect), but this is the test-contract section whose miscounts caused r5 I-2, and the count is checkable. **Fix:** one sentence — the `ForeignAccounts` fixture carries a non-empty `foreign_country_names` (or: "four *unconditional*; `ForeignAccounts` `y` adds a fifth, conditional on blank 7b — supply 7b in the fixture").

## Nit

- **Nit-1** — r4 Nit-1 / r5 Nit-2 **still unfolded, third consecutive round**: §2.6's preserved-reasoning block (line 478) says the rejected migration "would have **permanently refused** the dormant-HSA holder" — `income answer` re-asks answered questions, so the adverb is false, and the block is explicitly the text future migrations must follow. Fold it or record its rejection; carrying a nit silently across three folds is itself the discipline fraying at the edge.
- **Nit-2** — Step 1's churn note covers only the four always-live questions. The mortgage question also bricks fixtures at step 4: `testonly.rs:223` and `:526` (the golden builder) plus ~7 inline `return_1040.rs` test fixtures carry nonzero `mortgage_interest_1098` with a `schedule_a`. Same fix, fifth line: default `mortgage_all_used_to_buy_build_improve = Some(true)` in the testonly Schedule-A builders.
- **Nit-3** — Degenerate input: a TOML can supply `mortgage_all_used_to_buy_build_improve = Some(false)` with `mortgage_interest_1098 = 0` (the question is not live, but the field exists). As specced, the box checks and `MixedUseMortgageNotAllocated` fires with `forgone_interest = $0` — a content-free nag. Scope the `Some(false)` value behaviour and advisory to the live predicate (`mortgage_interest_1098 > 0`).
- **Nit-4** — `income show` (`tax.rs:154`) also refuses on a stale row, so the filer cannot inspect what `clear` is about to discard before discarding it. Acceptable under the no-real-data premise, but worth one sentence in §2.6/step 11 — and it is one more tooth for the step 12(b) expiry follow-up.
- **Nit-5** — §3.2/§3.5 name the value-refusal `HsaActivityUnsupported`, but the shipped variant is `RefuseReason::HsaPresent` (`return_refuse.rs:108`). The rename is obviously intended alongside step 1's field rename, but after r4 Nit-3 (an invented variant name) the spec should say "rename `HsaPresent` → `HsaActivityUnsupported`" explicitly.

---

## What I verified sound (so the fold does not re-litigate it)

The **rejection adjudication** end to end (all four points above, each against shipped code). The **§2.6 mechanics**: `clear` exists as a CLI subcommand and never deserializes; `get`/`all` share `row_to_inputs`; `set` stamps both upsert branches; the mutation-check remains keyless and non-vacuous. The **census**: independently re-swept every `FieldValue::Check` write site in `btctax-forms/src` — the write-site inventory matches §2.1 exactly; no box missing. The **build order**: all 12 steps walked; step 1's flips are compile-forced, step 2 self-contained, step 4's merged mechanism closes green (fixture churn is within-step, Nit-2), steps 5–12 depend only on predecessors; no step ends red. **§2.9** citations re-verified (`return_1040.rs:1462-1466`, `answer.rs:94-97`, `printed.rs:936/:943`); the always-live fix strictly subsumes `schedule_b_part3_unanswered`; the value-dependent `KiddieTax` refusal is conditional on the unearned threshold, so `DependentTaxpayer` `y` computes on the natural fixture (the four-count's membership is right; only the naive-assert claim is contingent — M-2). **§3.4**: the branched advisory texts are correct in both deduction outcomes and all filing statuses (MFS-forced-itemize lands in the itemized branch; "up to" hedges the itemized "OVERSTATED" claim correctly); the `persons` formula mirrors `AgedBlindBoxes`. **§4**: every bullet is true as stated and checkable, with the single exception charged as I-1 (the remedy bullet's "verified, not asserted" is verified for the refusal, unverifiable for restoration because no restoration step exists to verify). Existing guards `ForeignTrust`/`HsaPresent`/`DependentSpouseUnsupported` are test-held at HEAD. `make check` green: 1730/1730, 7.2s.

**Bottom line.** The architecture, the census, the statutory doctrine, the build order, and both owner decisions all hold under attack — and for the first time, the author's rejection of a reviewer fix was the *right* call, verified. What remains is one Important with a four-sentence fix, and it is §1's class at its seventh consecutive address: the remedy chain was traced exactly one command short of the data it was redesigned to protect. Fix the restoration sentence, swap two steps, and r7 has a real chance of being the first green round.
