# Fable — independent review of SPEC_form_questions.md (P9), round 7

*Persisted VERBATIM before folding. Author = Opus (`df4e576`); reviewer = Fable (this session, independent). Tree at HEAD `df4e576`, working tree clean before and after review; `make check` green at HEAD (1730/1730 passed, 1 skipped, 7.6s, exit 0). Every file:line citation in r7 was re-verified against HEAD source; the checkbox census was re-swept independently from `grep -rn "FieldValue::Check" crates/btctax-forms/src/`.*

**Verdict.** r7 is very close: the r6 Important is folded in three of its four target locations, all seven r6 Minors/Nits are genuinely folded (including the "permanently refused" nit that survived three rounds), no r1–r6 fold has regressed, the build order walks clean with no red-ending step (the step-9/10 swap is done), the census survives my independent write-site sweep with one benign omission, and the doctrine sections are sound. But the class kept its streak — **eight for eight** — in the smallest possible square: r6 I-1's fix (1) said *"the §2.6 refusal detail … end[s] with"* the `--write-carryover` rebuild sentence, and r7 added that sentence to the block quote, step 11, and §4's bullet — **but the literal refusal-detail string in §2.6's code block, the one concrete text an implementor will copy, still ends at `income import`**, and the only named test that pins the refusal's text pins it to *"naming `income clear`"* only. §4's bullet ("The refusal names, in order: …`--write-carryover`") is therefore contradicted by the spec's own exemplar, and nothing red catches an implementor who ships the exemplar. A second, independent Important: the `serde_ignored` unknown-key refusal — a P9-added guard with a build step and an acceptance bullet — has **no named test and no named mutation**, which makes §4's "Every guard above is mutation-checked, and each mutation is named in §3.5" false as stated: the untested-guard pattern, surviving inside the acceptance section that claims to have closed it. **NOT GREEN — 0C / 2I / 2M / 2Nit.**

---

## Fold verification — every r6 finding

| r6 finding | folded? | evidence |
|---|---|---|
| I-1 (remedy unbricks the refusal but never names `--write-carryover`) | **◐ PARTIALLY — three of four fix locations** | Fix (2) ✓: the full-chain restoration test is named in §2.6 and §3.5 ("seed N, write-carryover onto N+1, stale both, run the whole remedy, assert N+1's carryover equals its pre-stale value"). Fix (3) ✓: §4's bullet reads "★★ AND THE REMEDY WORKS — INCLUDING CARRYOVER RESTORATION". Fix (4) ✓: §2.6 records the depth-1 rebuildability argument and ties it to the step 12(b) expiry. Fix (1) **half-applied**: step 11's LIMITATIONS entry names the full three-command chain, and the §2.6 block quote does too — **but the refusal-detail exemplar string itself (§2.6 code block, `StaleReturnInputs` comment) still ends at "then re-run `btctax income import`"** with no rebuild sentence. → **I-1 below.** |
| M-1 (steps 9/10 ordered so the classifier must classify class-(D) fields) | **✓** | Steps swapped: step 9 is now the deletion + `serde_ignored`, step 10 the classifier, with the r6 M-1 rationale recorded inline ("deleting first means the classifier never sees a field it may not classify"). |
| M-2 (four-vs-five count is fixture-contingent) | **✓** | §3.5 new paragraph (r6 M-2): the count is declared fixture-contingent, the `ForeignAccounts` fixture is required to supply a non-empty `foreign_country_names`, and the operative instruction (assert on the specific reason) is noted as robust either way. |
| Nit-1 ("permanently refused", third round) | **✓ — finally, on the fourth round** | §2.6's preserved-reasoning block now reads "would have **refused the dormant-HSA holder as unsupported**" — the false adverb is gone. |
| Nit-2 (mortgage question bricks fixtures too) | **✓** | Step 1: "★ And the mortgage question bricks fixtures too (r6 Nit-2): `testonly.rs:223/:526` and ~7 inline `return_1040.rs` fixtures…" — verified at HEAD: `testonly.rs:223` (`dec!(22000)`) and `:526` (golden builder) both carry nonzero `mortgage_interest_1098`. |
| Nit-3 (content-free $0 advisory) | **✓** | §2.7 table gained the `Some(false)` ∧ `== 0` row: no print, no box, no advisory — "the box-check and the advisory are BOTH scoped to the live predicate, never to the bare field value". |
| Nit-4 (`income show` refuses a stale row) | **✓** | Step 11 records it, credits it to the no-real-data premise, and adds it as "one more tooth" for the step 12(b) expiry. |
| Nit-5 (`HsaActivityUnsupported` is a rename of `HsaPresent`) | **✓** | §3.2 table: "★ r6 Nit-5: this is a **RENAME** of the shipped `RefuseReason::HsaPresent`, `return_refuse.rs:108/740`" — verified: variant at :108, refusal at :740, `hsa_present` read at :738. |

**r1–r5 regression spot-check — no silent regressions.** r5 I-2: §3.5 still asserts the mortgage `n` case *computes* (8a = 0, box checked, advisory fires); no compute-dependent trigger anywhere in r7 (§2.7 table, §3.2 strikethrough row, §3.5, step 6 — checked all four). r5 I-3: `DualStatusAlienUnsupported` still carried by step 8, the §3.5 value-refusal table, and §4's bullet. r5 M-1: both branched advisory texts intact in §3.4, `forgone_interest` still "up to". r4 I-1: the 8949 Box I/L row is present and true at HEAD (`fill8949.rs:87-93` write inside `fill_part` — fires whenever `data.rows` is non-empty; never Box C/F per `fill8949.rs:4-6`). r4 I-2: the mortgage refusal stays deleted. r3 I-5: the classifier guarantee is still stated at honest strength in §3.3 **and** §4. r3 I-7: both spouse prompts still gated on `spouse.is_some()`. r2 C-1: the dormant-HSA holder still computes (§2.4 `Some(false)` proceeds; §4 named regression test 1). r2 I-6: still no `deny_unknown_fields` anywhere in the workspace (re-grepped); `serde_ignored` v0.1.14 confirmed present in the offline cargo cache. §2.9 citations re-verified at HEAD: `return_1040.rs:1462-1466` (the circular predicate, verbatim as quoted), `:1473-1475` (`schedule_b_part3_unanswered`), `answer.rs:94-97` (gated liveness), `printed.rs:936/:943` (`unwrap_or(false)` ×2). P8a I1/I3 remain open at HEAD and subsumed by §3.1's `Mfj || spouse.is_some()` liveness and `HeaderError::Unanswered`. §2.3's dead-field claim re-verified: `box13_retirement_plan` and both `ssn_valid_for_employment` fields have zero consuming sites (declaration + the `fna` ignore-destructure only). §2.4's backstop re-verified: `"W"` is not in `INERT_BOX12_CODES` (`return_refuse.rs:28`); zero 1099-SA hits in the workspace. §2.8 re-verified: neither `W2.owner` (`return_inputs.rs:37`) nor `ScheduleCInputs.owner` (`:206`) carries `#[serde(default)]`.

---

## Findings

### I-1 — §2.6's refusal-detail exemplar still omits the `--write-carryover` rebuild step, contradicting §4's bullet, and no named test pins the refusal text past `income clear`

**What.** r6 I-1's fix (1) required *"the §2.6 refusal detail and step 11's LIMITATIONS entry"* to end with the rebuild sentence. r7 applied it to step 11, to the §2.6 block quote, and to §4's bullet — but the literal detail string in the §2.6 code block (the `CliError::StaleReturnInputs` comment) still reads *"Run `btctax income clear {year}` — which DISCARDS any carryover… — then re-run `btctax income import`."* Full stop. Its footnote addresses r5 I-1 ("★ NOT 'just re-import'") and nothing addresses r6 I-1. Meanwhile §4 asserts, as an acceptance bullet: *"The refusal names, in order: `income clear` …, `income import` …, **and, if the row carried a computed carryover, `report --tax-year <year−1> --write-carryover`**."* And the only named test touching the refusal's *text* is §2.6's *"`income import` over a stale row refuses (**naming `income clear`**)"* — it passes with the two-command string. The full-chain restoration test holds the *mechanism* (run all the commands, assert the carryover), not the *disclosure*; delete the rebuild sentence from the shipped refusal text and every named test stays green.

**Why it blocks.** This is r6 I-1's failure scenario, re-armed through the exemplar: an implementor at step 2 ("the stale-row refusal (§2.6)") copies the one ready-made string in the section, the text-test passes, §4's bullet is now false in shipped code, and a filer who follows the printed message verbatim ends with year+1 silently missing its `Computed` carryovers — the R3-M6 understatement whose avoidance is this very section's argument. The spec's own precedent (r5 I-2) graded exactly this shape Important: a stale concrete artifact that un-folds a fold, with no red test to stop it. Eight for eight — this time the recurrence is one un-updated string sitting six lines above the sentence that corrects it.

**Evidence.** SPEC §2.6 code block (`StaleReturnInputs` detail comment — ends at `income import`); §2.6 block quote + "The refusal detail must therefore name **all** commands"; §2.6 named tests ("naming `income clear`" — the only text assertion); §4's remedy bullet ("The refusal names, in order: … `--write-carryover`"); step 11 (correct, full chain).

**Fix (two sentences).** (1) Append to the exemplar string: *"…then, if this row carried a computed carryover, `btctax report --tax-year {year−1} --write-carryover` to rebuild it,"* and update its footnote to cite r6 I-1 beside r5 I-1. (2) Extend the §2.6 named text-test to assert the detail names all **three** commands (mutation: delete the rebuild clause from the detail ⇒ that test fails).

### I-2 — The `serde_ignored` unknown-key refusal has no named test and no named mutation, so §4's "every guard above is mutation-checked, and each mutation is named in §3.5" is false as stated

**What.** The unknown-key rejection is a P9-introduced refusal (§2.3, step 9, §4 bullet: *"It must reject `hsa_present` and each deleted field, naming them"*). It has a build step and an acceptance bullet — but §3.5's mutation inventory (registry loop, build refusal, dropped entry, three advisories, `SCHEMA_VERSION`, clear-first, full-chain restoration, plus the four value-refusals) never mentions it, and no test is named anywhere for it. §4 then claims *"Every guard above is mutation-checked, and each mutation is named in §3.5 — including the four value-refusals"* — untrue for this guard.

**Why it blocks.** This is the [[untested-guard-pattern]] / P8a I2 shape, surviving inside the acceptance section that cites it: revert `income import` from `serde_ignored::deserialize` back to bare `toml::from_str` (a one-line "simplification") and nothing fails — silently restoring the exact hole §2.3 exists to close (a faithfully transcribed W-2 `box13_retirement_plan = true` vanishing without a trace). r5 I-3 was graded Important for precisely this on `DualStatusAlienUnsupported`; the same standard applies. The guard is also load-bearing for §2.6: the stale-row policy assumes a re-imported TOML carrying `hsa_present` refuses loudly rather than silently dropping the key.

**Evidence.** SPEC §2.3 (mechanism, no test named); step 9 (build step, no test named); §3.5 mutation list (absent); §4 ("Every guard above is mutation-checked…" + "It must reject `hsa_present` and each deleted field, naming them" — asserted, unheld).

**Fix (two sentences).** Add to §3.5's mutation list and step 9: a named test imports a TOML carrying `hsa_present = false` and `box13_retirement_plan = true` and asserts the refusal names **both** paths; mutation — replace `serde_ignored::deserialize` with plain deserialization ⇒ that test fails.

## Minor

### M-1 — The census omits the 1040 filing-status checkbox, and §4's five-way category list omits the "filled from required input" class its own table uses

My independent sweep of `FieldValue::Check` writes found one write site with no census row: `form1040_full.rs:213-217` — the five-way filing-status box, checked on every filed 1040. It is benign: `ReturnInputs.filing_status` is the **one serde-required field** in the model (`return_inputs.rs:364`, no `#[serde(default)]` — a TOML without it refuses to parse), so no answer can ever be silently supplied; it belongs in the same "filled from input" class as Schedule A 5a/18 and Schedule C line F. But §2.1 claims the census is "closed by ENUMERATION, not by silence", §4 claims every printed box is "a registry question, a class-(B) advisory, a pen-deferred pair, a rule-3 exemption, or structurally inapplicable" — the filing-status box is **none of the five**, and it is nowhere in the table. Both prior census passes (r4's "COMPLETE", r6's "matches the census exactly — no box missing") missed it: the box-nobody-looked-at shape, third occurrence, this time with zero hazard. **Fix:** add the row (`1040 header: filing status ×5 | — | filled from the one serde-REQUIRED input; no default exists to launder`), and add "filled from an explicit input" to §4's category list.

### M-2 — The MFJ-with-no-spouse build refusal (r3 M-6) has an acceptance bullet but no named test, and the generic build mutation does not cover it

§4: "MFJ with no spouse identity ⇒ `ReturnHeader::build` REFUSES (r3 M-6 — decided, not deferred)"; step 5 implements it. §3.5's "delete the `build` refusal ⇒ a named test fails" mutation holds the `Unanswered(QuestionId)` loop — a distinct arm from the missing-spouse-identity check, which can be deleted independently while every named test stays green. Minor rather than Important because the failure is fail-loud downstream (a joint return with no spouse name/SSN cannot fill the 1040 header cells), not a silent wrong number. **Fix:** one line in §3.5 — named test: MFJ + `spouse: None` ⇒ `build` refuses; mutation: delete that arm ⇒ it fails.

## Nit

- **Nit-1** — §2.2 still says "**Both flips need the §2.6 migration**" — §2.6's title is now "THE MIGRATION — there isn't one". The sentence should read "the §2.6 schema-version refusal" (the discipline it names is right; the noun is stale, and this is the preserved-prose hazard that produced r6's Nit-1).
- **Nit-2** — The spec cites "`packet.rs`" for two different files without disambiguation: `crates/btctax-core/src/tax/packet.rs` (§2.1's `:200-216`, §2.5's `:325`, `:361-389`) and `crates/btctax-forms/src/packet.rs` (§2.1's `:129-134`). Both resolve correctly today; qualify the crate so the citations survive the next refactor.

---

## What I verified sound (so the fold does not re-litigate it)

**The r6 I-1 fold's substance**: the full-chain restoration test is correctly specified (seed N → write-carryover → stale both → whole remedy → assert equality; drop the rebuild step ⇒ it fails), the depth-1 rebuildability argument matches shipped code (`write_carryover` requires both rows, `cmd/tax.rs:426`; TY2024-only), and the unreachability of the `cmd/tax.rs:88-95` note after a `clear` is real (it lives inside `if let Some(existing)`). **§2.6 mechanics**: `income clear` → `return_inputs::delete` is a bare `DELETE` (`return_inputs.rs:118-122`) that never deserializes; the D-8 `version < 1` unlaunder slated for deletion exists exactly as cited (`return_inputs.rs:56-64`); the keyless mutation-check remains non-vacuous. **The census**: independently re-swept — 13 write sites (fill8949 ×1, form1040_full ×3, form8283 ×1, schedule_a ×1, schedule_b ×1, schedule_c ×1, schedule_d_full ×2; form1040.rs/schedule_d.rs are the non-packet short forms; pdf.rs is the enum writer); every one maps to a census row except the filing-status box (M-1); `PrintedForms` is genuinely fourteen forms (`packet.rs:361-389`); the QOF "No" (`schedule_d_full.rs:283-294`), Box I/L (`fill8949.rs:87-93`, never C/F), CTC/ODC deliberately-unchecked (`form1040_full.rs:377`), >4-dependents refusal (`:366-368`), presidential pair (`:328-338`), and the dual-status box printed from the MFS coupling alone (`core packet.rs:324-326`) all check out. **§2.9**: the circular predicate is verbatim at `return_1040.rs:1462-1466`; `foreign_trust` is genuinely absent from it; the shipped doc comment ("never a Schedule-B path") is the one §2.9 schedules for update; the red-test fixture ($100 interest, `foreign_accounts = None`) is red on `main` by inspection of `schedule_b_part3_unanswered`. **The build order**: all 12 steps walked; step 1's flips are compile-forced (`Person.blind: bool` at `:132`, `salt_use_sales_tax: bool` at `:288`, `hsa_present: bool` at `:321` — all still bare at HEAD, as the spec assumes), the 9↔10 swap is coherent (neither deletion nor `serde_ignored` depends on the classifier), and no step ends red. **The registry**: 8 `QuestionId` variants = 8 entries = the §3.1 liveness table. **§3.4**: `persons` formula mirrors `AgedBlindBoxes::for_return` (`core packet.rs:200-216` — joint-only spouse boxes, verified). **The three dead fields**: zero consumers, re-verified. `make check` green (1730/1730, 7.6s); working tree clean before and after.

**Bottom line.** The architecture, doctrine, census classification, migration policy, and both owner decisions all hold; every r6 finding was either folded or (in one case) three-quarters folded. What remains is the class's eighth recurrence — one exemplar string that never received the sentence three other sections got, plus one guard outside the mutation inventory — and both fixes are mechanical. Fold these four sentences and two test lines, keep everything else still, and r8 should be green.
