# IMPL-P4 Fable code review r2 — re-review after fold `018e199` (phase diff `b61d595..018e199`)

*(Persisted verbatim per STANDARD_WORKFLOW §2. Reviewer: Fable, independent of the opus author.)*

**VERDICT: GREEN 0C/0I** — Phase 4 closes. (1 new Nit recorded below, non-gating; P4.9 write-back remains open on its own already-agreed schedule, per r1's closing note.)

**Verified green surface:** `git diff 059ec2a..HEAD -- types.rs compute.rs se.rs` = **0 bytes** (frozen invariant holds through the fold); `cargo test -p btctax-core --lib` **226/0**; full workspace **81/81 suite-result lines, 0 failures** (all goldens unchanged); `cargo clippy --workspace --all-targets` **0 warnings**. Every hunk of the fold commit (all 9 files) was read and checked against its r1 finding.

## r1 fold verification — all confirmed correct

**C1 (CRITICAL) — folded correctly.** `tables.rs:177-183`: QSS moved to the `Single | HoH | Qss => dec!(200000)` arm; `niit_threshold` (tables.rs:200-206) **unchanged**, QSS still $250,000 — the doc comment now cites §1401(b)(2)(A)(iii) vs §1411(b)(1) and warns against "unifying" them. The new KAT `form_8959_qss_uses_200k_threshold_not_250k` is correct (Σbox5 $240,000 → 0.9% × $40,000 = $360.00, exactly the r1 failing scenario) and the `statutory_values_are_constant_across_years` pins assert all five SE-addl thresholds including the `Qss ≠ niit_threshold(Qss)` asymmetry. The frozen `se.rs:143` clamp now behaves per §1401(b)(2) for QSS via the unfrozen helper — this is the *intended* behavior change, no frozen bytes moved. **No existing test pinned the old value**: I grepped every `Qss` use in `crates/`; the only end-to-end QSS test (`kat_rate_engine.rs:511 qss_resolves_to_mfj_schedule`) has zero SE income and MAGI $110k — it exercises §1(h)/§1411 aliasing only, both genuinely MFJ-valued, so its "aliases MFJ" premise stays true.

**I1 — folded correctly, and the cancellation argument is sound.** amt.rs module doc + `recon/amt-worksheet-2024.txt` now list the FTC (and Form 8801) in the Exception and give the cancellation argument instead of the false "every other item is out-of-scope" claim. I verified the argument on the merits: Form 6251 line 9 = line 7 − line 8 (no floor), line 10 subtracts the regular FTC, line 11 = max(0, L9 − L10), so AMT > 0 ⇔ (TMT − AMTFTC) > (L16 − FTC) ⇔ TMT > L16 when AMTFTC = FTC — trivially exact algebra, and the flooring on line 11 doesn't disturb it. The premise AMTFTC = FTC genuinely holds on the screened space: the 2024 Form 6251 line-8 instructions direct a §904(j) elector to enter the regular-tax FTC as the AMTFTC, and this crate's input space (1099-INT box 6 / 1099-DIV box 7 payee-statement passive foreign tax, refused above $300/$600 as `ForeignTaxOverCeiling`) is exactly the §904(j)-qualified space. The KAT `ftc_cancels_from_the_amt_decision` pins the Next-test threshold correctly: Single, AGI 300,000 → line 11 = 300,000 − 85,700 = 214,300 ≤ 232,600 (no STOP), line 12 = 26% × 214,300 = 55,718; decision ≡ `55,718 > L16` with strict `>` matching the worksheet, checked at four L16 values including the boundary.

**I2 — folded correctly; every cent value re-derived by hand and right.** `deep02_example2_other_taxes_block_to_the_cent` (return_1040.rs): SE base = round_cents(60,000 × 0.9235) = **55,410.00**; ss = **0.00** (taxpayer's own box3 168,600 consumes the wage base — the Schedule C is correctly owned by the capped earner); medicare = 2.9% × 55,410 = **1,606.89** = Sch 2 L4 (0.9% unbundled); 8959 Part I = 0.9% × (280,000 − 250,000) = **270.00**; Part II line 11 = max(0, 250,000 − 280,000) = 0 → 0.9% × 55,410 = **498.69**; L18 = **768.69**; Part V = 4,240 − 1.45% × 280,000 (= 4,060) = **180.00**. All asserted through `assemble_absolute` with `box6_medicare_withheld` live for the first time — the missing discriminating fixture is now exactly the one shipped. (Internal consistency bonus: taxpayer box6 3,370 = 1.45%×220,000 + 0.9%×20,000, so the fixture is a self-consistent employer-withholding story.)

**I3 — folded correctly; the $1,250.00 is exact.** `section6_pref_over_ti_delta_overstates_and_does_not_reconcile`: absolute with/without both $0 (TI 45,400/40,400, capped pref entirely under the $47,025 0% breakpoint) ✓; frozen delta = [10%×5,000 = 500 ordinary] + [15%×(55,000−47,025) − 15%×(50,000−47,025) = 1,196.25 − 446.25 = 750] = **1,250.00** — my independent derivation matches the pinned value to the cent; asserts `delta > with − without` (overstates) + `assert_ne` ✓. `section6_medical_floor_delta_understates_and_does_not_reconcile`: with−without = tax(111,250)−tax(57,500) = 19,742.50−7,703 = 12,039.50 vs frozen delta (floor fixed at non-crypto AGI) = 18,842.50−7,703 = 11,139.50 → `with − without > delta` (understates, the anti-conservative channel) ✓ — both KATs compute **both** quantities as required. Both FOLLOWUP entries updated.

**I4 — folded correctly, no over-refusal.** `return_refuse.rs:473-489`: per-form `box1b > box1a` and `box5 > box1a` → `InconsistentDividendSubset` with a named-box payload; the KAT covers both refusals **and** the boundary-legitimate `box1b == box5 == box1a` (fully-qualified, all-REIT) → no refusal, since the screens use strict `>`. Per-form (not aggregate) is the stricter, correct reading of "form-guaranteed subset." Workspace green including all 23 report goldens and the qdcgt/QBI fixtures (which all use box1b ≤ box1a) — nothing newly broken.

**Minors/Nits — all folded as claimed.** M1: `AbsoluteReturn.deduction_is_itemized` stored at assembly, render uses it; the helper replicates `choose_deduction` exactly on the MFS §63(c)(6) coercion and the Auto tie-break (equality → standard). M2: amt.rs doc now carries the floored-L15 caveat **with** the conservativity argument. M4: the NOT-COMPUTABLE branch prints the identical header + `Profile source:` line. N2: `debug_assert!` in the `_` arm. N3: "At most one …". M3 + N1 recorded in FOLLOWUPS with explicit **P5** ownership.

## New-defect scan of the fold — clean

- **QSS at $200k through frozen se.rs**: intended and correct (§1401(b)(2)(A)(iii)); no test/golden/doc pinned the old behavior.
- **New MFJ `real_2024_table` schedule**: brackets 0/23,200/94,300/201,050/383,900/487,450/731,200 at 10/12/22/24/32/35/37% and §1(h) breakpoints max_zero 94,050 / max_fifteen 583,750 — all verified against Rev. Proc. 2023-34. Correct.
- **`itemized_was_chosen` vs `choose_deduction`**: consistent on every reachable-and-meaningful shape, with one degenerate residual — see Nit.
- No clippy or test regressions.

## Nit (new, non-gating — file to the ownerless residue or P5)

- **crates/btctax-core/src/tax/return_1040.rs:182-196 — `ForceItemize` with no Schedule A labels the $0 deduction "standard".** `choose_deduction` returns the itemized-arm `unwrap_or(0)` = $0 (pre-existing, r1-reviewed §63(e) behavior), but `itemized_was_chosen` returns `false`, so the dual report prints "Deduction (L12, standard): $0.00" while the standard deduction would be $14,600. Input shape is degenerate (force-itemize with nothing to itemize), isn't screened, amounts are unchanged — label-only, strictly narrower than r1 M1. Either label it "itemized" there or screen the shape as inconsistent input.

## Gate status

0 Critical / 0 Important / 1 Nit; full validation surface green. **Phase 4 is certified GREEN at `018e199`.** Outstanding on the phase per prior agreement (not findings of this review): P4.9 carryover write-back + `p2-r2-n4-pseudo-year-viewer-gap`; P5-owned items `p4-r1-m3-ctc-advisory-P5` and `p4-r1-n1-taxyearreport-struct` swept at P5 entry.
