# Fable — independent review of SPEC_form_questions.md (P9), round 8

*Persisted VERBATIM before folding. Author = Opus (`426a1ed`); reviewer = Fable (this session, independent). Tree at HEAD `426a1ed` (the r8 commit is HEAD; `426a1ed` touched only `design/`, so every source citation was verified at the artifact's own commit), working tree clean before and after review; `make check` green at HEAD (1730/1730 passed, 1 skipped, 7.8s, exit 0). The checkbox census was re-swept independently from `grep -rn "FieldValue::Check" crates/btctax-forms/src/` (13 write sites); ~30 file:line citations re-verified against source; the r7→r8 diff (137 lines) was read in full and each edit audited for a fresh recurrence of the class.*

**Verdict.** r8 is green, and here is why that is a verified conclusion and not fatigue: both r7 Importants are folded **in substance, at the artifact** — the §2.6 exemplar string (the one concrete text an implementor copies) now ends with the `report --tax-year {year−1} --write-carryover` rebuild sentence and carries the r6 I-1 footnote, and §3.5's new "TWO MORE guards" block gives both the refusal-text disclosure and the `serde_ignored` unknown-key rejection a named test *and* a named mutation, which makes §4's "every guard is mutation-checked, and each mutation is named in §3.5" true for the first time. Both r7 Minors and both Nits are genuinely folded (census row + §4 category; MFJ-no-spouse named test/mutation; the "migration" noun retired; all five `packet.rs` citations disambiguated). I hunted the ninth recurrence in its two favorite shapes — a fold that reached one of its two named locations, and a guard held by nothing — and what I found does not re-arm any failure scenario: the residue is one under-strength local test description (§2.6's "Named tests" line) and one missing build-step cross-reference (step 9), both of which are backstopped by the full-strength tests §3.5 now names, so no sequence of implementor choices consistent with the spec ships the r7 failure. The build order walks clean end to end (step 1's `blind`/`salt` reads are lawful class-(B) forgoing, not laundering; the shipped test at `return_refuse.rs:1350` survives the rename and holds the `HsaActivityUnsupported` arm; no step ends red). The census survives my independent sweep with every write site mapped, including the newly added filing-status row. Every §4 bullet is true as stated and checkable. The r1–r6 folds show no silent regression. **GREEN (0C/0I)** — exact count **0C / 0I / 1M / 3Nit**.

---

## Fold verification — every r7 finding

| r7 finding | folded? | evidence |
|---|---|---|
| **I-1** (§2.6 exemplar string ends at `income import`; no text-test past `income clear`) | **✓ in substance** | The `StaleReturnInputs` detail comment in §2.6's code block now reads *"…then, if this row carried a computed carryover, `btctax report --tax-year {year-1} --write-carryover` to rebuild it"*, with a second footnote (*"★ AND name the rebuild… r6 I-1"*). §3.5's new block names the text-test (*"asserts the detail names all three commands"*) and its mutation (*"drop the rebuild clause ⇒ that test fails"*). §4's remedy bullet is no longer contradicted by the exemplar. r7's failure scenario (implementor copies the exemplar, ships the two-command string, all green) is dead. **Residue → M-1 below**: r7's fix (2) said "extend the *§2.6* named text-test", and §2.6's local "Named tests" line still reads "(naming `income clear`)" — the strong test lives only in §3.5. Non-blocking (see M-1). |
| **I-2** (`serde_ignored` guard has no named test / mutation; §4's inventory claim false) | **✓ in substance** | §3.5: named test imports a TOML carrying `hsa_present = false` **and** `box13_retirement_plan = true`, asserts the refusal names both keys; mutation: replace `serde_ignored::deserialize` with plain `toml::from_str` ⇒ that test fails. §4's "each mutation is named in §3.5" is now true. **Residue → Nit-1**: r7's fix said "add to §3.5 *and step 9*"; step 9 still schedules the guard with no test language. Non-blocking. |
| **M-1** (filing-status box missing from census; §4 list missing the category) | **✓** | Census row added (`form1040_full.rs:213-217`, `filing_status` at `return_inputs.rs:364`) — I verified both at source: the write site is exactly there, and `filing_status` is the one field in `ReturnInputs` with no `#[serde(default)]`. §4's list gained "**filled from an explicit input** (filing status, Sch A 5a/18, Sch C line F)". |
| **M-2** (MFJ-no-spouse build refusal had no named test/mutation) | **✓** | §3.5: "delete the MFJ-with-no-spouse `build` refusal → a named test fails… it is a *distinct arm* from the `Unanswered(QuestionId)` loop… named test = MFJ ∧ `spouse: None` ⇒ `build` refuses." |
| **Nit-1** ("Both flips need the §2.6 migration") | **✓** | §2.2 now: "Both flips are covered by the §2.6 schema-version refusal… the noun 'migration' is retired — r7 Nit-1." |
| **Nit-2** (two `packet.rs` files cited without disambiguation) | **✓** | All five sites qualified: `btctax-core/.../tax/packet.rs` at §2.1 (×2), §2.5, §3.4; `btctax-forms/src/packet.rs` at the 8949 row. Verified each resolves: `AgedBlindBoxes::for_return` at core `packet.rs:200-216` (joint-only spouse filter, verbatim), the MFS coupling at `:322-326`, `PrintedForms` at `:361-389` (**fourteen** forms, counted), `fill_8949` dispatch at forms `packet.rs:127-136`. |

**r1–r6 regression spot-check — no silent regressions.** §2.9: the circular predicate is verbatim at `return_1040.rs:1462-1466` (`foreign_accounts == Some(true)` inside its own liveness input); `foreign_trust` genuinely absent from it; the doc comment "never a Schedule-B path" (scheduled for update) is at `:1461`; `schedule_b_part3_unanswered` at `:1473-1475`; `answer.rs:94-97` still gates both questions on `schedule_b_files` — the red-test fixture ($100 interest, `foreign_accounts = None`) is red on `main` by inspection. r5 I-1/r6 I-1 mechanics: `income import` opens with `return_inputs::get(s.conn(), year)?` at `cmd/tax.rs:63`; the `--write-carryover` note at `:88-95` is inside `if let Some(existing)` (unreachable after `clear`, as §2.6 says); `write_carryover` reads year N−1's inputs (`:412`) and requires year N+1's existing row (`:426`); `income clear` → `return_inputs::delete` is a bare `DELETE` (`return_inputs.rs:118-122`). The D-8 `version < 1` unlaunder slated for deletion exists exactly as cited (CLI `return_inputs.rs:56-64`) with its P8a tests. §2.3: `box13_retirement_plan` (core `return_inputs.rs:58`), both `ssn_valid_for_employment` (`:129`, `:143`) — zero consuming sites, re-grepped; no `deny_unknown_fields` anywhere; zero 1099-SA hits; `serde_ignored` v0.1.14 confirmed in the offline cargo cache. §2.2/§2.4 field lines: `blind: bool` at `:132`, `salt_use_sales_tax: bool` at `:288`, `hsa_present: bool` at `:321` — all still bare, as step 1 assumes; `"W"` absent from `INERT_BOX12_CODES` (`return_refuse.rs:28`); `HsaPresent` variant at `:108`, refusal read at `:738-740`, **and a shipped test asserts it** (`return_refuse.rs:1350`) — so §3.5's "compile-forced" claim for `HsaActivityUnsupported` is sound: the rename drags both the arm and its test. r4 I-1: Box I/L write at `fill8949.rs:89` fires whenever rows exist, never C/F (`:4-6`). QOF "No" unconditional at `schedule_d_full.rs:283-294`. CTC/ODC deliberately unchecked (`form1040_full.rs:377` comment verbatim), >4-dependents refusal (`:366`), presidential pair filled from input (`:331-338`). Schedule A writes exactly two checkboxes (`schedule_a.rs:90-97`: 5a + 18); Schedule C writes exactly one (line F, `schedule_c.rs:91-101`, from input); 8960 writes none. `printed.rs:936/:943` `unwrap_or(false)` ×2 still present (P8a I3's second site, closed by the build refusal). r3 I-7: both spouse prompts still gated; `set_date` silent-discard at `answer.rs:147-151`. r2 C-1: the dormant-HSA holder still computes (§2.4 `Some(false)` proceeds; §4 named regression test 1). r5 I-2: §3.5's mortgage assertion is still the compute-form (8a = 0, box checked, advisory fires) — no compute-dependent trigger anywhere. r6 Nit-2: `testonly.rs:223/:526` both carry nonzero `mortgage_interest_1098`, as step 1 says. Census sweep: 13 write sites, every one mapped to a census row (`form1040.rs:99` / `schedule_d.rs:114` are the non-packet crypto-slice short forms, outside the fourteen-form packet the census scopes — consistent with r4–r7; `pdf.rs:395` is the enum writer). P8a I1/I3 remain open at HEAD and are subsumed (§3.1's `Mfj || spouse.is_some()`; `HeaderError::Unanswered`). The build order was re-walked start to finish: step 1 compiles and ends green (the `blind`/`salt` consumer flips read `== Some(true)`, which is lawful class-(B) forgoing per §2, not laundering — the advisory arrives at step 7 with its own red test), and no later step ends red.

---

## Findings

### M-1 — §2.6's local "Named tests" line still describes the refusal-text test at pre-r7 strength ("naming `income clear`"), while §3.5 requires all three commands

**What.** §2.6's named-test list reads *"`income import` over a stale row **refuses** (naming `income clear`)"* — the r6-era strength. The full-strength version (detail names `income clear`, `income import`, **and** `report --write-carryover`, with the drop-the-rebuild-clause mutation) lives only in §3.5's new block. r7 I-1's fix (2) asked for the *§2.6* test to be extended; the fold added the §3.5 bullet instead.

**Why it does not block.** The failure scenario that made r7 I-1 Important is dead: the exemplar string is correct, §4's bullet is true of it, and a full-strength named test with a named mutation exists in the canonical test inventory. The residue is two descriptions of one obligation at different strengths — the exact duplicated-statement drift this spec indicts, and the pair has already drifted once (r7 I-1), so it should not be left standing — but no implementor path consistent with the spec (step 2 builds §2.6; §3.5 is the test inventory §4 binds) ships an unpinned disclosure.

**Evidence.** SPEC §2.6 "Named tests" paragraph ("(naming `income clear`)"); §3.5 "TWO MORE guards" first bullet (all three commands + mutation); r7 I-1 fix (2).

**Fix.** One clause: change §2.6's parenthetical to "(naming all **three** commands — the §3.5 text-test)".

### Nit-1 — Step 9 schedules the `serde_ignored` guard without naming its test

r7 I-2's fix said "add to §3.5's mutation list **and step 9**"; step 9 got the guard ("together or not at all; the rejection must name `hsa_present` too") but no test language, making it the only guard-adding step without any (steps 2, 4, 6, 8 all name theirs). The §3.5 bullet fully binds the guard, so nothing fails open — add "(its named test + mutation: §3.5)" to step 9.

### Nit-2 — The r7 review was persisted in the same commit as the r8 fold

`426a1ed` adds `reviews/P9-SPEC-fable-r7.md` *and* the fold together, unlike every prior round (`3ab9738`, `5ee9a3b`, `37a2d84` were separate persist commits before their folds). The workflow's "persist verbatim **before** folding" is unverifiable from history within a single commit. Content appears intact; restore the two-commit pattern for r8's own persist.

### Nit-3 — §4's category enumeration has no home for the class-(C) never-written single boxes

Schedule C line H and the Form 8960 elections are census-classed (C) ("lawful unchecked" / "no tax direction") but map onto none of §4's six categories (registry question / class-(B) advisory / pen-deferred pair / rule-3 exemption / filled from explicit input / structurally inapplicable) as literally enumerated. The census table itself enumerates them, so closure holds where it matters; add "a class-(C) lawful-blank box" to §4's list for exactness.

---

## What I verified sound (so the fold does not re-litigate it)

**The r7 I-1 fold's artifact**: the exemplar string's `{year-1}` is the correct rebuild target (`write_carryover` computes year N−1 and writes onto year N's row — `cmd/tax.rs:405-450`); the multi-year-stale case converges fail-loud (the rebuild's own read of year N−1 hits that row's `StaleReturnInputs`, which names its own remedy); the full-chain restoration test covers it. **The r7 I-2 fold**: the named fixture (`hsa_present = false` + `box13_retirement_plan = true`) exercises both the rename and a deleted field, and the mutation is the exact plausible "simplification". **The census**: independently re-swept, 13 write sites, all mapped; the filing-status row's claims verified at source (the one serde-required field). **The build order**: all 12 steps re-walked; step 1 green (class-(B) reads are lawful pre-advisory; `return_refuse.rs:1350` survives the rename), 9↔10 ordering coherent, no red-ending step. **The registry**: 8 variants = 8 entries = the §3.1 table. **§4**: every bullet checked against the spec's own content and the source; none overclaims (the classifier guarantee still stated at r3 I-5's honest strength). `make check` green (1730/1730, 7.8s); `git status` clean before and after.

**Bottom line.** Eight revisions re-armed the class; the ninth did not. Both r7 Importants reached the artifact this time — the exemplar string an implementor copies and the mutation inventory §4 binds — and the residue (one under-strength local test description, one missing cross-reference, two hygiene nits) re-arms nothing. **GREEN (0C/0I)** — 0C / 0I / 1M / 3Nit. The Minor and Nits are recorded for inline fix or FOLLOWUPS with owning phase P9; none holds the gate. The spec is ready for the implementation plan.
