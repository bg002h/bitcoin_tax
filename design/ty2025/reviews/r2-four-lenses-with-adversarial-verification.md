# TY2025 SPEC review r2 — four lenses with adversarial verification

**Method:** a 16-agent workflow — four independent review lenses (fold integrity · fresh Criticals in
r2's never-reviewed text · constants vs primary sources · still-hidden gaps, fresh eyes), each of whose
blocking findings was then handed to an independent verifier briefed to **REFUTE by default**.

**Result: 14 findings raised, 9 survived verification, 5 refuted** — 4 Critical, 3 Important, 1 Minor,
1 Nit.

**Author verification before folding:** the two cheapest decisive claims were re-checked directly —
Schedule 1-A really has **six** Parts (Part VI, line 38), and taxcalc's `AutoLoanInterestDed_ps` really
is `[100000, 200000, 100000, 100000, 200000]`, doubling QSS where the form says "if married filing
**jointly**" and where its own sibling parameters leave QSS undoubled. Both confirmed.

**Persisted VERBATIM before folding**, per `STANDARD_WORKFLOW.md` §2.

---

## Survived verification

### 1. Critical — [new-text]

**Claim.** D-8 concludes "Part IV rides taxcalc" without reading taxcalc's Part IV parameters — taxcalc gives QSS (MARS=5) a $200,000 phase-out threshold where Schedule 1-A line 26 says $100,000, so the one oracle D-8 leaves standing is itself wrong for a filing status §6.4 makes mandatory.

**Scenario.** QSS, MAGI $150,000, $10,000 of qualified passenger vehicle loan interest. Form: L24=10,000, L26=100,000, L27=50,000, L28=50, L29=50×$200=10,000, L30=$0. taxcalc: po_start=200,000, magi≤po_start ⇒ no phase-out ⇒ deduction $10,000. btctax computes the correct $0; §6.2 reports one unexpected disagreement whose only spec-sanctioned resolution is "OTS is disqualified on Part IV (D-8), taxcalc is the witness" — i.e. adopt the $10,000. Ironically OTS picks the threshold correctly here (`if (status == MARRIED_FILING_JOINTLY) 200000 else 100000`, taxsolve_US_1040_2025.c:1885-1888), so D-8's blanket disqualification of OTS's whole Part IV discards the only correct witness for the one cell taxcalc gets wrong.

**Fix.** Narrow D-8's disqualification to the three defective cells (L28 truncation, L29 rate, L29 suppression) rather than all of Part IV, and add a fourth named-and-sized defect on the taxcalc side: `AutoLoanInterestDed_ps[widow] = 200,000` vs the form's $100,000, disqualifying taxcalc for Part IV whenever filing status is QSS. State that with both oracles disqualified, QSS Part IV ships zero-oracle and must say so in the §6.2 census.

**Evidence.** design/ty2025/SPEC.md:147 "Part IV rides taxcalc plus this named disqualification"; .venv/lib/python3.12/site-packages/taxcalc/policy_current_law.json `AutoLoanInterestDed_ps` = {'year':2013,'MARS':'widow','value':200000.0}; f1040s1a--2025.pdf line 26 "Enter $100,000 ($200,000 if married filing jointly)"

**Verifier (tried to refute, could not).** CONFIRMED from primary sources; could not refute. (1) SPEC.md:147 does say "Part IV rides taxcalc plus this named disqualification", and D-3 (~line 90) frames Part IV as "one-oracle-plus-named-disqualification". (2) f1040s1a--2025.pdf text layer, line 26: "Enter $100,000 ($200,000 if married filing jointly)" — QSS is a distinct 1040 status, so $100,000. (3) taxcalc policy_current_law.json AutoLoanInterestDed_ps widow = 200000.0, consumed at calcfunctions.py:1111 as AutoLoanInterestDed_ps[MARS-1] with no other status gate in the Part IV block, so MARS=5 gets $200,000; the scenario resolves exactly as claimed (magi 150,000 not > 200,000 => no phase-out => $10,000 vs the form's $0). (4) It is an isolated outlier inside taxcalc's own Schedule 1-A family — TipIncomeDed_ps widow=150,000, OvertimeIncomeDed_ps widow=150,000, OvertimeIncomeDed_c widow=12,500, SeniorDed_ps widow=75,000 all follow "only mjoint doubles" — so it is a taxcalc defect, not a defensible reading. (5) OTS taxsolve_US_1040_2025.c:1883-1886 is `if (status == MARRIED_FILING_JOINTLY) 200000 else 100000`, i.e. correct for WIDOW, as the finding says. (6) The cell is mandatory, not hypothetical: btctax has first-class Qss (btctax-core/src/tax/types.rs:7) and SPEC 6.4 requires Schedule 1-A reconcile "across all five filing statuses"; the r1-7 harness bug (gen_goldens.py:110 maps every non-MFJ to MARS=1) currently masks it, and the 5.3-mandated "real MARS map" fix is what exposes it.

Not covered elsewhere in the spec: D-6's named taxcalc limitation is the MAGI add-back proxy (c00100), a different mechanism that does not fire with zero 2a-2d amounts; 5.2's table writes Part IV as "100,000 / 200,000" in the same unmarried/MFJ shorthand as Parts II/III and never distinguishes QSS. Not an r1 restatement (r1-7 is the harness MARS map; r1-4 is the MFS bar).

Two secondary points the finding is also right about: (a) D-8's blanket disqualification of OTS across all of Part IV is not "computed from the defect's own mechanism" as 6.2 requires — both OTS defects (the $300 rate and the int truncation) live downstream of line 27, so whenever MAGI <= threshold L27=0 => L29=0 and OTS's line 30 is exact; the blanket bar discards the correct witness precisely where taxcalc is wrong. (b) The consequence is worse than one bad witness: at QSS / MAGI $110,000 / $10,000 interest the form gives $8,000, OTS gives $7,000 (truncation + $300 rate), taxcalc gives $10,000 (no phase-out at all) — ZERO valid witnesses, against a 6.2 green definition of "0 unexpected".

Two overstatements in the finding that do NOT rescue the spec: the scenario's "only spec-sanctioned resolution is adopt the $10,000" is too strong — 1's guardrail "A disagreement is adjudicated against the FORM, never encoded" would keep btctax at $0 — but that guardrail resolves the NUMBER, not the COVERAGE claim, and the coverage claim (Part IV ships on one sound oracle plus one named disqualification) is false for QSS with no sized taxcalc disqualification anywhere in the document. Also "without reading taxcalc's Part IV parameters" is loose: D-8 did cite AutoLoanInterestDed_po_rate_per_step = 0.2; it did not read _ps's per-MARS values, which is the load-bearing part.

---

### 2. Critical — [new-text]

**Claim.** §5.1 makes the §164(b)(6) SALT phase-down MAGI-driven and D-6 defines the MAGI surface, but nothing connects them — the add-backs are scoped as Schedule 1-A inputs, so a filer with no Schedule 1-A deduction silently gets AGI as MAGI, and neither oracle can catch it because both are blind on exactly that leg.

**Scenario.** Single filer, AGI $450,000, $130,000 excluded under §911 (Form 2555 line 45), SALT paid $30,000, no tips/overtime/car loan/senior deduction so Schedule 1-A is entirely inapplicable and lines 2a–2d are never collected. True MAGI = $580,000 ⇒ cap = max(40,000 − 0.30×80,000, 10,000) = $16,000. With AGI as the proxy: $450,000 < $500,000 ⇒ no phase-down ⇒ Schedule A line 5e = $30,000, a $14,000 overstated itemized deduction on a signed return, with no refusal fired. taxcalc's SALT phase-down keys off `posagi` (c00100) and OTS's worksheet hardcodes `ws[3] = 0.0` under the printed disclaimer "Assuming no Puerto Rico income, Form 2555 or 4563 entries" — so both oracles reproduce the same wrong number and the suite is green.

**Fix.** State in §5.1 that §164(b)(6) MAGI is the *same* quantity as Schedule 1-A line 3 (both are AGI + §911/931/933 exclusions), that the 2a–2d Option surface is therefore collected whenever Schedule A is in play and not only when Schedule 1-A is, and that `None` refuses at the SALT site too. Add to §6.2 that the add-back leg of the SALT phase-down is zero-oracle and must be witnessed by a KAT against the worksheet, not by reconciliation.

**Evidence.** design/ty2025/SPEC.md:174-180 ("reduced by 30% of MAGI over $500,000", "MAGI threshold") vs :121-125 (D-6 scopes the surface to Schedule 1-A Part I); taxcalc calcfunctions.py:1367-1372 (`salt_cap` phase-down driven by `posagi`); taxsolve_US_1040_2025.c:2470-2471

**Verifier (tried to refute, could not).** CONFIRMED, not refuted — every leg verified against primary sources. (1) Statute: 26 U.S.C. §164(b)(7)(B)(i) reduces the applicable limitation amount by "30 percent of the excess … of the taxpayer's modified adjusted gross income over the threshold amount", (iii) floors it at $10,000, and (iv) defines MAGI as "adjusted gross income increased by any amount excluded from gross income under section 911, 931, or 933" — i.e. exactly Schedule 1-A Part I lines 2a-2d's add-back set (verified against the archived f1040s1a--2025.pdf text layer: 2a PR income, 2b/2c Form 2555 lines 45/50, 2d Form 4563 line 15). So the SALT phase-down is MAGI-driven and MAGI != AGI for precisely that population. (2) Both oracles blind, as claimed: taxcalc 6.7.2 calcfunctions.py ItemDed uses `posagi = max(c00100, 0.)` and `if posagi > salt_ps: salt_cap = max(0., salt_cap - (posagi - salt_ps) * ID_AllTaxes_c_po_rate)`; OTS taxsolve_US_1040_2025.c:2469-2473 prints "Sched-A State and Local Tax Deduction Worksheet -- Assuming no Puerto Rico income, Form 2555 or 4563 entries." then sets `ws[3] = 0.0` with `ws[4] = ws[2] + ws[3]`, ws[2]=L[11] — and it is worse than the finding says: the worksheet is only entered when `L[11] > 500000.0` (:2460-2461), so the scenario's filer (AGI 450k) never enters it at all. (3) Spec gap is textually real: :174-180 names the SALT "MAGI threshold" as a constant only; D-6 (:121-125) scopes the collected surface to "Schedule 1-A Part I … drives all four phase-outs" (the four Sch 1-A parts per the §5.2 table :209-213) and §5.2's input surface (:221-223) files the add-backs under Schedule 1-A. A grep of every magi/2555/4563/puerto occurrence in the file finds no text making Schedule A line 5e consume the same quantity, and no rule forcing MAGI collection when Schedule 1-A is inapplicable. (4) Not a restatement of a folded r1 finding: r1-3's scenario was Part V overpaying a 65+ filer and its fold is what created the Schedule 1-A-scoped surface this finding says leaves SALT dangling; r1-1 asked for the salt sub-structure and never addressed what feeds it. (5) Scenario is reachable in this repo: no §911/Form 2555 surface exists anywhere (grep over crates/ and scripts/ finds only FTC boxes and Sch B 7a/8 foreign questions), and return_1040.rs:322-327 applies salt_cap as a bare min with no MAGI input. Arithmetic checks: MAGI 580,000 → 40,000 − 0.30×80,000 = 16,000 (above the 10,000 floor) vs 40,000 under the AGI proxy → Schedule A line 5e = 30,000 instead of 16,000, a $14,000 overstatement. §6.5's required "golden household above the SALT phase-down threshold" is built against two AGI-driven oracles, so it would cement the AGI proxy rather than catch it. Minimal fold: state MAGI as a return-level quantity (§164(b)(7)(B)(iv)) consumed by BOTH Schedule 1-A Part I line 3 and Schedule A line 5e, collected unconditionally under D-5 (None refuses even when Schedule 1-A is inapplicable), and add the both-oracles-blind SALT-MAGI leg to §6.2's named-and-sized disqualification list plus a golden household with MAGI > AGI.

---

### 3. Critical — [still-hidden]

**Claim.** §5.1's SALT sub-structure is keyed to MAGI but D-6 scopes the collected MAGI add-backs to Schedule 1-A's "all four phase-outs", and NEITHER oracle uses MAGI for §164(b)(6) — taxcalc uses `posagi`, OTS hardcodes the add-back line to 0 — so wiring the SALT phase-down to AGI is invisible to both.

**Scenario.** Itemizing filer with $130,000 of Form 2555 excluded foreign earned income, AGI $420,000 (MAGI $550,000), $45,000 of state tax. Form: MAGI over $500,000 by $50,000 → cap 40,000 − 15,000 = $25,000 on Schedule A line 5e. btctax wired to AGI: no phase-down at all → $40,000. $15,000 of over-deducted SALT, and it also under-adds-back on Form 6251 line 2a. taxcalc agrees with the wrong answer (its phase-down reads `posagi`); OTS agrees too (its worksheet line 3 is literally `ws[3] = 0.0` under the comment "Assuming no Puerto Rico income, Form 2555 or 4563 entries"). §6.5's required SALT household passes either way.

**Fix.** In §5.1, state that the SALT phase-down consumes the SAME D-6 collected MAGI (Sch 1-A line 3 = AGI + 2a–2d), not AGI, and add to §6.2/§9 that BOTH oracles are disqualified on the SALT MAGI add-backs (taxcalc: `posagi`; OTS: ws[3] hardcoded 0), sized as 30% of the excluded amount up to the $30,000 cap-to-floor spread. Add a §6 requirement that the SALT household have MAGI ≠ AGI, or state that btctax refuses a Form 2555/4563/PR filer outright.

**Evidence.** taxcalc calcfunctions.py:1368-1369 `salt_ps = ID_AllTaxes_c_ps[MARS - 1]` / `if posagi > salt_ps:`; ~/OpenTaxSolver2025_23.06_linux64/src/taxsolve_US_1040_2025.c:2470-2471; `grep -rn "2555|4563|PuertoRico" crates/btctax-core/src` returns nothing — btctax has no surface and no refusal for excluded income today

**Verifier (tried to refute, could not).** HOLDS (with one correction to the scenario's AMT clause).

Every load-bearing fact checks out against primary sources:

1. **taxcalc uses AGI, not MAGI.** `/scratch/code/bitcoin_tax/.venv/lib/python3.12/site-packages/taxcalc/calcfunctions.py:1367-1373` is verbatim as cited: `salt_cap = ID_AllTaxes_c[MARS-1]` / `salt_ps = ID_AllTaxes_c_ps[MARS-1]` / `if posagi > salt_ps:` / `salt_excess_agi = posagi - salt_ps`. The variable is literally named `salt_excess_agi`; `posagi` carries no §911/§931/§933 add-back.

2. **OTS 2025 hardcodes the add-back line to zero.** `~/OpenTaxSolver2025_23.06_linux64/src/taxsolve_US_1040_2025.c` ~2460-2500: the "Sched-A State and Local Tax Deduction (STTD) Worksheet" sets `ws[2] = L[11]` (AGI), prints `"...Assuming no Puerto Rico income, Form 2555 or 4563 entries."`, then `ws[3] = 0.0; ws[4] = ws[2] + ws[3];`. So OTS has the MAGI line *structurally* — corroborating that the real worksheet's MAGI = AGI + §911/§931/§933 exclusions, the same add-backs as Schedule 1-A lines 2a–2d — but can never populate it. Both oracles are therefore blind to the MAGI-vs-AGI input of §164(b)(6); this is a **zero-oracle** region, not a one-oracle one.

3. **D-6 is scoped to Schedule 1-A only.** SPEC.md:121-125 — "That difference drives **all four** phase-outs", and the phrase traces straight from r1-3 (reviews/r1…md finding 3: "the MAGI that drives *all four* phase-outs"), where "four" = Sch 1-A Parts II/III/IV/V. §5.2's input-surface bullet likewise files "MAGI add-backs 2a–2d" under Schedule 1-A. §5.1's SALT sub-structure (SPEC.md:174-181) is keyed to a "MAGI threshold" with no statement that its input is that same collected surface. SALT is an unenumerated fifth consumer.

4. **The test plan cannot catch it.** §6.2's "computed and sized disqualification" rule is extended to "Schedule 1-A as well as Form 6251" — SALT is not covered. §6.5's required household is specified only as "above the §164(b)(6) SALT phase-down threshold"; with no add-backs (the corpus has none) an AGI-wired and a MAGI-wired btctax give the same number and both oracles agree. The inverse is the worse hazard the spec doesn't anticipate: a *correctly* MAGI-wired btctax on an add-back household diverges from **both** oracles, and the cheapest route to green is to "fix" btctax to match — the exact failure mode §1's "adjudicated against the FORM" guardrail exists for, but with no named disqualification to hang it on.

5. **Not a restatement of r1.** r1-1 was "`salt_cap` is a scalar and no harness runs SALT at 2025 constants" (folded into §5.1's sub-structure + §6.5). r1-3 was "collect Sch 1-A lines 2a–2d". Neither says the SALT phase-down's MAGI input must come from that collected surface, nor that no oracle witnesses the wiring. This is the intersection the fold left open.

6. **The scenario is reachable.** `grep -rnE "2555|4563|PuertoRico" crates/btctax-core/src` returns nothing (only `other_taxes.rs:213,277` and `return_1040.rs:722` comments saying "MAGI = AGI (fail-closed, no §911/CFC/PFIC add-backs)"), and `salt_cap` is consumed as a bare `min` at `crates/btctax-core/src/tax/printed.rs:1145`. Post-spec the surface *does* exist (§5.2 collects 2a–2d, `None` refuses), so a filer can enter $130,000 of Form 2555 exclusion for Schedule 1-A while SALT keeps reading AGI. Arithmetic checks: MAGI 550,000 − 500,000 = 50,000; ×30% = 15,000; 40,000 − 15,000 = 25,000 (above the 10,000 floor) vs. 40,000 with no phase-down at AGI 420,000 → $15,000 over-deducted on Sch A line 5e.

**One correction, which does not save the finding:** the scenario's tail claim that it "also under-adds-back on Form 6251 line 2a" is wrong in both direction and effect. Line 2a adds back Schedule A line 7 in full, so an overstated SALT deduction produces an equally *over*stated add-back and AMTI is unchanged — AMT is insensitive to this defect. The regular-tax $15,000 over-deduction is the whole of the harm.

**Also fair to the author:** §5.1 does say the reduction is "30% of MAGI over $500,000" and names the field a "MAGI threshold", so the spec never affirmatively directs the implementer to AGI. The gap is (i) D-6's enumeration of MAGI consumers stops at Schedule 1-A's four phase-outs, so nothing binds the collected surface to §164(b)(6), and (ii) §6 has no test and no named zero-oracle disqualification for that wiring — a passing suite hides both the AGI slip and the "encode the oracles' answer" fix.

Files: /scratch/code/bitcoin_tax/design/ty2025/SPEC.md (§5.1 L174-181, D-6 L121-125, §6.2/§6.5 L282-291), /scratch/code/bitcoin_tax/design/ty2025/reviews/r1-adversarial-would-executing-this-spec-produce-a-correct-return.md (findings 1 and 3).

---

### 4. Critical — [still-hidden]

**Claim.** §6 requires a constants test for exactly one TY2025 field (`std_deduction`), and nothing in §5 or §6 covers the non-MFS `AmtParams` cells — the §55(d)(3) identities constrain only the five MFS constants, §5.5 scopes the TY2025 vector fixture to MFS, and the corpus has 0/104 AMT-owing households.

**Scenario.** The TY2025 `AmtParams` literal is typed with `exemption_mfj_qss: dec!(133300)` — the 2024 value carried forward, the single most likely slip when copying a struct literal. `mfs_kicker_constants_satisfy_the_two_section_55d3_identities` only asserts `mfs_kicker_max == exemption_mfs` and `mfs_kicker_start == phaseout_start_mfs + exemption_mfs/rate`, so it stays green. §5.5's fixture is MFS-only, so no vector touches the MFJ exemption. The corpus has no AMT household. An MFJ filer with AMTI $1,300,000 gets an exemption $3,700 too small and a Form 6251 line 11 about $1,000 too high, on a signed return, with five green gates. Same exposure for `exemption_single_hoh`, `phaseout_start_mfj_qss`, `breakpoint_28pct` (239,100) and `rate_28_subtrahend` (4,782).

**Fix.** Add to §6 a numbered requirement: "a TY2025 analogue of `ty2024_full_return_params_bundled` asserting EVERY `FullReturnParams` and `AmtParams` cell against its §5.1 citation" — the std_deduction test (§6.6) is one field of ~20. Alternatively require the TY2025 vector fixture to span all four statuses (see the census finding), which covers the AMT cells by oracle instead.

**Evidence.** crates/btctax-adapters/src/tax_tables.rs:798-832 (the identity test asserts only the two MFS relations); tax_tables.rs:744-773 (`ty2024_full_return_params_bundled` is the only per-cell AMT assertion and is 2024-only); SPEC §6 lists no AmtParams test

**Verifier (tried to refute, could not).** CONFIRMED (with one clause of the scenario corrected). Every load-bearing element checks out against the repo, and the finding is not an r1 restatement.

1. The cited evidence says what is claimed. `mfs_kicker_constants_satisfy_the_two_section_55d3_identities` (/scratch/code/bitcoin_tax/crates/btctax-adapters/src/tax_tables.rs:798-832) asserts exactly three things: `mfs_kicker_max == exemption(Mfs)`, `exemption_phaseout_rate > 0`, and `mfs_kicker_start == phaseout_start(Mfs) + exemption_mfs/rate`. It touches only `exemption_mfs`, `phaseout_start_single_hoh_mfs`, `exemption_phaseout_rate`, `mfs_kicker_max`, `mfs_kicker_start`. `exemption_mfj_qss`, `exemption_single_hoh`, `phaseout_start_mfj_qss`, `breakpoint_28pct`, `breakpoint_28pct_mfs`, `rate_28_subtrahend(_mfs)` and the line-19/25 bands are unconstrained by it. `ty2024_full_return_params_bundled` (tax_tables.rs:744-773) is indeed the only per-cell AMT assertion and is 2024-only (it also asserts `full_return_for(2025).is_none()`, the line D-2 tells the implementer to delete).

2. The spec gap is real. §6 names one required constants test — item 6, `std_deduction` (SPEC.md:291). §6.7's "conformance is a test" is illustrated with Schedule 1-A line presence and doc-comment text, i.e. form transcription, not bundled constants. §5.1's own star note (SPEC.md:201-203) claims automatic coverage only for "a slip in any of five MFS constants" — sitting directly under the full non-MFS table, which reads as reassurance the table is protected when it is not.

3. No other gate catches it, and one of the finding's own assumptions is understated in its favour. The golden corpus cannot: gen_goldens.py:262-267 *rejects* any candidate where taxcalc reports `c09600` AMT ("applies to EVERYONE, anchors included"), and sweep.py:365-370 skips them in the live sweep — so 104/104 households are AMT-free by construction, and §6.5's newly-required TY2025 SALT household will be too. The witness census cannot: verify_f6251.py:290,298 iterates `{v["inputs"]["filing_status"] for v in vectors}`, i.e. statuses *present* in the fixture, so §5.5's MFS-only TY2025 vector set clears §6.3 vacuously. And stronger than the finding states: even a full-status TY2025 fixture would not cover the bundled cells, because the vector KAT's `params()` (form6251.rs:437-455) is a test-local `AmtParams` literal, not `full_return_for(year).amt` — and §5.3 tells the implementer to make that same local `params()` select per year, reproducing the independence for 2025.

4. Not an r1 restatement. r1's ten findings (design/ty2025/reviews/r1-...md) are salt_cap structure, std_deduction source, MAGI 2a-2d, MFS eligibility, OTS Part IV, Form 1040 shape, gen_goldens seams, Sch 1-A instructions, taxcalc `exact=1`, valid-SSN. None raises AmtParams cell coverage.

CORRECTION to the scenario, which the fold should reflect rather than reproduce: "on a signed return" is wrong for the stated 2024-carried-forward direction. §4 keeps the Tier-1 refusal, and return_1040.rs:1505-1513 refuses with `RefuseReason::AmtScreenTriggered` whenever `must_attach()` (`line7 > line10`, form6251.rs:207-209) fires — which any AMT-owing filer trips. Every 2024→2025 carry-forward slip is *stingier* (85,700<88,100, 133,300<137,000, 609,350<626,350, 1,218,700<1,252,700, 232,600<239,100, subtrahend 4,652<4,782), so it raises line 7 and over-refuses. The real exposures are: (a) the opposite-direction slip (digit transposition, or copying the 2026 draft) lowers line 7 below line 10 and silently *files* a return that owed AMT — a fail-open understatement; (b) the wrong worksheet is still computed, rendered (render.rs:1528-1539 prints lines 4/5/9/10/11) and emitted by the oracle harness (btctax-oracle-harness/src/main.rs:302); and (c) these constants become filing-critical at Tier-2 E4, which is the stated purpose of this project, with no gate ever having asserted them. Suggested fix is unchanged in substance: add a numbered §6 requirement for a per-cell TY2025 `AmtParams` assertion against the §5.1 table (the 2024 twin at tax_tables.rs:762-769, extended to the mfj/qss and single/hoh cells, the MFS breakpoint/subtrahend, and lines 19/25).

---

### 5. Important — [fold-integrity]

**Claim.** r1-1 only partially closed: the fold replaced a one-cell `salt_cap` scalar with a 4-parameter × 5-status structure but added exactly one test requirement — "a household above the threshold" — which witnesses one point of that structure and never the floor or any MFS-halved cell.

**Scenario.** MFS itemizer, MAGI $300,000, $25,000 of state and local tax. Correct: $20,000 cap − 0.30 × $50,000 = $5,000 (the MFS floor). An implementer who carries the general $500,000 threshold into the MFS cell — the statute halves it, and §5.1 lists the halved values only in prose — finds MAGI under the threshold and deducts $20,000, $15,000 too much. §6.5's single household above $500,000 (naturally built as Single, like every other corpus SALT cell) passes, and so do all five gates.

**Fix.** Dimension §6.5 the way §6.4 dimensions Schedule 1-A: the SALT structure reconciles per filing status, with at least one household in the floor region (MAGI ≥ $600,000; ≥ $300,000 MFS). Also name scripts/oracle/corpus.py in §5.3's year-seam list — its `SALT["over"]` = $8,000 + $9,000 = $17,000 is below the 2025 $40,000 cap, so the axis that exists to test the cap becomes a no-op at TY2025 constants.

**Evidence.** SPEC.md:174-180 (per-status cap / phase-down rate / MAGI threshold / floor) against SPEC.md:288-290 (the sole requirement). taxcalc 2025: `ID_AllTaxes_c_ps` = [500000, 500000, 250000, 500000, 500000], `ID_AllTaxes_c_po_floor` = [10000, 10000, 5000, 10000, 10000]. scripts/oracle/corpus.py:97-100 `SALT = {"under": ... , "over": {8_000, 9_000}}  # 5d = 17,000 → capped to $10k`.

**Verifier (tried to refute, could not).** The finding holds; every cited fact checks out and no other part of the spec closes the gap.

VERIFIED AGAINST THE REPO
1. The structure. SPEC.md:174-180 does replace `salt_cap: Usd` with a per-status four-parameter sub-structure (cap / phase-down rate / MAGI threshold / floor), stating the MFS halves only inline in prose ("$40,000 cap ($20,000 MFS) ... over $500,000 ($250,000 MFS) ... floored at $10,000 ($5,000 MFS)").
2. The sole test requirement. SPEC.md:288-290 (§6.5) reads, in full: "A TY2025 golden household above the §164(b)(6) SALT phase-down threshold (r1-1)." One household, "above the threshold" — which is satisfied by any point past the knee. It pins neither the floor knee (which needs MAGI ≥ $600,000 general / $300,000 MFS, since 0.30 × $100,000 = the full $30,000 reduction) nor any MFS cell.
3. The MFS column is unreachable by that harness *by construction*. scripts/oracle/corpus.py:44 — "Domain: filing status ∈ {Single, MFJ} only (MFS deferred)" — and corpus.py:66-67, `STATUS = ["Single", "Married/Joint"]`. §6.5 says the corpus "is the only harness that exercises SALT", so no household added under §6.5 can be MFS. The finding's "naturally built as Single" is if anything understated: MFS is not in the domain at all.
4. No other §6 clause reaches it. §6.4's "across all five filing statuses ... the MFS bar" is scoped to **Schedule 1-A**, which I confirmed from the archived text layer (design/amt-form6251/f1040s1a--2025.pdf) is "Additional Deductions" — Part I MAGI, Part II tips, Part III overtime — not Schedule A line 5e. §6.3 is the AMT witness census, and scripts/oracle/verify_f6251.py carries no `state_income_tax`/`real_estate_tax` inputs at all, so the MFS AMT vectors never touch SALT.
5. Evidence citations are exact. corpus.py:97-100 is verbatim as quoted (SALT "over" = 8,000 + 9,000, comment "5d = 17,000 → capped to $10k"). taxcalc 2025 via .venv/bin/python: `ID_AllTaxes_c` [40000, 40000, 20000, 40000, 40000], `ID_AllTaxes_c_ps` [500000, 500000, 250000, 500000, 500000], `ID_AllTaxes_c_po_rate` 0.3, `ID_AllTaxes_c_po_floor` [10000, 10000, 5000, 10000, 10000] — all four as cited.
6. The scenario can occur and the arithmetic is right. MFS is a live status in the engine today (crates/btctax-core/src/tax/return_1040.rs:322-325 already special-cases `FilingStatus::Mfs` by halving the cap), so a per-status rewrite is exactly where a mis-typed threshold lands. MFS/$300,000 MAGI/$25,000 SALT: correct = max($20,000 − 0.30 × $50,000, $5,000) = $5,000; with the general $500,000 threshold in the MFS cell = $20,000. $15,000 over-deducted, and nothing in the five gates or either oracle sweep reds.

NOT A RESTATEMENT. r1-1 was "salt_cap cannot express the phase-down"; this is whether the fold closed it. The fold did two things — added the structure, added §6.5 — and §6.5 witnesses one of twenty cells at one of two knees.

PARTIAL MITIGATIONS, STATED HONESTLY (they lower severity, they do not refute). SPEC.md:177-180 does require the rate/threshold/floor to "be transcribed from the 2025 Schedule A instructions before use", so the MFS constants are not missing from the spec, and §6.8's generic "Mutation-verified guards" would surface the blind spot *if* an implementer chose to mutate the MFS cells. But neither is a test requirement, and §6.5's own closing sentence — "A numbered requirement, not an open question" — is the fold declaring that SALT coverage specifically must not be left to implementer discretion. The natural repair is one clause: require the phase-down knee, the floor knee, and an MFS cell, and note that the corpus's Single/MFJ domain means the MFS case needs a home outside it.

---

### 6. Important — [new-text]

**Claim.** §5.1 asserts the SALT phase-down rate/threshold/floor are "corroborated by taxcalc only" — OTS 2025 implements them too, and is defective for MFS in two ways that §6.5's mandated household can land on with no named or sized disqualification.

**Scenario.** MFS, AGI $400,000, SALT $25,000. Statute/taxcalc: cap = max(20,000 − 0.30×150,000, 5,000) = $5,000. OTS: the worksheet entry gate tests `L[11] > 500000.0` even on the MFS branch, so the worksheet is never entered and `localtax[5] = min(25,000, 20,000) = $20,000` — a $15,000 split. Second defect: inside the worksheet OTS applies the 30% to the un-halved $40,000 base against an already-halved $250,000 threshold and then halves the result (`ws[10] = ws[9]/2.0`), yielding an effective 15% rate for MFS. Combined, OTS gives MFS a cliff (full $20,000 below AGI $500,000, flat $5,000 above) instead of the statute's $20,000→$5,000 ramp across AGI $250,000–$300,000. §6.5 requires a TY2025 household above the phase-down threshold and §6.4 requires all five statuses; the MFS one reds with nothing in the spec to explain it.

**Fix.** Add a named, sized disqualification for OTS 2025's SALT worksheet on MFS (mechanism: `L[11] > 500000` gate + post-hoc `ws[9]/2`; scope: MFS with MAGI in ($250,000, $500,000] fully wrong, and MAGI in ($250,000, $300,000) rate-wrong), and correct §5.1 to say OTS does implement the phase-down and corroborates it for non-MFS statuses. Pin §6.5's household's filing status explicitly.

**Evidence.** taxsolve_US_1040_2025.c:2462-2463 (`L[11] > 500000.0` on both legs), :2466 (`ws[1] = 40000.0` un-halved), :2476 (`ws[5] = 250000.0` halved), :2494 (`ws[10] = SmallerOf( ws[9]/2.0, localtax[4] )`); design/ty2025/SPEC.md:178-180

**Verifier (tried to refute, could not).** CONFIRMED on every checkable element; not refuted.

SPEC TEXT: design/ty2025/SPEC.md:179-180 says verbatim "the rate, threshold and floor are corroborated by taxcalc only and MUST be transcribed from the 2025 Schedule A instructions before use", and :69-70 repeats it ("currently only corroborated by taxcalc").

OTS SOURCE (~/OpenTaxSolver2025_23.06_linux64/src/taxsolve_US_1040_2025.c) — every cited line is exact: :2462-2463 both legs of the worksheet entry gate test `L[11] > 500000.0`, including the MARRIED_FILING_SEPARAT leg (only the localtax[4] de-minimis differs, 10000 vs 5000); :2466 `ws[1] = 40000.0` un-halved; :2476 `ws[5] = 250000.0` for MFS; :2484 `ws[7] = 0.30 * ws[6]`; :2494 `ws[10] = SmallerOf( ws[9]/2.0, localtax[4] )`. Plus :2405-2407 `loctaxlimit = 40000.0 / 20000.0 (MFS)` and :2459 `localtax[5] = smallerof(localtax[4], loctaxlimit)` — the pre-worksheet default the un-tripped gate leaves standing. So OTS DOES implement the rate, threshold and floor; "taxcalc only" is false as written.

SECOND ORACLE CORROBORATES THE DEFECT: taxcalc policy_current_law.json 2025 has ID_AllTaxes_c_ps mseparate = 250000, ID_AllTaxes_c_po_rate = 0.3, ID_AllTaxes_c_po_floor mseparate = 5000. OTS's own ws[5]=250000 contradicts its own >500000 gate — an internal inconsistency, not an interpretive split.

SCENARIO ARITHMETIC: MFS, AGI 400,000, SALT 25,000 -> statute/taxcalc cap = max(20,000 - 0.30*150,000, 5,000) = 5,000; OTS gate not tripped so localtax[5] = min(25,000, 20,000) = 20,000. $15,000 split, which flows into taxable income and therefore L16, the line the OTS harness compares.

NOT COVERED ELSEWHERE / NOT AN r1 RESTATEMENT: §6.2's "every disqualification COMPUTED ... and SIZED" rule extends explicitly only to "Schedule 1-A as well as Form 6251"; D-8 covers Part IV only; R-3 (:308) lists OTS as verified on §55(d)(3), the charitable ceiling and Part IV and flags Parts II/III/V as unread — Schedule A / SALT is in neither list. r1-1 was "salt_cap is not a scalar", a different claim.

TWO REFINEMENTS to fold (neither rescues the spec):
(1) Inevitability is overstated: §6.4's five-status requirement is scoped to Schedule 1-A, and §6.5 mandates one SALT household of unspecified status. An MFS SALT household is the natural choice (the spec insists MFS is "a required case, not an incidental one") and a live red, but is not strictly forced by §6.4 + §6.5.
(2) The second OTS defect is MASKED by the first: once MFS clears L[11] > 500000, ws[8] = 40000 - 0.30*(L11 - 250000) < 0 always, so ws[9] hits the 10,000 floor and ws[10] = 5,000 unconditionally. The halved-rate (15%) ramp is unreachable in current OTS; the observable behavior is exactly the cliff described ($20,000 below AGI 500,000, $5,000 above). Size the disqualification off the cliff, not off a 15% ramp.

---

### 7. Important — [constants]

**Claim.** §5.1 fixes the SALT sub-structure as per-status "cap / phase-down rate / MAGI threshold / floor" — a derived parameterization copied from taxcalc — but the instrument is a 10-line Schedule A worksheet, and the only line-by-line transcription available has a different shape that produces a different MFS answer.

**Scenario.** OTS 2025 transcribes the "State and Local Tax Deduction Worksheet" as: ws[1]=$40,000 for EVERY status, ws[5]=250,000 MFS / 500,000 otherwise, ws[7]=30% of the excess, ws[9]=floor at $10,000 for EVERY status, and MFS handled only at the last line by halving (ws[10]=ws[9]/2). That is not algebraically equal to the spec's per-status form: it yields an effective 15% MFS phase-down rate, not 30%. MFS, MAGI $270,000, SALT paid $25,000 — spec/taxcalc shape: cap = 20,000 − 0.30×20,000 = 14,000. Worksheet shape: (40,000 − 0.30×20,000)/2 = 17,000. A $3,000 swing in itemized deductions on a filed Schedule A line 5e, with no primary source in the repo to adjudicate it. The divergence is invisible in OTS's *output* (its gate bug, next finding, suppresses that band), so a green two-oracle sweep will not surface it.

**Fix.** Apply the standing transcription rule, which explicitly covers worksheets: fetch Schedule A (2025) + instructions and transcribe the State and Local Tax Deduction Worksheet line by line in its own numbering, rather than specifying a derived 4-field per-status struct. Let the worksheet decide the MFS treatment (per-status amounts vs. halve-at-the-end); do not encode taxcalc's parameterization.

**Evidence.** ~/OpenTaxSolver2025_23.06_linux64/src/taxsolve_US_1040_2025.c:2466 `ws[1] = 40000.0;` (unconditional), :2488 `ws[9] = LargerOf( ws[8], 10000.0 );` (unconditional), :2494 `ws[10] = SmallerOf( ws[9]/2.0, localtax[4] );` (MFS only) — vs SPEC.md:177-178 "Replace with a per-status sub-structure: **cap / phase-down rate / MAGI threshold / floor**"

**Verifier (tried to refute, could not).** CONFIRMED — and stronger than filed. I could not refute any element, and the primary source the finding said was missing now exists and sides against the spec.

**1. The cited evidence is exact.** `~/OpenTaxSolver2025_23.06_linux64/src/taxsolve_US_1040_2025.c` :2466 `ws[1] = 40000.0;` (unconditional), :2476/:2478 `ws[5]` = 250000 MFS / 500000, :2484 `ws[7] = 0.30 * ws[6]`, :2486 `ws[8] = ws[1] - ws[7]`, :2488 `ws[9] = LargerOf( ws[8], 10000.0 );` (unconditional), :2494 `ws[10] = SmallerOf( ws[9]/2.0, localtax[4] );` (MFS only). Line numbers and semantics verified verbatim. SPEC.md:174-178 says what is quoted.

**2. OTS is not the odd man out — it is a faithful transcription of the IRS worksheet.** I fetched the real 2025 Instructions for Schedule A (irs.gov/pub/irs-pdf/i1040sca.pdf, header "2025 Instructions for Schedule A (Form 1040)", sha256 `b0999b12…`, text layer at `/tmp/claude-1000/-scratch-code-bitcoin-tax/503ac85e-c204-4ab9-a72b-5d669c8d5f41/scratchpad/i1040sca2025.txt`, worksheet at line 502). The "State and Local Tax Deduction Worksheet" reads: **L1 "Enter $40,000"** (no MFS variant), L2 1040 line 11b, L3a–3e PR/2555/4563 add-backs, L4 add, **L5 "$500,000 ($250,000 if married filing separately)"**, L6 excess, **L7 "Multiply line 6 by 30% (0.30)"**, L8 subtract 7 from 1, **L9 "Enter the larger of the amount on line 8 or $10,000"** (no MFS variant), **L10 "Enter the smaller of the amount on line 9 (half the amount on line 9 if married filing separately) or the amount from Schedule A, line 5d."** OTS matches this line for line. So the "different shape" is the IRS's own instrument, not an OTS quirk.

**3. The statute agrees with the worksheet, not with the spec.** IRC §164(b)(6)(B): deduction shall not exceed "the applicable limitation amount **(half the applicable limitation amount in the case of a married individual filing a separate return)**". §164(b)(7)(A)(i): applicable limitation amount = **$40,000** for 2025 — *not* halved here. §164(b)(7)(B)(i): reduced by 30 percent of the excess of MAGI over "the threshold amount **(half the threshold amount** in the case of a married individual filing a separate return)" — only the *threshold* is halved. §164(b)(7)(B)(iii): "The reduction … shall not result in the applicable limitation amount being less than **$10,000**" — floor not halved. So the statutory order is: $40,000 → phase down 30% of excess over $250,000 (MFS) → floor $10,000 → **then halve for MFS**. Exactly lines 1–10. The spec's "**$20,000 MFS cap, reduced by 30% of MAGI over $250,000 MFS, floored at $5,000 MFS**" (SPEC.md:174-176) is a false restatement of §164(b)(7) for MFS: it applies the 30% to an already-halved base.

**4. The scenario occurs and the arithmetic is right.** MFS, MAGI $270,000, Schedule A line 5d $25,000 (5d > $5,000, and 11b > $250,000, so the worksheet is mandatory). Worksheet/statute: L6 20,000 → L7 6,000 → L8 34,000 → L9 34,000 → L10 min(17,000, 25,000) = **$17,000**. Spec/taxcalc: max(20,000 − 6,000, 5,000) = **$14,000**. $3,000 swing on filed Schedule A line 5e. The divergence band is MFS MAGI $250,000–$350,000, and the worst case is **$7,500 at MAGI $300,000** (worksheet 12,500 vs spec 5,000) — larger than the finding claimed.

**5. taxcalc, the spec's sole source, carries this defect.** `calcfunctions.py:1367-1372` does `salt_cap = ID_AllTaxes_c[MARS-1]` then `max(cap − excess*po_rate, po_floor[MARS-1])` with 2025 `ID_AllTaxes_c` mseparate = 20000, `_ps` mseparate = 250000, `_po_rate` = 0.3, `_po_floor` mseparate = 5000 — the exact per-status shape SPEC.md adopted, with no final halving. So the spec copied a genuine taxcalc MFS bug. The spec's own ⚠ ("corroborated by taxcalc only") was the right instinct; the fold nonetheless wrote the taxcalc-derived rule into the spec in bold as the statement of §164(b)(6).

**6. The green-sweep blindness claim checks out.** OTS's worksheet gate (:2461-2462) is `((L[11] > 500000) && (localtax[4] > 10000)) || (MFS && (L[11] > 500000) && (localtax[4] > 5000))` — the MFS disjunct also demands L[11] > 500000, so at MFS/$270,000 OTS never enters the worksheet and falls to `localtax[5] = smallerof(localtax[4], loctaxlimit)` with `loctaxlimit = 20000.0` (:2407) = **$20,000** — a *third* answer. Worksheet 17,000 / spec 14,000 / OTS output 20,000. A two-oracle sweep in this band is uninformative.

**Refutation routes, each closed.** (a) *Spec covers it elsewhere*: SPEC.md:64-70 lists "Schedule A (2025) + instructions" as STILL TO FETCH and §5.1's ⚠ puts "the rate, threshold and floor" under a transcribe-before-use obligation. That mitigates the *values* but does not close this: the spec states the wrong rule as fact, marks the per-status **cap** as "confirmed," and pre-commits the structure — and no amount of transcribing three numbers into "per-status cap/rate/threshold/floor" reproduces the worksheet unless the implementer independently derives MFS rate = **0.15** (since max(40,000−0.3E, 10,000)/2 = max(20,000−0.15E, 5,000)), which the spec's own bolded text tells them is 30%. (b) *Evidence misquoted*: no, verified line by line. (c) *Scenario impossible*: no, it is the mandatory path. (d) *Restatement of r1*: no — r1-1 established only that `salt_cap: Usd` cannot be a scalar and proposed the replacement; this finding is that the **replacement itself is wrong for MFS**, i.e. a defect introduced by the fold, which is exactly the dimension-A question.

Also worth noting for the parent: this is a textbook instance of the project's standing "transcribe, never paraphrase" rule — a 10-line worksheet was replaced by a four-parameter closed form with no equivalence proof and no KAT, and the equivalence is false on the MFS branch. Suggested fix direction: transcribe the worksheet's ten lines (single $40,000, single $500,000/$250,000 threshold, 30%, single $10,000 floor, final MFS halving at line 10) plus the non-worksheet path from the Line 5e instruction ("smaller of line 5d or $40,000 ($20,000 if MFS)"), and add an MFS golden in the $250k–$350k band. The fetched primary source is at the scratchpad path above (sha256 `b0999b12d9dc4b13868501a9c1879011c00d2fc035f13a1391251d355a6242c8`) and should be archived per SPEC.md §2; I did not mutate the repo. taxcalc's MFS SALT handling is a real upstream defect, but per the project's one-oracle rule it needs the second witness before filing — OTS and the IRS worksheet both supply it here.

---

### 8. Minor — [fold-integrity]

**Claim.** D-4 states the direction of the QBI defect backwards — omitting line 13b overstates `ti_before_qbi`, so the §199A(e)(2) refuse fires too EARLY (a false refusal), not "too late".

**Scenario.** TY2025 single filer with QBI whose true taxable-income-before-QBI is just under the §199A(e)(2) threshold and who has a $6,000 senior deduction: btctax's overstated value crosses the threshold and refuses a return it is entitled to compute. An implementer trusting D-4 hunts for an under-refusal that does not exist, and may fix only the deduction limit while leaving the refuse predicate fed by the overstated value.

**Fix.** In D-4, replace "firing `qbi_over_threshold` too late" with "firing `qbi_over_threshold` too early — a false refusal". The prescribed ordering `11b − 12e − 13b` is unchanged and fixes both.

**Evidence.** crates/btctax-core/src/tax/qbi.rs:103 `&& ti_before_qbi > params.qbi_ti_threshold(status)` — a larger `ti_before_qbi` trips the refuse sooner. SPEC.md:113-114.

**Verifier (tried to refute, could not).** non-blocking, not verified

---

### 9. Nit — [constants]

**Claim.** D-1 binds "Schedule 1-A is implemented IN FULL — all five parts", but the 2025 form has six parts.

**Scenario.** §7 of the spec makes "is every Schedule 1-A part/line present?" a conformance test. A test written from D-1's count enumerates Parts I-V and silently omits Part VI. §5.2 does state line 38 in prose, so nothing is lost today, but the binding decision and the test's part list disagree with the form.

**Fix.** Say six parts, and add the Part VI row to §5.2's table (line 38, "Total Additional Deductions").

**Evidence.** f1040s1a--2025.pdf text layer: " Part VI         Total Additional Deductions" immediately above line 38; SPEC.md:81

**Verifier (tried to refute, could not).** non-blocking, not verified

---

## Refuted by verification

Recorded because a refuted finding is still evidence about where reviewers look.

- **Important** [fold-integrity] r1-5/r1-4 only partially closed: D-8's "named and sized" disqualification omits OTS 2025's MFS gate and is scoped to a Schedule 1-A line, but the Part IV error propagates through L13b into the only two figures ots_direct.py actually returns (L16, L24).
  - *Refuted:* REFUTED on three independent grounds: the spec covers the MFS gate two paragraphs above D-8, the cited harness evidence is factually wrong, and §6.4 already requires the scenario as a test cell.

**1. The spec already names the MFS gate, with the same source line, in D-7.** `/scratch/code/bitcoin_tax/design/ty2025/SPEC.md:131-133` reads verbatim: "★ **The oracles split, and it is adjudicated against the form.** OTS bars *all four* parts for MFS (`taxsolve_US_1040_2025.c:1824`: `if (status != MARRIED_FILING_SEPARAT)`); taxcalc allows Part IV for MARS=3. **The form governs: Part IV is allowed for MFS**, and OTS is disqualified on it (D-8)." That is the mechanism named with file:line, the adjudication against the form, and an *unconditional* disqualification of OTS on Part IV for MFS — sized, since OTS's Part IV is suppressed in full (`sched1A_L[30]` never assigned, `L13b = sched1A_L[38]` at :1933 = 0). The finding's EVIDENCE line describes :133 as "D-7 delegates the MFS split to D-8", which inverts the cross-reference: D-7 *states* the fact and points at D-8 as the decision carrying Part IV's one-oracle posture. §6.2 requires the predicate be "COMPUTED from the defect's own mechanism", not "computed from D-8's bullet list"; the MFS mechanism is printed with its source line. A predicate built from the spec as written yields `status == MFS ∧ Part IV > 0 ⇒ OTS disqualified`, which fires on the scenario.

**2. The placement is correct, not an omission.** D-8's heading is "OTS 2025's Part IV **defects**" — three coding bugs (wrong `300.0` constant at :1893, int truncation of `sched1A_L[27]/1000.0`, the `S1A_20` mislabel). The MFS gate is not a Part IV coding defect; it is a deliberate filing-status eligibility disagreement spanning Parts II–V, which is precisely D-7's subject. I confirmed against the text layer of `design/amt-form6251/f1040s1a--2025.pdf` that Parts II, III and V each print "If married, you must file jointly to claim this deduction" while Part IV's Caution says only "you, or your spouse if married filing jointly, paid or accrued qualified passenger vehicle loan interest (QPVLI)" — no joint-filing bar. So D-7's adjudication is right and its home for the gate is right. Wanting a fourth D-8 bullet is a cross-reference tightening (Nit), not a gap a green suite hides.

**3. The cited harness evidence is false.** `scripts/oracle/ots_direct.py:464-476` is one dict literal: :465 `"adjusted_gross_income": final.get("L11")`, **:466 `"taxable_income": final.get("L15", 0.0)`**, :468 `"income_tax_before_credits": L16`, :474 `"total_tax": L24 + niit`, :476 `"deduction_taken": L12`. So "the only two figures ots_direct.py actually returns (L16, L24)" is contradicted by the two lines immediately above the one it cites. L15 is where L13b lands most directly (:2613 `L[14] = L[12] + L[13] + L13b`; :2616 `L[15] = L[11] - L[14]`), it is returned, and it is *asserted*: `crates/btctax-core/tests/golden_returns.rs:255-259,287,293` compares `ar.taxable_income` against both `e.taxable_income` (OTS) and `t.taxable_income` (taxcalc `c04800`, `gen_goldens.py:161`). The scenario's "$10,000 high" L15 surfaces on the most direct compared line, not only downstream in L16/L24.

**4. §6.4 already requires the case**: "Schedule 1-A reconciles part by part … and **across all five filing statuses** (r1-4)". Part IV × MFS is a required reconciliation cell, so the "0 unexpected" in §6.2 is not reachable by silence. Separately §5.3 already flags that the OTS driver emits no `S1A_*` block at all today, so `sched_1A()` is never called and L13b is silently 0 for *every* status — a broader spec'd seam that subsumes the MFS-specific worry.

All OTS citations in the finding do check out (:1824, :1893, :1933, :2613, :2616 verified verbatim at `~/OpenTaxSolver2025_23.06_linux64/src/taxsolve_US_1040_2025.c`), and the scenario is physically real — but the spec already disqualifies OTS on it by name, so r1-4/r1-5 are closed, not partially closed.

- **Important** [fold-integrity] r1-6 only partially closed: D-4 was edited to record the 2025 Form 1040 shape and the `11b − 12e − 13b` QBI ordering, but r1-6's other half — putting Form 1040 in §5's scope — was not done, and §6 gained no requirement that any reconciled TY2025 case carry both a Schedule 1-A deduction and QBI, so the defect stays invisible to every green gate.
  - *Refuted:* REFUTED — the cited evidence is accurate, but the framing ("r1-6 only partially closed", "invisible to every green gate") does not hold on three counts.

(1) The "other half" it says was skipped is a section-placement complaint, and the brief bars those. r1-6's remedy (reviews/r1-...md:30) asked for exactly two edits: "add Form 1040 (and the §5.1 QBI ordering `11b − 12e − 13b`) to §5's scope and to D-4's coverage". Every substantive element r1-6 named is present verbatim in D-4 (SPEC.md:108-115) — the 2025 shape (11a/11b, 12e, 13a, 13b, line 14 = "Add lines 12e, 13a, and 13b"), the precise failure mode ("omits 13b entirely and leave Form 8995 line 11 overstating taxable-income-before-QBI by the whole Schedule 1-A total — overstating the §199A deduction and firing `qbi_over_threshold` too late"), and the ordering `11b − 12e − 13b`. The finding concedes this. D-4 is a **binding decision**, and §3 is where this spec puts form re-transcription mandates — D-3 ("Form 6251 Part I is re-transcribed for 2025") is likewise not in §5. An implementer is bound by D-4 regardless of which section number it sits under.

(2) The §6 half was never r1-6's ask, so it is not an unclosed fold. The finding's own contrast is backwards: r1-1 and r1-2 got §6.5/§6.6 **because they explicitly demanded them** ("add ... to §6 as a numbered requirement, not an open question"; "require a test asserting the four TY2025 `std_deduction` values"). r1-6 demanded neither; it mentioned "the fixture has no QBI vector at all" only as evidence. The fold gave each r1 finding what it asked for.

(3) "Nothing reds" is not established. §6.2 requires both oracles, per year, 0 unexpected over the golden-corpus sweep, which compares `qbided` and `c04800` per household (scripts/oracle/gen_goldens.py:161-162); §5.3 names `gen_goldens.py` as the Schedule 1-A harness; §6.4 requires Schedule 1-A to reconcile part by part across all five statuses through it. Oracle 2 does witness exactly this defect: taxcalc's `TaxInc` (.venv/.../taxcalc/calcfunctions.py) computes `odeds = senior_deduction + overtime_income_deduction + tip_income_deduction + auto_loan_interest_deduction` then `pre_qbid_taxinc = max(0., c00100 - max(c04470, standard) - c04600 - odeds)` before the QBI income limit — i.e. it subtracts line 13b where the buggy `agi - deduction` would not. And the corpus that harness runs already carries `se` (the Schedule C QBI source) in `PAIRWISE_AXES` (scripts/oracle/corpus.py:227-236) behind a committed `assert_pairwise_t2_coverage` guard (corpus.py:587, invoked at gen_goldens.py:373), plus the anchor `single_miner_qbi_limited_by_net_capital_gain` (corpus.py:425) which already pins the binding 20%-of-(TI − net capital gain) limit. Any Schedule 1-A axis added to that array forces the QBI × Schedule 1-A co-occurrence by construction.

Two evidence points check out but do not carry the claim: `ti_before_qbi: agi - deduction` is real (crates/btctax-core/src/tax/return_1040.rs:1365, and qbi.rs documents it as "1040 AGI − L12"), and `form6251_vectors.json` has zero hits for qbi/reit/business. But that fixture takes taxable income as a *derived input* (`taxable_income_1040_L15` → `line1`) and never computes Form 8995 — it is not a harness where a line-11 defect could ever red, in 2024 or 2025, so its lack of a QBI vector is not evidence of a gate gap. The scenario itself is arithmetically real (e.g. Single/66, REIT $5,000, LTCG $50,000, other $20,000: correct deduction $250 vs buggy $1,000, capped at 20% × $6,000 = $1,200) — but that is r1-6's already-folded defect, which D-4 now names and forbids.

Residue, at most Minor and not gating: §6 could cheaply say "at least one reconciled TY2025 household carries both a Schedule 1-A deduction and QBI", since point (3) is contingent on the plan putting Schedule 1-A in the corpus's axis array rather than as standalone anchors.

- **Important** [constants] §5.1 asserts "The cap is confirmed" for the $40,000/$20,000 SALT cap on the strength of a single oracle (taxcalc) and no primary source, which exempts that one constant from §2's fetch gate and directly contradicts §2's own statement that the SALT parameters are unverified.
  - *Refuted:* REFUTED on three independent grounds — the quoted text does not create an exemption, the scenario cannot occur, and the factual premise ("single oracle") is wrong.

1. No field is exempted from §2's gate. §2's gate is stated at /scratch/code/bitcoin_tax/design/ty2025/SPEC.md:64 as "**STILL TO FETCH AND ARCHIVE before implementation**", and its Schedule A bullet (:69-70) is unconditional. That gate is on *implementation start*, not per-field, so there is no window in which any SALT constant — cap included — is typed while Schedule A is unfetched. §5.1's clause (:178-180) adds an extra "before use" emphasis on rate/threshold/floor; absence of that emphasis on the cap is not a carve-out, and nothing in §5.1 says the cap may be typed early. The cap is independently covered by §2's per-field rule (:75-76, "a field without one does not ship") and by §7 R-5 (:314, "Every field gets a citation, or it does not ship"). The scenario's premise — "the rate/threshold/floor wait for the Schedule A fetch, but 40,000/20,000 gets typed now" — requires ignoring :64, which §5.1 explicitly cross-references.

2. The scenario is not a realizable state. §5.1:176-178 mandates replacing the scalar `salt_cap: Usd` (crates/btctax-core/src/tax/tables.rs:326, consumed as a bare `min` at return_1040.rs:322-327 and printed.rs:1145) with a per-status sub-structure of cap / rate / threshold / floor. The cap has no field to be typed into until that four-member structure exists, and the other three members are hard-gated. "Cap ships alone, ahead of the others" is not a state this design admits.

3. The "single oracle" premise is false against primary evidence in the environment. OTS 2025 (~/OpenTaxSolver2025_23.06_linux64/src/taxsolve_US_1040_2025.c:2405-2407) sets `loctaxlimit = 40000.0; /* Updated for 2025. */` and `20000.0` for MARRIED_FILING_SEPARAT — independently of taxcalc's `ID_AllTaxes_c` 2025 = [40000, 40000, 20000, 40000, 40000] (verified via .venv/bin/python against policy_current_law.json). OTS's worksheet at :2466-2486 also carries ws[1]=40000, ws[5]=500000/250000 (MFS), 0.30, and floor 10000 (halved to 5000 for MFS at ws[10]) — i.e. the entire structure §5.1 states. So the cap is the *most* corroborated of the four SALT values, two-oracle agreed including the MFS 20,000; the finding's harm premise ("the one field waved past the gate is the one field the gate existed for") is inverted. The oracles are not split on the MFS cap: both say 20,000. (What differs in OTS is only the worksheet *trigger* test at :2462-2463, which uses L[11] > 500000 for MFS while ws[5] = 250000 — a threshold-gating defect, not a cap defect.)

Residual, non-blocking: §5.1's word "confirmed" cites only taxcalc where OTS would have made it two-oracle, and §2:69-70's "only corroborated by taxcalc" understates the repo's own evidence. That is a citation-strengthening Nit whose direction is conservative (the spec undersells its support, never oversells it), and it is precisely the "§X disagrees with §Y" class CLAUDE.md says to stop on rather than gate. The cited evidence also confirms the finding's own PDF observation — pdftotext -layout over f1040--2025.pdf, f1040s1a--2025.pdf and i6251--2025.pdf yields no SALT text, and crates/btctax-forms/forms/2025/ has no Schedule A — but that is exactly why §2:64 gates implementation on the fetch, which the spec already does.

- **Important** [constants] OTS 2025 skips the SALT phase-down worksheet entirely for MFS filers below $500,000 of AGI, so the SALT household §6.5 now *requires* will split against OTS in MFS with no named, sized disqualification — D-8 and R-3 cover only Schedule 1-A.
  - *Refuted:* REFUTED — the cited OTS line is accurate and the OTS bug is real, but the load-bearing claim ("the SALT household §6.5 now *requires* will split") is false: nothing in the spec mandates a vector in the defect band, and the band is far narrower than the finding asserts.

1) EVIDENCE IS ACCURATE (confirmed, not disputed). `~/OpenTaxSolver2025_23.06_linux64/src/taxsolve_US_1040_2025.c:2462-2463` reads exactly as quoted — both arms gate on `L[11] > 500000.0`, including the MFS arm. taxcalc corroborates the statutory MFS parameters (`policy_current_law.json`: `ID_AllTaxes_c` mseparate=20,000 · `ID_AllTaxes_c_ps` mseparate=250,000 · `_po_rate` 0.30 · `_po_floor` mseparate=5,000), so MFS/AGI 400k/SALT 25k would indeed be OTS $20,000 vs statute $5,000.

2) THE SPLIT BAND IS MFS ∩ AGI (250,000, 500,000] ONLY — I read the whole worksheet at :2464-2500, not just the gate. Above $500,000 MFS the gate fires and OTS is *correct by coincidence*: `ws[5]=250000`, `ws[8]=40000−0.30×(AGI−250000)`, `ws[9]=LargerOf(ws[8],10000)`, then `ws[10]=SmallerOf(ws[9]/2, localtax[4])`. For any MFS AGI > 500,000, ws[8] ≤ −35,000 → ws[9]=10,000 → /2 = 5,000 = the statutory floor. For Single/MFJ/HoH/QSS the worksheet is the statute line-for-line (ws[1]=40,000, ws[5]=500,000, 0.30, floor 10,000, no halving), and both skip branches are algebraically identical to running it (AGI ≤ 500k ⇒ no phase-down, cap 40,000; localtax[4] ≤ 10,000 ⇒ min(ws[9]≥10,000, localtax[4]) = localtax[4]).

3) §6.4 + §6.5 DO NOT "TOGETHER MANDATE EXACTLY THIS VECTOR" — that is a scope conflation of two independently scoped requirements:
   - §6.5 requires a TY2025 golden household above the phase-down threshold and fixes **no filing status**. The corpus generator it lands in is Single/MFJ only: `/scratch/code/bitcoin_tax/scripts/oracle/corpus.py:67` `STATUS = ["Single", "Married/Joint"]`, and `scripts/oracle/sweep.py:94` `STATUSES = ["Single", "Married/Joint"]`. For those statuses "above the threshold" means AGI > 500,000 — precisely where OTS is correct. Zero split.
   - §6.4's "across all five filing statuses" is scoped to **Schedule 1-A**, not SALT. The only Part MFS can claim is Part IV, whose knee/cap live at MAGI 100,000–150,000 (spec §5.2 table: cap 10,000, $200 per $1,000 over 100,000 ⇒ zero by 150,000) — below the 250,000 MFS SALT threshold. Parts II/III/V for MFS are zero-value assertions. An MFS Sch 1-A vector pushed into 250k–500k would zero out Part IV and defeat its own purpose.
   - §5.5's MFS vectors are "above the 900,350 kicker start" — outside the band. And `crates/btctax-core/src/tax/fixtures/form6251_vectors.json` has no SALT input at all: the input keys across all 29 vectors are `cash_gift, filing_status, net_ltcg, sch3_line1_ftc, state_refund, wages`; every MFS vector (V8 895,950 · V22 740,000 · V23/24/25 905,000) is above 500,000 anyway.

4) THE GOVERNING RULE IS ALREADY GENERAL. §6.2 requires "every disqualification COMPUTED from the defect's own mechanism and SIZED" for both oracles — it is not an enumeration. D-8/R-3 are Schedule 1-A-specific because Schedule 1-A is the one surface where the spec *does* mandate a five-status reconciliation against a defective OTS path. Separately, §5.1 already flags that the MFS rate/threshold/floor are taxcalc-corroborated only and "MUST be transcribed from the 2025 Schedule A instructions before use" — the claim's own statutory premise is a step the spec has already scheduled.

RESIDUE (not a blocking finding): if implementation ever chooses to build an MFS SALT household with AGI in (250,000, 500,000], OTS will split by exactly (min(SALT,20,000) − max(20,000 − 0.30×(AGI−250,000), 5,000)). That is a Minor note worth carrying into the plan, not an Important spec defect — no §6 requirement produces such a vector, and no current harness even draws MFS.

- **Critical** [still-hidden] Part IV's only surviving oracle (taxcalc, after D-8 disqualifies OTS) gives a QSS the MFJ-doubled $200,000 phase-out start, while Schedule 1-A line 26 says "$100,000 ($200,000 if married filing jointly)" — so QSS Part IV has zero valid witnesses and §6.4's five-status reconciliation can only go green by encoding taxcalc's number.
  - *Refuted:* REFUTED — the two factual premises check out, but the load-bearing consequence ("zero valid witnesses", "green only by encoding taxcalc") does not.

WHAT IS TRUE (verified, primary sources):
1. Form: `pdftotext -layout design/amt-form6251/f1040s1a--2025.pdf` line 26 = "Enter $100,000 ($200,000 if married filing jointly)". A QSS does not file a joint return, so line 26 = $100,000. Scenario arithmetic on the form is right: L27=50,000, L28=50, L29=$10,000, L30=$0.
2. taxcalc: `.venv/.../taxcalc/policy_current_law.json` `AutoLoanInterestDed_ps` widow = 200000.0, while `TipIncomeDed_ps`/`OvertimeIncomeDed_ps`/`SeniorDed_ps` all give widow the SINGLE value (150000/150000/75000). With `calcfunctions.py:1111` `po_start = AutoLoanInterestDed_ps[MARS - 1]`, MARS=5 at MAGI $150,000 never enters the phase-out branch and returns the full $10,000. taxcalc is genuinely wrong for QSS on Part IV (and internally inconsistent with its own Parts II/III/V).

WHY THE FINDING IS REFUTED:

(a) OTS is NOT wrong for QSS on line 26, so the "only surviving oracle" premise is false. `~/OpenTaxSolver2025_23.06_linux64/src/taxsolve_US_1040_2025.c:1888-1891`:
    if (status == MARRIED_FILING_JOINTLY)  sched1A_L[26] = 200000.0;  else  sched1A_L[26] = 100000.0;
`WIDOW` is #define'd as 5 (:49) and is a DISTINCT global status. The only WIDOW→MARRIED_FILING_JOINTLY coercions in the file (:100, :114) rewrite a *local parameter* inside `TaxRateFormula` and `Report_bracket_info`; `sched_1A()` (called at :2380) reads the global `status`, which is still WIDOW. It also passes the `status != MARRIED_FILING_SEPARAT` gate (:1824), so Part IV is computed. QSS therefore gets $100,000 — the form's number — from OTS.

(b) On the finding's OWN scenario, OTS returns the correct $0, so the claim "nothing witnesses the correct $0" is false. OTS: L27 = 150,000−100,000 = 50,000; `j = 50000/1000` = 50 (int truncation is a no-op on a whole number); L29 = 300×50 = 15,000; L30 = NotLessThanZero(10,000 − 15,000) = **0**. Form: L28 = 50, L29 = 200×50 = 10,000, L30 = **0**. Both D-8 defects (rate $300 vs $200, floor vs ceil) push the phase-out above the $10,000 cap, so line 30 floors to zero either way. OTS witnesses the form exactly; taxcalc's $10,000 is the lone outlier and shows up as a split, not as a silent consensus.

(c) The D-8 disqualification is not the blanket the finding assumes. SPEC §6.2 requires every disqualification be "COMPUTED from the defect's own mechanism and SIZED — never a list of vector names", and D-8 (line 147) says Part IV rides taxcalc "plus this named disqualification — computed from the mechanism, per §6.2", sizing it with a worked case (MAGI $120,000 → form $6,000 vs OTS $4,000). The mechanism-computed predicate is max(0, C − 300·floor(E/1000)) ≠ max(0, C − 200·ceil(E/1000)); at E=50,000, C=10,000 it does not fire. Reading D-8 as "OTS is disqualified on all of Part IV for all vectors" is precisely the vector-list disqualification the spec bans.

(d) "Green only by encoding taxcalc's number" contradicts controlling text already in the spec. §1 guardrail: "A disagreement is adjudicated against the FORM, never encoded." §1 also mandates verbatim line-by-line transcription, so btctax's line 26 carries the form's own "($200,000 if married filing jointly)" test and cannot silently acquire taxcalc's widow value. D-7 (lines 127-135) is a worked precedent in this exact Part IV: the oracles split on the MFS bar, "The form governs", and the losing oracle was disqualified. §6.2's closing sentence ("Any part shipping on one oracle must say so, in the same census that says it for AMT") covers the residue.

NOT PART OF THIS FINDING, but worth the plan's attention (offered as observation, not as a surviving Critical/Important): there is a narrow QSS band — MAGI roughly $100k–$149k with a nonzero resulting deduction — where OTS's rate/rounding defect DOES bite and taxcalc's widow `_ps` is also wrong, so neither oracle matches the form. That is a two-oracle split (diagnostic per CLAUDE.md), adjudicated against the form by §1, and it is already inside the §6.2 "named and sized, per part and per status" obligation. It is also reachable only after the r1-7 fix to `gen_goldens.py` maps QSS to MARS=5 (today it maps every non-MFJ status to MARS=1, which accidentally gives QSS the correct $100,000). Naming taxcalc's widow `_ps` alongside D-8's OTS defects would be a Minor improvement, not a blocking gap.
