# Polish batch — independent review r1 — NOT GREEN (2 Important: ProRata note unrenderable, P3-e wiring untested)

_Fable, independent. P2-d/P2-b/(m)/(l)/Task-2(d) correct + mutation-held; cargo-deny sensible; P3-d deferral
right. Blocked by 2 Importants (both cheap/contained) + Minor-1 doc + 3 nits._

---

VERDICT: NOT GREEN

Suite verified fresh: `make check` exit 0 (2080 pass + clippy `-D warnings`), `cargo fmt --check` clean, `pii-scan` clean. All mutation experiments were done via cp-backup/restore; worktree confirmed byte-identical to the reviewed diff afterward (only extra vs the diff file is the author's later FOLLOWUPS.md entry).

Per-item:

- **P2-d — OK** — Helper is sound: `snapshot` → `mutate(&conn)` → `and_then(save)` → restore on either failure; borrows check out against `Session` (session.rs:440-460); no skipped-restore or double-restore path; all three refactors keep guards before the snapshot, park's stash-before-delete, and return types (input_form_store.rs:288-300,320,360,421). KAT is discriminating — I ran the restore-only-on-save mutant and it FAILED the KAT. Note `save_draft` correctly stays outside the helper (restoring on autosave failure would throw away the user's live edits). One Minor doc defect (below).
- **P2-b — OK** — Exact structural parity with `parked_flag` (input_form_store.rs:91-104 vs 106-117); `QueryReturnedNoRows → Ok(false)`, real error propagates.
- **P3-e — OPEN** — `seed` itself is correct (`cap = FIELD_CAP.max(len)`, never below 64; per-call reset means no cap inheritance across fields since every edit session enters via `begin_edit`; Secret fields seed `""` so no digit echo; Text parse is unbounded so the value survives commit). The unit KAT is good. But the **integration wiring is unheld**: I reverted `tax_inputs.rs:184` `seed`→`set` and ALL 352 btctax-tui-edit tests still passed — the mutation survives. No test anywhere calls `begin_edit`.
- **(m) — OK** — FilingStatus is in `ReturnOptions` (depth 0 per `row_depth`, apply.rs:130-143) and its setter ignores the addr (sections.rs:109), so the KAT is discriminating; I ran the guard-removed mutant and the KAT FAILED as required. Rejection precedes materialization (`w` stays `None`).
- **(l) — OK** — The liveness assertions mirror `is_exempt` char-for-char (`==` / `"{p}."` / `"{p}["`, coverage.rs:253-281) over the same domain (`before`). Empty exempt Vecs/None structs still serialize as a leaf (`walk`'s scalar arm), so the `==` arm covers them — no false-fail.
- **Task-2(d) — OK** — All 8 `FieldValue` kinds genuinely round-tripped and asserted. `SecretEntry` serde is a plain derive (only `Debug` is custom-masked, seam.rs:154-167); digits over the inbound wire are by-design (the set path), get-side is presence-only — the test's claim is correct, and a failing assert prints the masked Debug so no digit leak. Nit: the comment says "both `Edit` variants" — `Edit` has six; the four shape variants' serde is unexercised (trivial unit-ish derives, low risk).
- **ProRata — OPEN** — Wording is accurate to the engine (lots computed once under `pre2025_method`, method-independent G3 (form.rs:2050-2058, `cycle_alloc_method` doc), tag drives only the timebar/attestation rule (resolve.rs:1210-1221), no in-tool custom-lots path). But the note **cannot render**: it is one 225-char `Line` in an unwrapped `Paragraph`, in a banner fixed at `Length(3)`, inside a modal hard-capped at 96 cols (draw_edit.rs:4144,4153-4160,4177-4184) — at most the first 94 chars ever display (78 on an 80-col terminal). Everything from "(specific-unit)…" onward — including "the tag sets only the timebar rule" and the entire actionable sentence "you must compute + attest it yourself" — is invisible on ANY terminal, and the visible text ends mid-word. Also unpinned: no render test covers this modal (the old text, itself 119 chars and already clipped, was unpinned too).
- **cargo-deny — OK** (sensibleness only) — Job matches the SHA-pinned house style (same three action SHAs), lld installed (`.cargo/config` applies to `cargo install` from CWD), `cargo deny check advisories licenses bans sources` is valid syntax, deny.toml plausible (`version = 2` tables, warn-level bans on first pass — sensibly documented). Allowlist is reasonable; `ring` 0.17.14 declares `Apache-2.0 AND ISC` (both allowed — no clarify needed); all 397 external lockfile packages are crates.io, so `sources` passes. Residual first-run risk is real but self-documented in the file.
- **P3-d-deferral — OK** — Right call. The glyph layer (draw_edit.rs:2590-2602) cannot distinguish "explicitly 0/false" from "untouched" because plain-value fields' `get` always returns `Some` — this is the answered-ness invariant, structural by definition here. The naive fix would false-✓ an untouched Payments section (worse: over-claims completeness); the current behavior errs conservative (`…`), and the glyph gates nothing (`commit` screens independently). No safe partial fix at this layer without touched-ness tracking or `Option` leaves.

## New findings

**Important-1 (ProRata note is mostly unrenderable)** — draw_edit.rs:4177-4182. Failure scenario: a user opens safe-harbor allocate on an 80-col terminal and sees `NOTE: ProRata is NOT auto-computed here — both methods attest the SAME per-wal` — truncated mid-word; the attest-only instruction this polish item exists to deliver never appears, on any terminal size (modal inner width caps at 94). Fix: split into 2-3 short `Line`s ≤ ~90 chars and grow the banner constraint (or add `.wrap()` + rows), and pin the "NOT auto-computed" fragment in a `TestBackend` render test alongside the existing 18.

**Important-2 (P3-e wiring unheld — mutation empirically survives)** — tax_inputs.rs:184. Reverting `seed`→`set` leaves the entire suite green (verified: 352/352 pass under the mutant), so the data-loss fix's only protective call site is untested — precisely the project's untested-guard pattern. Fix: one test that materializes a working return, applies a >64-char `Text` (e.g. `AddrStreet`), focuses it, calls `begin_edit`, and asserts `form.buf.as_str()` is the full value (fails today under `set`, passes under `seed`).

**Minor-1 (doc misattachment)** — input_form_store.rs:270-300: `mutate_and_save` was inserted between `commit`'s doc block and `pub fn commit`, so the entire commit contract doc (NoTables/Refused/all-or-nothing) now documents the private helper as a merged franken-doc, and `pub fn commit` is undocumented. Move the helper (and its own doc) above the commit doc block.

**Nit-1** — seam test comment "both `Edit` variants" overclaims (six variants; four shape variants' serde unexercised). **Nit-2** — `FieldBuffer::seed` would shrink a `with_cap(FREETEXT_CAP)` buffer's cap to 64 (no current caller does; latent API hazard worth a doc line). **Nit-3** — `discard_parked_draft`'s doc still says restore happens "on a failed `sess.save()`"; it is now any-error (stale but conservative).

No regressions found; P2-b/(m)/(l)/Task-2(d)/P2-d are correct and (where claimed) mutation-held. GREEN is blocked by the two Importants — both are cheap, contained fixes.
