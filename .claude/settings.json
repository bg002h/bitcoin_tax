{
  "$comment": [
    "btctax harness — design/HARNESS.md. PROJECT-SCOPED BY CONSTRUCTION: Claude Code loads this file",
    "only when working in this repository, so nothing here can fire in another project. The harness is",
    "grown from THIS repo's observed failures and must never be promoted to ~/.claude/settings.json or",
    "to the cross-project /scratch/code/CLAUDE.md without its own evidence.",
    "",
    "PreToolUse exit 2 blocks the call and feeds stderr back into the model's loop at the decision",
    "point -- which is the whole reason these are hooks and not more passive doctrine."
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/scripts/hooks/deny-bypass.sh"
          }
        ]
      },
      {
        "matcher": "Write|Edit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/scripts/hooks/on-write.sh"
          }
        ]
      }
    ]
  }
}
