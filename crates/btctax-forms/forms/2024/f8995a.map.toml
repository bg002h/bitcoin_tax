# Form 8995-A (Qualified Business Income Deduction), TY2024 — logical line → PDF field name.
#
# Extracted with `cargo run -p xtask -- dump-fields crates/btctax-forms/forms/2024/f8995a.pdf`
# (111 AcroForm fields) and correlated against `design/forms/extract/f8995a--2024.txt`.
#
# ★★★ WHY THIS FORM EXISTS ALONGSIDE f8995. i8995a: "You must complete Part I if you have QBI from a
# qualified trade, business, or aggregation. **If you don't have QBI, and only have REIT, PTP, skip
# Parts I through III and complete Part IV.**" Above the §199A(e)(2) threshold the SIMPLIFIED Form 8995
# no longer applies, so a filer there must use this form even when the arithmetic is identical — which
# is exactly why the refusal and the emitter are one unit: narrowing the refusal without this map would
# print the WRONG FORM on a filed return.
#
# Column x-clusters, the same shape as f8995: MID x ≈ [410,482], AMOUNT x ≈ [504,576].
#
# ★★ PARENTHESIZED BOXES AND THE SIGN CONVENTION. Lines 29 and 40 are printed inside literal `(   )`
# and their widgets are inset from the ordinary column — MID-paren [413.4,479.4] vs MID [410.4,481.6];
# AMOUNT-paren [507.0,573.0] vs AMOUNT [504.0,576.0]. **The parentheses supply the minus sign, so the
# value written must be a POSITIVE MAGNITUDE.** Writing `-1234` renders as `(-1,234)` — a positive
# number on a filed return.
#
# ★★★ HOW THE PART IV ASSIGNMENT WAS VERIFIED, rather than assumed from ordering. Page 2 carries
# exactly 14 non-table fields (f2_36…f2_49) and Part IV is exactly 14 lines (27–40), so a naive
# in-order zip is tempting and unfalsifiable. Three independent corroborations instead:
#
#   1. COLUMN COUNTS. Reading the form's own text, seven Part IV lines print in the MID column
#      (27, 28, 29, 30, 31, 33, 34) and seven in the AMOUNT column (32, 35, 36, 37, 38, 39, 40). The
#      dump has exactly seven fields at x≈410 and exactly seven at x≈504. Both partitions agree.
#   2. THE INSET PAIR. The two parenthesized `(loss) carryforward` lines are 29 and 40; the only two
#      inset widgets are f2_38 (413.4, in MID) and f2_49 (507.0, in AMOUNT). They land on 29 and 40.
#   3. MONOTONIC y. Field y descends 468 → 240 as the line number ascends 27 → 40, with no crossing.
#
# Any one of these alone would be weak. Together they pin the assignment without a single guess.
#
# ★ SCOPE — PART IV ONLY, deliberately. This is B1a: the REIT/PTP-only filer the form's own skip rule
# describes, who needs no input btctax does not already collect. Parts I–III (the W-2-wage and UBIA
# limitations, the SSTB phase-in) are B1b and need three new filer inputs plus the phase-in range
# width, which does not exist in `FullReturnParams` at all. Mapping them here with nothing to write
# would be an instrument nobody has watched discriminate.

form = "f8995a"
year = 2024

# ── Part IV — Determine Your Qualified Business Income Deduction ─────────────────────────────────
# 27 "Total qualified business income component from all qualified trades, businesses, or
#     aggregations. Enter the amount from line 16"
line27 = "topmostSubform[0].Page2[0].f2_36[0]"
# 28 "Qualified REIT dividends and publicly traded partnership (PTP) income or (loss). See
#     instructions"
line28 = "topmostSubform[0].Page2[0].f2_37[0]"
# 29 "Qualified REIT dividends and PTP (loss) carryforward from prior years"  — PARENTHESIZED
line29 = "topmostSubform[0].Page2[0].f2_38[0]"
# 30 "Total qualified REIT dividends and PTP income. Combine lines 28 and 29. If less than zero,
#     enter -0-"
line30 = "topmostSubform[0].Page2[0].f2_39[0]"
# 31 "REIT and PTP component. Multiply line 30 by 20% (0.20)"
line31 = "topmostSubform[0].Page2[0].f2_40[0]"
# 32 "Qualified business income deduction before the income limitation. Add lines 27 and 31"
line32 = "topmostSubform[0].Page2[0].f2_41[0]"
# 33 "Taxable income before qualified business income deduction"
line33 = "topmostSubform[0].Page2[0].f2_42[0]"
# 34 "Enter your net capital gain, if any, increased by any qualified dividends (see instructions)"
line34 = "topmostSubform[0].Page2[0].f2_43[0]"
# 35 "Subtract line 34 from line 33. If zero or less, enter -0-"
line35 = "topmostSubform[0].Page2[0].f2_44[0]"
# 36 "Income limitation. Multiply line 35 by 20% (0.20)"
line36 = "topmostSubform[0].Page2[0].f2_45[0]"
# 37 "Qualified business income deduction before the domestic production activities deduction (DPAD)
#     under section 199A(g). Enter the smaller of line 32 or line 36"
line37 = "topmostSubform[0].Page2[0].f2_46[0]"
# 38 "DPAD under section 199A(g) allocated from an agricultural or horticultural cooperative. Don’t
#     enter more than line 33 minus line 37"
line38 = "topmostSubform[0].Page2[0].f2_47[0]"
# 39 "Total qualified business income deduction. Add lines 37 and 38"
line39 = "topmostSubform[0].Page2[0].f2_48[0]"
# 40 "Total qualified REIT dividends and PTP (loss) carryforward. Combine lines 28 and 29. If zero or
#     greater, enter -0-"  — PARENTHESIZED
line40 = "topmostSubform[0].Page2[0].f2_49[0]"

# ── Header ───────────────────────────────────────────────────────────────────────────────────────
# REQUIRED, like every full-return schedule: a form that does not name its taxpayer is not filable.
#
# ★ LAST on purpose. TOML binds every bare key after a table header INTO that table, so an `[identity]`
#   block placed above the `lineNN` keys silently swallows all fourteen of them — the parse then fails
#   with "missing field `line27`", which reads like a typo and is really a layout error.
[identity]
name = "topmostSubform[0].Page1[0].f1_01[0]"
ssn  = "topmostSubform[0].Page1[0].f1_02[0]"

# ── The census: every one of the 111 AcroForm fields is accounted for ────────────────────────────
#
# ★★★ B1a fills PART IV ONLY. The 95 fields below are Parts I-III, and each is `unmodeled` rather
# than forgotten: `unmodeled` means a path btctax does not take, and the QbiAboveThreshold refusal
# still stands for the filer who would need them. A filer who reaches Part IV never touches these.
#
# ★ Grouped by PART, because the reason really is uniform within each — and stating one honest reason
# per part is worth more than 95 copies of a sentence nobody wrote.

[census]


# ── Part I ──
"topmostSubform[0].Page1[0].Table_PartI[0].RowA[0].f1_03[0]" = { line = "Part I RowA", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowA[0].f1_04[0]" = { line = "Part I RowA", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowA[0].c1_3[0]" = { line = "Part I RowA", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowA[0].c1_1[0]" = { line = "Part I RowA", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowA[0].c1_2[0]" = { line = "Part I RowA", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowB[0].f1_05[0]" = { line = "Part I RowB", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowB[0].f1_06[0]" = { line = "Part I RowB", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowB[0].c1_6[0]" = { line = "Part I RowB", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowB[0].c1_4[0]" = { line = "Part I RowB", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowB[0].c1_5[0]" = { line = "Part I RowB", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowC[0].f1_07[0]" = { line = "Part I RowC", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowC[0].f1_08[0]" = { line = "Part I RowC", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowC[0].c1_9[0]" = { line = "Part I RowC", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowC[0].c1_7[0]" = { line = "Part I RowC", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }
"topmostSubform[0].Page1[0].Table_PartI[0].RowC[0].c1_8[0]" = { line = "Part I RowC", rule = "unmodeled", reason = "Part I identifies each trade, business or aggregation and its SSTB / aggregation / patron checkboxes. B1a covers the REIT/PTP-only filer, whom i8995a's own rule sends straight to Part IV: \"If you don't have QBI, and only have REIT, PTP, skip Parts I through III and complete Part IV.\" Reaching Part I needs the SSTB DECLARATION, which does not exist as an input yet (B1b)." }

# ── Part II ──
"topmostSubform[0].Page1[0].Table_PartII[0].Row2[0].f1_09[0]" = { line = "Part II Row2", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row2[0].f1_10[0]" = { line = "Part II Row2", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row2[0].f1_11[0]" = { line = "Part II Row2", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row3[0].f1_12[0]" = { line = "Part II Row3", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row3[0].f1_13[0]" = { line = "Part II Row3", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row3[0].f1_14[0]" = { line = "Part II Row3", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row4[0].f1_15[0]" = { line = "Part II Row4", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row4[0].f1_16[0]" = { line = "Part II Row4", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row4[0].f1_17[0]" = { line = "Part II Row4", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row5[0].f1_18[0]" = { line = "Part II Row5", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row5[0].f1_19[0]" = { line = "Part II Row5", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row5[0].f1_20[0]" = { line = "Part II Row5", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row6[0].f1_21[0]" = { line = "Part II Row6", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row6[0].f1_22[0]" = { line = "Part II Row6", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row6[0].f1_23[0]" = { line = "Part II Row6", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row7[0].f1_24[0]" = { line = "Part II Row7", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row7[0].f1_25[0]" = { line = "Part II Row7", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row7[0].f1_26[0]" = { line = "Part II Row7", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row8[0].f1_27[0]" = { line = "Part II Row8", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row8[0].f1_28[0]" = { line = "Part II Row8", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row8[0].f1_29[0]" = { line = "Part II Row8", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row9[0].f1_30[0]" = { line = "Part II Row9", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row9[0].f1_31[0]" = { line = "Part II Row9", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row9[0].f1_32[0]" = { line = "Part II Row9", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row10[0].f1_33[0]" = { line = "Part II Row10", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row10[0].f1_34[0]" = { line = "Part II Row10", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row10[0].f1_35[0]" = { line = "Part II Row10", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row11[0].f1_36[0]" = { line = "Part II Row11", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row11[0].f1_37[0]" = { line = "Part II Row11", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row11[0].f1_38[0]" = { line = "Part II Row11", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row12[0].f1_39[0]" = { line = "Part II Row12", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row12[0].f1_40[0]" = { line = "Part II Row12", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row12[0].f1_41[0]" = { line = "Part II Row12", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row13[0].f1_42[0]" = { line = "Part II Row13", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row13[0].f1_43[0]" = { line = "Part II Row13", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row13[0].f1_44[0]" = { line = "Part II Row13", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row14[0].f1_45[0]" = { line = "Part II Row14", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row14[0].f1_46[0]" = { line = "Part II Row14", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row14[0].f1_47[0]" = { line = "Part II Row14", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row15[0].f1_48[0]" = { line = "Part II Row15", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row15[0].f1_49[0]" = { line = "Part II Row15", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row15[0].f1_50[0]" = { line = "Part II Row15", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row16[0].f1_51[0]" = { line = "Part II Row16", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row16[0].f1_52[0]" = { line = "Part II Row16", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }
"topmostSubform[0].Page1[0].Table_PartII[0].Row16[0].f1_53[0]" = { line = "Part II Row16", rule = "unmodeled", reason = "Part II is the W-2-wage and UBIA limitation. It needs two inputs `QbiInputs` does not carry — allocable W-2 wages (line 4) and UBIA of qualified property (line 7). Collecting them is B1b; computing the limitation without them is what the QbiAboveThreshold refusal still prevents." }

# ── Part III ──
"topmostSubform[0].Page2[0].Table_PartIII[0].Row17[0].f2_01[0]" = { line = "Part III Row17", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row17[0].f2_02[0]" = { line = "Part III Row17", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row17[0].f2_03[0]" = { line = "Part III Row17", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row18[0].f2_04[0]" = { line = "Part III Row18", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row18[0].f2_05[0]" = { line = "Part III Row18", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row18[0].f2_06[0]" = { line = "Part III Row18", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row19[0].f2_07[0]" = { line = "Part III Row19", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row19[0].f2_08[0]" = { line = "Part III Row19", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row19[0].f2_09[0]" = { line = "Part III Row19", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row20[0].Ln20[0].f2_10[0]" = { line = "Part III Row20", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row20[0].f2_11_RO[0]" = { line = "Part III Row20", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row20[0].f2_12_RO[0]" = { line = "Part III Row20", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row20[0].f2_13_RO[0]" = { line = "Part III Row20", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row21[0].Ln21[0].f2_14[0]" = { line = "Part III Row21", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row21[0].f2_15_RO[0]" = { line = "Part III Row21", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row21[0].f2_16_RO[0]" = { line = "Part III Row21", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row21[0].f2_17_RO[0]" = { line = "Part III Row21", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row22[0].Ln22[0].f2_18[0]" = { line = "Part III Row22", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row22[0].f2_19_RO[0]" = { line = "Part III Row22", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row22[0].f2_20_RO[0]" = { line = "Part III Row22", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row22[0].f2_21_RO[0]" = { line = "Part III Row22", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row23[0].Ln23[0].f2_22[0]" = { line = "Part III Row23", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row23[0].f2_23_RO[0]" = { line = "Part III Row23", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row23[0].f2_24_RO[0]" = { line = "Part III Row23", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row23[0].f2_25_RO[0]" = { line = "Part III Row23", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row24[0].Ln24[0].f2_26[0]" = { line = "Part III Row24", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row24[0].f2_27_RO[0]" = { line = "Part III Row24", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row24[0].f2_28_RO[0]" = { line = "Part III Row24", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row24[0].f2_29_RO[0]" = { line = "Part III Row24", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row25[0].f2_30[0]" = { line = "Part III Row25", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row25[0].f2_31[0]" = { line = "Part III Row25", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row25[0].f2_32[0]" = { line = "Part III Row25", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row26[0].f2_33[0]" = { line = "Part III Row26", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row26[0].f2_34[0]" = { line = "Part III Row26", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
"topmostSubform[0].Page2[0].Table_PartIII[0].Row26[0].f2_35[0]" = { line = "Part III Row26", rule = "unmodeled", reason = "Part III is the phased-in reduction. Beyond the two Part II inputs it needs the PHASE-IN RANGE WIDTH ($50,000 / $100,000, §199A(b)(3)(B)(ii)(II)), which `FullReturnParams` does not carry at all — and which is STATUTORY, not inflation-indexed, unlike the threshold beside it. B1b." }
