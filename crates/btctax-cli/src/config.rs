//! CLI configuration — vault path, cost-basis method, and other session-wide settings (filled in Task 1).

/// Session-wide CLI configuration loaded from flags and/or a config file.
pub struct CliConfig;
