# FINAL review — whole branch `main..HEAD`, pointed at INTERACTION (harness B3)

_Fable, read-only, dispatched per `reviews/BRIEF-filing-readiness-final.md`. 34 commits, 75 files,
+8465/−217. The last review before the branch is handed to its owner. **Persisted VERBATIM before any
fold.** The agent could not write this file itself (the harness blocks subagents from writing report
files), so the controller is the scribe; the text is copied verbatim, in its own commit, before any
fold._

---

VERDICT: needs-changes

CROSS-PHASE FINDINGS:

**1. The CWA gate never fires for a STANDARD-deduction filer whose gift defers to carryover — the R2 fold's predicate sits in front of the R3 fold's disjunct, and their rationales collide in exactly one region, which is the natural population.**

- THE SEAM: two folds inside one un-reviewed commit (`531f6817`, authored *after* the phase-2 review was persisted, so no review has seen it), interacting with each other and with lane C's carryover machinery (phase 1). The R2/R5a fix established "standard deduction ⇒ nothing sworn ⇒ safe", and put `ar.deduction_is_itemized` in front of refusals. The R3 fix established "a §170(b)-ceiling-zeroed claim is DEFERRED, not denied, and the CWA deadline dies at THIS filing", and added the `cwa_deferred_to_carryover` disjunct (`crates/btctax-core/src/tax/return_1040.rs:2441-2448`). But the disjunct sits *behind* `ar.deduction_is_itemized &&`. R3's reasoning does not depend on the election: §170(d)(1) creates the excess-over-ceiling carryover regardless of §63(e), `apply_170b` runs unconditionally for exactly that reason (comment at `return_1040.rs:2774-2777`), the P6 advisory tells the filer to roll it (`render.rs:1684-1691`), and `apply_carryover_writeback` persists it (its vouch-for gate checks restrictions, never the CWA). Next year's line-13 claim is deliberately outside the gate ("the carryover year's CWA deadline passed with that year's return", `return_1040.rs:2403-2406`). No per-item review could see this: R3 was found on an *itemizing* fixture, the fold implemented the reviewer's ask verbatim, and the standard-deduction variant is precisely the population R2 had just declared safe.
- THE FAILING CASE: Single renter, AGI $40,000, standard deduction, one $25,000 appreciated-BTC gift (ledger `Removal`, ≥$250). 30% ceiling = $12,000 < $14,600 standard ⇒ election is standard; whole allowed amount + $13,000 excess defer/carry. `deduction_is_itemized` is false ⇒ the CWA question is never posed as a gate (the registry entry is a skippable — "the mandatory half is in `screen_absolute`", `questions.rs:1222-1227` — and that half is itemize-gated). The year files; §170(f)(8)(C)'s cure extinguishes at that filing (*Durden*); the writeback stamps the carryover `Computed`; next year line 13 deducts it unsubstantiated. This is R3's own mechanism — "D4's prong (3), the silently lost right, reappearing on the gate built to close it" — surviving on the *other* branch of the election. Note the fix shape is not simply dropping the conjunct: `Some(false)` + standard + deferral should probably not hard-refuse a return that claims nothing this year — the writeback vouch-for gate (which already refuses to persist a restriction-tainted carryover) may be the honest lever. That design question is exactly why this needed a review and did not get one.
- SEVERITY: **I** (same class and severity the phase-2 review assigned R3).

**2. The CWA question's prompt and refusal text still scope themselves to "deducting this year" — so the deferral population the R3 fold added can truthfully defeat the gate it now trips.**

- THE SEAM: the fold (`531f6817`) extended the gate's *firing condition* to deferred claims but touched neither the prompt in `questions.rs` (phase-2 text) nor the `None`-branch refusal detail. Prompt (`questions.rs:1203-1211`): *"For EVERY charitable gift of $250 or more that you are deducting **this year**, do you already hold…"* — a wholly-deferred filer deducts none this year, so a literal "yes" is vacuously true while they hold no CWA; the gate then passes and the right is lost anyway. The help's *"Skipping is harmless if you claim no charitable deduction"* is also now false for them. And the refusal detail (`return_1040.rs:2461`): *"this return claims a charitable deduction"* — false for the ceiling-zeroed year (lines 11+12 are $0). Text and mechanism were adjudicated in different rounds and nobody re-read the words after the population widened.
- THE FAILING CASE: R3's own fixture-3 household (AGI $0, itemizes on mortgage, $50k crypto gift) is refused, runs `btctax income answer`, reads a prompt about gifts "you are deducting this year", correctly observes it deducts nothing this year, answers yes — gate satisfied, CWA never obtained, cure dies at filing.
- SEVERITY: **M** (the gate does stop and ask; the instrument's words undermine it for exactly the added population).

**3. A MIXED-USE mortgage that is also OVER a debt limit: all three answers refuse, although the answer moves no figure on the return btctax would print — the branch's Critical shape at the intersection of a base-tree question and a phase-1 refusal.**

- THE SEAM: `MortgageAllUsedToBuyBuildImprove == Some(false)` (base tree, §2.7) already zeroes line 8a and checks the line-8 box (`schedule_a_parts`, `return_1040.rs:479-484`) — a *disclosed conservative zero*, D3's own words. The phase-1 `MortgageOverDebtLimit` refusal (`return_1040.rs:2308-2337`) fires independently on `mortgage_within_debt_limit == Some(false)` for any itemizing return with 1098 interest, without checking whether 8a is already $0. For that filer the refusal's stated premises are both false: "deducting the full Form 1098 figure would UNDERSTATE" (btctax would deduct $0, not the full figure) and "Schedule A has no box that would disclose such a zero" (the box IS checked, truthfully, on this return). The `screen_inputs` comment even says "unlike the mixed-use zero it has no line-8 checkbox disclosing it" (`return_refuse.rs:861-862`) — the author held the distinction and reasoned only about the non-mixed-use filer. P1 was reviewed against its own population; the mixed-use question belongs to the base tree; no window contained both.
- THE FAILING CASE: itemizer with a $1.1M post-2017 cash-out refi, ~$900k acquisition portion (so the honest debt-limit answer is No even counting only qualifying debt), part of the proceeds not used on the home (so mixed-use, honestly No), SALT + charitable big enough to itemize after 8a zeroes. `None` → `MortgageDebtLimitUnanswered` (input level); `Some(false)` → `MortgageOverDebtLimit`; `Some(true)` → false testimony. Yet the return btctax would print for them — $0 on 8a, box checked — is the identical disclosed-conservative return it prints for the mixed-use-only filer, and the over-limit fact changes nothing on it (the whole 1098 amount is already forgone). No honest in-product cure; the message's by-hand instruction sends them to a Pub. 936 worksheet whose result has no input to receive it (FOLLOWUPS P9(a) has not landed).
- SEVERITY: **I** — under the brief's literal definition this is the Critical class (a refusal stopping a filer whose return btctax can compute honestly today, with no honest answer available). I set I rather than C for two reasons the controller may weigh differently: D3's accepted adverse branch explicitly tolerates over-limit filers being unable to export until S2, and the population is the narrow intersection (acquisition portion itself over the ceiling AND mixed-use AND still itemizing).

**4. LIMITATIONS.md: "*Only ever raised on a return that itemizes*" is false of `Form4952Required`.**

- THE SEAM: phase 4's rewrite (`aa9ab054`) documented the phase-2 bound leg, but the `Some(true)` leg of the same reason code deliberately stayed in `screen_inputs` (capital-gains routing — `return_refuse.rs:831-842`) and fires on standard-deduction returns too. The doc's own adjacent bullet ("Answer **yes** and it refuses") describes that leg without the itemize qualifier, so the two bullets contradict each other about which filers `Form4952Required` can reach. A standard-deduction filer answering yes gets a refusal the promise-surface doc says can never reach them.
- SEVERITY: **M** (false statement on a promise surface; the behavior itself is correct and deliberate). May overlap with the parallel phase-4 review.

**5. LIMITATIONS.md's hand-marks list omits the Form 8283 Part IV/V mark class — recreating F2's omission on the doc surface.**

- THE SEAM: phase 1's F2 fold added a fourth mark class to the manifest (`admin.rs:388-401` — the appraiser/donee ink signatures, "the one item on this list you cannot finish at your desk tonight"); phase 4's LIMITATIONS rewrite enumerates three classes (digital-asset question, line-7 box, signature block) as the manifest's content. The omitted class is again exactly the one that takes calendar time — the same asymmetry F2 was about. Fold in one phase, doc in another; no window held both.
- SEVERITY: **M**. (Same overlap caveat as #4.)

**6. LIMITATIONS.md states `--write-carryover` "stamps the **charitable** and **QBI-REIT/PTP** carryovers" — the code stamps three (charitable, REIT/PTP, *and* the QBI business-loss carryforward, `return_1040.rs:2858-2869`), and the same file's §(ii) paragraph correctly says "the two QBI carryovers". Internal contradiction, under-listing.** SEVERITY: **N**.

PHASE 3 FINDINGS (the un-reviewed surface):

**P3-1. A collected line 9b large enough to zero line 17 makes Form 8960 vanish from the packet — the filer's sworn allocation reduces total tax by the whole former NIIT and prints nowhere.**

- `form_8960_lines` returns `None` when `line17 <= 0` ("no §1411 tax ⇒ no Form 8960", `other_taxes.rs:322-355`), and `packet.rs:588` drops the form on `None`. That predicate was sound when line 11 was structurally zero — "no tax" could only mean "form not engaged". Phase 3 made line 11 a *collected deduction*, creating a new way to reach line17 = 0: `9b ≥ line 8` is accepted whenever it is ≤ the SALT bound (the bound is min(5a, 5e), never related to line 8 — e.g. line 8 = $6k of interest, bound $10k, 9b = $7k passes the refusal). Result: MAGI over threshold, real investment income, tax reduced to $0 by a filer-claimed deduction, and no Form 8960 in the packet — while i8960's Who Must File is categorical (*"Attach Form 8960 to your return if your MAGI is greater than the applicable threshold amount"*, extract `i8960--2024.txt:61-64`). The no-9b/no-NII half of this is `main` behavior and out of scope; the new half is this branch's: a collected input that changes `total_tax` (threaded into the absolute `form_8960` precisely so the two chains agree) with **no reader on the paper** — the repo's own "figure with no reader" class, and invisible to both oracles by §G-9 (neither models a Part II deduction). None of the six phase-3 mutations or four KATs exercises 9b ≥ line 8; the fixture's $60k of interest keeps the region unreachable.
- SEVERITY: **I** (a filed return missing a form its instructions require, with the tax-moving testimony unprinted; not C because the tax *figure* is what the filer's own allocation produces, and reaching the region requires an allocation no reasonable method yields — which btctax deliberately does not police).

**P3-2. The 9b refusal labels Schedule A line 5e as "§164(b)(6)'s cap" whenever 5d < the statutory cap.** `ScheduleAParts::salt_cap` is defined as `= salt_5e` (`return_1040.rs:474`), and the refusal prints it as *"after §164(b)(6)'s {cap} cap"* (`return_1040.rs:2514, 2527`). A filer with 5a = $4k, 5b = $3k (5d = 5e = $7k) entering 9b = $5k is told the §164(b)(6) cap is **$7,000** — the statute's cap is $10,000; $7,000 is their own 5e. The `nii_line9b_bound` doc's third min-term ("the cap … the statute's own ceiling, transcribed anyway") is likewise not what is coded — `.min(a.salt_cap)` is `.min(salt_5e)`, redundant by construction, not "while 5e ≤ cap". Bound *value* is right in all cases; the filer-facing label and the doc claim are wrong. SEVERITY: **M**.

**P3-3. The `Form8960Line9bNotClaimed` advisory asserts "your tax is currently OVERSTATED" unconditionally — false whenever line 15 binds.** Fires on `niit.tax > 0 && bound > 0` (`advisories.rs:837-841`) with text "so your tax is currently OVERSTATED" (`advisories.rs:512-521`). When MAGI-over-threshold (line 15) is smaller than NII (line 12) — wages $150k + $100k crypto gains: excess $50k < NII $100k, a *common* btctax shape — line 16 = line 15 and a 9b entry of any size up to the bound changes the tax by $0. The advisory tells that filer they are overpaying; they are not. No filed figure is wrong; a filer-facing surface states a false quantitative claim, and may induce a pointless sworn allocation election. SEVERITY: **M**.

Phase 3 otherwise held up well under attack: the bound's three branches are each independently kill-tested (the sales-tax-election two-mechanism demonstration is exemplary), blank-preservation is proven in both layers, the refusal correctly lives in `screen_absolute` per the phase-2 Critical's layering, the standard-deduction and sales-tax refusal messages are accurate, the i8960 ratio-method citation is verbatim-correct against the extract (`i8960--2024.txt:1800-1803`), and the foreign-tax exclusion reasoning checks out.

IS THE REFUSAL SURFACE COHERENT AS A WHOLE? **No — at two intersections.** The filer I tested hardest: an itemizing crypto-margin homeowner with a mixed-use, over-limit jumbo refi, a large crypto gift, margin interest on line 9, and a 9b entry. The escape-cascade behaves (each refusal names its own cure, and the chain "lower line 9 → election flips to standard → 9b now over a $0 bound → clear 9b" terminates with honest steps at every link — that came back clean, as did MFS forced-itemization against every itemize-gated refusal). What breaks is finding 3 (all three mortgage answers refuse a filer whose printed return the answer cannot move) and finding 1 (the one filer the surface should stop-and-ask — the standard-deduction deferral donor — is the one it never asks). One residue worth recording: `CharitableCwaUnresolved`'s `Some(false)` cure, "remove that gift from the deduction," has no in-product mechanism for a *ledger* crypto donation short of deleting or reclassifying a truthful event — the escape shape the branch's own 8283 comment condemns (`return_1040.rs:2239-2241`). The lawful outcome (don't deduct an unsubstantiated gift) is right; the product has no honest lever for it. (N, records with finding 2's cluster.)

WHAT WOULD MAKE THIS REVIEW WRONG: Findings 1–3 rest on code-reading rather than execution — if some path I did not trace poses the CWA question to standard-deduction filers, suppresses the debt-limit question once mixed-use zeroes 8a, or keeps Form 8960 in the packet when `form_8960_lines` is `None`, the corresponding finding collapses; I searched for all three (the skippable registry's "mandatory half is in screen_absolute" comment, `mortgage_question_live`'s definition, and `packet.rs:588`) and found none.
