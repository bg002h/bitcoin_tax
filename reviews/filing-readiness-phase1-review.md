# Phase 1 review — filing-readiness branch, scoped `3fc88497..HEAD`, pointed at INTERACTION

_Fable, read-only, dispatched per `reviews/BRIEF-filing-readiness-phase1.md` (copy of the scratchpad
brief). Whole-branch scope per harness B3: three lanes built in parallel worktrees off one base and
never saw each other's code. **Persisted VERBATIM before any fold.**_

★ **The agent could not write this file itself** — the harness blocks subagents from writing report
files ("Subagents should return findings as text, not write report files"). All four agents this
session hit it. The controller is therefore the scribe here, which is exactly the independence
property the agent-persists-its-own-report rule exists to protect. The mitigation used: this text is
copied verbatim from the agent's returned message, in its own commit, before any fold — so
`git diff <this commit>..<fold commit>` still means what it should.

---

VERDICT: needs-changes

SEAM FINDINGS (the point of this pass):

**F1 — Lane C's N1 advisory and the frozen M4 carryforward-consistency check now assert opposite figures about the same filer, and M4 tells the filer to doubt the correct one.**

- THE SEAM: lane C (btctax-core: `capital_loss_carryover.rs`, `advisories.rs`, `return_1040.rs`) against the untouched base-tree instrument `carryforward_consistency` (`crates/btctax-core/src/tax/compute.rs:444-456`), invoked from `crates/btctax-cli/src/cmd/tax.rs:541-561`. No lane touched M4, which is exactly why no lane's mutations could see this — it is the P2a/P2b shape with the base tree playing the second lane.
- WHAT EACH SIDE ASSUMED: Lane C assumed delivering the worksheet figure via an advisory ("★ THE WORKSHEET FIGURE IS THE CORRECT ONE. Enter it … as next year's capital-loss carryover in `btctax income import`") completes the loop. Its own test doc (`return_1040.rs`, `n1_a_loss_year_at_the_floor_carries_the_whole_loss`) names the harm: "next year's M4 consistency check then *disputes the correct figure* if they work the worksheet themselves" — written as the defect of the OLD flat rule. M4 assumed prior-year `compute_tax_year` (the frozen delta engine, structurally locked to the flat rule) is the authority on last year's carryforward-out.
- WHAT IS ACTUALLY TRUE: M4 still recomputes year Y−1 through the frozen engine and compares with exact equality. The N1 fix changed which number btctax *recommends*, not which number M4 *disputes*. So the dispute lane C's commit cites as motivation is not removed — it is now triggered by obeying btctax's own advisory.
- THE FAILING CASE: the L4 household itself. TY2024: Single, no wages, one $20,000 LT loss. `report --tax-year 2024` prints the flat "carryforward out: short $0 / long $17,000" plus the N1 advisory "CARRY $20,000, NOT $17,000". Filer obeys, enters $20,000 into 2025's inputs. `report --tax-year 2025` then prints: "ADVISORY (M4): carryforward_in (short 0 / long 20000) does not match prior-year carryforward_out (short 0 / long 17000) — **verify your prior return**." The filer verifies, finds the 2024 report's own printed line says $17,000, "corrects" to $17,000 — and 2025's Schedule D line 14 forfeits $3,000 of loss permanently (~$720 of overstated tax at 24%, per allowance-sized floor year). The path is confirmed live: `derive_tax_profile` carries `ri.capital_loss_carryforward_in` (`return_1040.rs:1179`) into the profile M4 reads, and the L4 prior year screens Ready (no carryforward-in), so M4 fires.
- The fix shape follows the repo's own excuse-list rule (state the mechanism, let it decide): M4 should compare against the prior year's worksheet-applied `AbsoluteReturn::capital_loss_carryforward_out` when full-return inputs exist, falling back to the flat figure otherwise — with a B1 pair (entered-worksheet-figure → silent, watched red first; entered-arbitrary-figure → disputes).
- SEVERITY: **I** — not C because M4 is non-gating, the wrong figure lands only if the filer obeys the weaker instrument, and the direction is overstatement; but two product surfaces now give one filer contradictory sworn-figure instructions, and the wrong one is phrased as an audit of the right one.

**F2 — N4's "COMPLETE BY HAND" manifest section does not know the packet can contain a Form 8283 needing two ink signatures.**

- THE SEAM: lane B (`hand_marks` in `crates/btctax-cli/src/cmd/admin.rs`) vs the 8283 that lanes A and C spent this branch working on, and which the full-return packet includes (`packet.rs:219-221`).
- WHAT EACH SIDE ASSUMED: lane B enumerated hand-marks from the 1040's view (digital-asset question, line-7 box, signature block) and titled the manifest section "marks btctax deliberately did NOT make". Lanes A/C established that a filed Section B 8283 additionally needs a signed Part IV (appraiser) and Part V (donee) — covered today only by a stderr note (`main.rs`, `form_8283_section_b == Some(true)` branch).
- WHAT IS ACTUALLY TRUE: lane B's own decision-13 rationale — the manifest is "the one artifact the filer is told to follow while assembling paper, and the one that does not scroll away" — applies with equal force to the 8283 ink blocks, which stayed stderr-only. A filer assembling from the manifest of an 8283-bearing packet sees "1 mark(s)" and an authoritative-sounding closed list.
- THE FAILING CASE: crypto donor, Section B 8283 in the packet. Manifest says the only thing left is the signature block; the two third-party signatures that make the 8283 filing-ready are named only in stderr that scrolled away. Filer mails an 8283 without Part IV/V — the exact "NOT filing-ready" state the stderr warns about.
- SEVERITY: **M** — the information exists on one surface; the defect is that N4's chosen surface under-enumerates, against N4's own stated rationale.

**F3 — the N1 advisory's "printed above" attribution is false on one of its two surfaces and can be false on both.**

- THE SEAM: lane C's advisory text ("the flat 'loss minus $3,000' figure **printed above** says {flat}") vs lane B-era §G-19d, which prints the same advisory on `export-irs-pdf` stderr — where no flat carryforward figure is printed at all. Additionally, `{flat}` is `capital_net`'s full-return netting (broker 1099-B totals joined, §G-28/B4), while the figure the *report* actually prints at render.rs:1339 is the delta engine's crypto-only carryforward — so on a floor household with broker capital losses the advisory attributes to "printed above" a number that appears nowhere ($17,000 claimed vs e.g. $7,000 printed). The bottom-line instruction (carry the worksheet figure) stays correct in both cases.
- SEVERITY: **M**.

SINGLE-LANE FINDINGS:

**F4 (lane C, Minor)** — four sites still state the worksheet is unmodeled, and N1 made all four false. The brief's known-open item covers the IN-side *refusal*; I am arguing the deferral line was drawn one notch too wide: keeping the refusal is right, but keeping its false *statements* was never part of that decision's risk rationale, and correcting strings widens nothing. The sites: the filer-facing refusal detail `"…is unmodeled in v1"` (`crates/btctax-core/src/tax/return_1040.rs:2397`), the `RefuseReason` doc (`return_refuse.rs:249-253`), the r3 I-4 writeback comment `"there is no capital-loss carryover-OUT on AbsoluteReturn to write, because the … Worksheet … is UNMODELED in v1"` (`return_1040.rs:2524-2531`) — both clauses now false in the very file lane C edited — and a test doc repeating it (`return_1040.rs:6970-6971`). This is precisely the stale-claim class this branch's own P10 items fixed three times; lane A's stale-refusal sweep was scoped to btctax-forms and could not see these. The writeback comment is the dangerous one: a future reader consulting it will conclude there is nothing to write, when the field now exists and the open question (should `--write-carryover` roll it, with provenance) deserves a filed follow-up rather than a comment asserting the field's nonexistence.

**F5 (lane B, Minor)** — `render_charitable_carryover_out`'s closing sentence, "Your {year} gifts exceeded their §170(b) ceiling, so the excess is NOT deducted on this return," is false for re-carried prior vintages. `apply_170b`'s `carryover_out` includes unused, unexpired *carryover-in* items with their original `origin_year` (aging per Reg §1.170A-10(a)(2) — the writeback's own comments rely on this), so a household whose current-year gifts fit under the ceiling — or that made none — can still get the block, asserting a fact about "your {year} gifts" that is not true. The per-item lines (vintage, class, usable-through) are correct; only the summary sentence over-claims. Lane B's two fixtures both use current-year-only gifts, so its B1 pair could not see this.

**F6 (lane A, Nit)** — un-mapping the three 2025 column-(i) cells leaves them accounted for by no instrument: `census_accounts_for_every_field` is hardcoded to `year = 2024` (`crates/btctax-forms/tests/field_census.rs:105`), and the 2025 map has no `[census]` section (stated in the map comment). The blank *value* is pinned by `sp2::form_8283_section_b_checks_digital_assets_box`, so this is recorded-reason-lives-in-a-comment rather than a live gap — but by the repo's provenance doctrine a comment is not a census, and when the census program reaches 2025 nothing forces these three onto it.

Dispositions of the brief's offered seams: (1) clean — I diffed the part labels in both extracts myself: `f8283--2024.txt:108/116/136` and `f8283--2025.txt:105/113/133` agree exactly (III Taxpayer / IV Appraiser / V Donee), donation.rs, form8283.rs's dual-numbering comment, and both maps are mutually consistent. (2) right call — the Rev. 12-2014 authority is genuinely absent from the repo, the map records what would close it, and `sp3b` pins TY2017 byte-identical; inventing the 2014 rule would be the worse error. (3) became F1 and F3 — the mechanism-based firing condition itself is right (I traced applicability limbs (a)/(b) and the None⇔zero boundary; the grid test covers the equivalence region), but the advisory's *ecosystem* is not. (4) clean on files (manifest rows are written from the same `packet.forms` loop that writes the PDFs; hand-marks and Schedule D presence read the same `must_file()` predicate), incomplete on marks — F2. (5) clean — `None` reaches the emitter only on revisions whose instructions do not ask the cell of this filer; the write is `if let (Some(cell), Some(ded))`, TY2017 semantics unchanged, and the 2024 mutation reds two independent instruments; residue is F6. (6) clean — no masking in either direction: the xtask B1 test manufactures its own poisoned `GIT_DIR` so the hook-level `unset` cannot hide it, and half 2 asserts the `env_remove` list directly; the pre-commit's extra `GIT_PREFIX` unset is harmless surplus, not a divergence.

WHAT WOULD MAKE THIS REVIEW WRONG: It trusts the commit messages' mutation evidence rather than re-running it, and F1 rests on code-reading, not execution — if some path I did not trace suppresses M4 for a Computed-prior full-return year (I found none: the only gate is prior-year `TaxOutcome::Computed` through the frozen engine, which the L4 household satisfies), the Important downgrades to the F4 doc class.
