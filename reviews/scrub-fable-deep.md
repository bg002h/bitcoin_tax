# Fable deep pass — `btctax income scrub` (POST-PUBLISH, v0.17.0)

_Fable, read-only, dispatched against `reviews/BRIEF-scrub-fable-deep.md`. The tenth pass on this
feature and the first after publication. Asked ONE question: what is still green and blind?
Persisted VERBATIM before any fold._

---

VERDICT: needs-changes

STILL GREEN AND BLIND:

INSTRUMENT: `the_surviving_sentinels_are_exactly_the_deliberately_kept_fields` — `crates/btctax-core/src/tax/scrub_axis.rs:456`
APPEARS TO GUARANTEE: Both directions of §7 — "anything extra is identity riding into a shareable file; anything missing is a KEPT decision silently reversed." A reader takes this as the backstop against a wrong classification when a new field is added: the one instrument that would catch identity surviving a scrub.
ACTUALLY ASSERTS: Membership of a **hand-list of 18 tokens** in the scrubbed JSON. `survived` is built by iterating the list and asking `json.contains(kept)` — never by scanning the JSON for the `SENTINEL_` prefix. A surviving token that is not one of the 18 is invisible by construction. The reversed-KEPT direction is genuinely held; the "anything extra" direction holds only for extras the list already names, which is no extras at all. The test's own comment records that three entries once named tokens that no longer existed — the fold removed the stale entries (false coverage) but kept the scan direction that makes *fresh* tokens invisible. Same instrument, same blindness, one level down: the exact shape of all nine prior passes.
THE MUTATION IT WOULD SURVIVE: Add a new free-text field — the realistic case is a whole new form type, e.g. `Form1099Misc { payer, ... }`, which is precisely how the six missed structs entered in the first place. The `..`-free destructures force two edits: the author classifies it `_` ("money only" — wrong, `payer` is free text) in `scrub_pii`, and gives it `"SENTINEL_misc_payer"` in `maximal_sentinel`. Scrub keeps it; the derived axis shows no diff, so the matrix demands no row; `the_identity_does_not_survive` does not know the field; the figure test compares two identical values; the round trip is green; and this scan never looks for the new token. A filer's real free text then rides into a file the command stamps shareable — the v1 `business_description` failure re-run with every instrument green. The mechanism-true form is one line away: collect every `SENTINEL_`-prefixed substring actually present in the scrubbed JSON and assert that *found set* equals `{relationship, naics}`. B1 kill: plant a kept `SENTINEL_` field not in the list, observe red.
SEVERITY: I

INSTRUMENT: `the_scrubbed_toml_round_trips_back_through_import` — `crates/btctax-cli/tests/scrub_refusal.rs:631`
APPEARS TO GUARANTEE: The recipient receives what scrub produced — the artifact boundary is faithful, so everything §3.2 proves in memory reaches the stranger's vault.
ACTUALLY ASSERTS: Byte-boundary fidelity for exactly **one point in the output space: the all-valid class**. `scrub_pii(maximal_sentinel())` contains zero empty strings, zero malformed values, and no `ip_pin` key at all (valid is dropped) — because the sentinel *must* be all-valid so the matrix baseline files. Two design constraints pull the same fixture in opposite directions, and the round trip inherited the matrix's constraint. §3.2's entire product — `absent → absent`, `Some("") → Some("")`, `malformed → synthetic-malformed`, `"x"`, `"1".repeat(n)` — is proven by in-memory value comparison and has **never once crossed the emit/parse boundary in any test**. The brief's hint 3 is right, and this is its sharpest instance: the feature's guarantee is "the recipient's *copy* refuses where the filer's did," and no malformed-class copy has ever been created.
THE MUTATION IT WOULD SURVIVE: Any transport-layer normalization of the non-valid classes. Concretely: a plausible emit-path "tidy" that drops `key = ""` lines from the TOML (or a parse-path trim). `scrub_ip_pin`'s `Missing` leg emits `Some("")` precisely so the recipient's `ReturnHeader::build` still errs `Missing`; drop that line and the recipient gets `None`, which builds `Ok` — the copy exports where the filer's refused, §3.2's governing harm, delivered through §4's transport. The round trip's cargo has no empty string, so the tidy changes no byte of the test's file; the matrix is in-memory and stays green; the marker tests are indifferent. Nothing reds. B1 kill: round-trip a sentinel variant carrying `ip_pin = Some("")`, a `"x"` SSN, and an 8-digit EIN, asserting the landed *classes*.
SEVERITY: I

INSTRUMENT: the per-cell assertion trio of `every_replaced_field_preserves_its_class_in_every_representable_state`, most narrowly `assert_emptiness_class_preserved` — `crates/btctax-core/src/tax/scrub_axis.rs:828` (walk), `:893` (loop)
APPEARS TO GUARANTEE: The name: every replaced field preserves its class in every representable state.
ACTUALLY ASSERTS: Class preservation of *one field off-baseline at a time*, observed through three comparisons that all short-circuit or truncate: `screen_inputs` first-refusal, `ReturnHeader::build` first-error, and a `zip` walk whose Object arm silently skips one-sided keys and whose Array arm iterates to the shorter side. Structure (collection length, key presence) is mechanically held **only on the unmutated base** — a length diff there lands `path[]` in the derived axis and check (a) reds on the missing row. Per cell, in a mutated state, nothing holds structure at all. The brief's hint 4 was argued from "scrub changes no collection length today"; the sharper fact is that the base-axis backstop everyone relied on *does not run* where the walk runs.
THE MUTATION IT WOULD SURVIVE: A state-conditional structural change — e.g. `scrub_header` clears `dependents` whenever `spouse` is `None`. Base axis: spouse present, no length diff, no row demanded. `the_identity_does_not_survive`: plants a spouse before asserting dependent length. Figure test: nothing computes from dependents (no CTC), all four fixtures equal. The spouse-absent cells: the walk zips 2 dependents against 0 and iterates zero times; refusal and header-error comparisons compare first-wins values dependents never reach. Entire suite green — and a single filer with a malformed dependent SSN is handed a copy that exports where the original refused at `packet.rs:425` (verified: `Ssn::canonical(&d.ssn)?` in the dependents map). B1 kill: make the walk assert array lengths and object key-sets equal before zipping (with the `ip_pin` Null transition as the one recorded exception), and plant the conditional drop.
SEVERITY: I

INSTRUMENT: the `w2s[].ein @ malformed` cell's discrimination liveness — `crates/btctax-core/src/tax/scrub_axis.rs:184` (the arming value), `:725-729` (the cell)
APPEARS TO GUARANTEE: The cell that caught r1's Critical stays armed: a malformed-EIN upgrade reds the matrix.
ACTUALLY ASSERTS: Refusal equality on a fixture whose over-cap state is held by a **comment**. The baseline assertion at `:912` polices one direction only (a refusing baseline). The arming precondition — Σ box3 = $12,000 > $10,453.20, which is the entire difference between this cell discriminating and the "EXERCISED AND VACUOUS" state the comment describes shipping r1's Critical through — is asserted nowhere. This is the answer to the brief's hint 1 beyond `box12: vec![]`: check (b) plus the fixed row table mechanically polices *presence/emptiness* drift of the sentinel, but **value drift that keeps a path in the axis while disarming the cell's screen** is invisible. `foreign_accounts: Some(true)` is the same family (though the emptiness walk still discriminates that cell); `box4_ss_withheld == box3_ss_wages == 6000` — a 100% withholding rate — is exactly the "obvious typo" a future tidy normalizes.
THE MUTATION IT WOULD SURVIVE: Lower the sentinel's `box3_ss_wages` to a normal-looking figure. Baseline still files (green), the cell still runs, `over_cap_needs_ein` is dead, and the cell reverts to comparing `None == None` — vacuous, silently. The defect *class* stays held by `a_malformed_ein_stays_malformed_...`, whose fixture is self-contained — which is why this is M and not I. B1 kill: assert in the matrix that the unscrubbed base with malformed EINs actually refuses `ExcessSsEmployerUnknown` (a positive control for the cell, the same shape as the baseline control).
SEVERITY: M

INSTRUMENT: the "and it must be scrubbed" canary in `a_ledger_free_vault_still_scrubs` — `crates/btctax-cli/tests/scrub_refusal.rs:391`
APPEARS TO GUARANTEE: The bytes the positive-control emits are scrubbed — the one byte-level identity-absence assertion on the CLI path.
ACTUALLY ASSERTS: That `"111-22-3333"` is absent from stdout. That literal is a dependent SSN in the kitchen-sink fixture (`testonly.rs:265`) and the committed example TOML — the vault this test builds via `store_wage_only_return` uses `not_a_dependent()`, which is a **default header: every identity string is `""`, no dependents, no IP PIN**. The canary scans for a value that was never stored; it cannot fire on any defect, including `scrub_pii` being the identity function. This is the third instance of the recorded class "a scan entry naming a token that cannot occur" — this time in the test whose job is being the positive control.
THE MUTATION IT WOULD SURVIVE: Emitting the return unscrubbed on stdout — though today that specific swap is structurally backstopped (one `toml` string feeds both stdout and `--out`, and the round trip compares `--out` against an in-test `scrub_pii`), which is why this is M. The fix is to store a return that *carries* identity and key the canary on it.
SEVERITY: M

INSTRUMENT: `no_fixture_value_collides_with_a_stand_in` — `crates/btctax-core/src/tax/scrub_axis.rs:509`
APPEARS TO GUARANTEE: "No stand-in string scrub can emit may appear anywhere in the ORIGINAL fixture."
ACTUALLY ASSERTS: Absence of a 15-literal hand-list — missing the synthetic SSN/EIN families, `"x"`, the `"1"`-repeats, and every index-2 stand-in (`Payer2`, `Dependent2`, ...). Backstopped at field granularity: a colliding field's path leaves the derived axis and its fixed matrix row reds via check (b). Blind only at element granularity, where nothing downstream currently loses discrimination — recorded, not gating.
THE MUTATION IT WOULD SURVIVE: Setting the fixture's second W-2 employer to `"Employer2"` — element 1 still diffs, the path stays in the axis, the scan checks `"Employer1"` only.
SEVERITY: N

On the brief's four hints: 1 is right and has more members than `box12` (finding 4); 2 resolved into finding 3 — first-wins is harmless per cell in isolation but is one of the three short-circuits that jointly blind the matrix to conditional structure; 3 is the sharpest hint and is finding 2; 4 is real but the zip is the smaller half — the base-only scope of the length backstop is the half that bites.

WHAT THIS FEATURE STILL CANNOT DO SAFELY: It cannot survive a wrongly-classified future field — the only gate is a human decision at a compile error, and every instrument behind that decision either hand-lists tokens or diffs a fixture that a wrong KEEP never perturbs. It proves §3.2's class preservation in memory and §4's transport for the valid class only; their composition — a malformed or empty class crossing the artifact boundary — is proven nowhere. And every instrument observes one field off-baseline at a time, so joint and conditional states sit outside the entire suite's field of view.

WHAT WOULD MAKE THIS REVIEW WRONG: It assumes `scrub_refusal.rs` plus the two core test modules are the complete instrument surface — verified by grepping for scrub call sites, `SENTINEL_` scans, and the SSN literal across `crates/` — so an instrument living elsewhere under a name mentioning none of these would blunt the blindness claims, most directly findings 1 and 2.
