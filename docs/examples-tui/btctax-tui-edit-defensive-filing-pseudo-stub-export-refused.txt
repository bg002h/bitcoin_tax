── glyphs ──
  0│┌ btctax-tui-edit [EDITOR] — Defensive Filing ─────────────────────────────────┐
  1││ ⚠ EXPERIMENTAL — DEFENSIVE FILING (declare/promote tranche, Form 8275, estima│
  2││Defensive Filing — journey dashboard (derived; nothing here is filed until you│
  3││act)                                                                          │
  4││                                                                              │
  5││UNCOMPUTABLE — this dashboard could NOT be computed: pseudo-reconcile         │
  6││synthetic defaults are contributing to this projection. This is NOT a         │
  7││statement that nothing is outstanding — an uncovered disposal may exist and   │
  8││would be MISSING from Form 8949 until it is declared. Resolve or approve the  │
  9││pending defaults ('P' from Browse), or turn pseudo mode off via the CLI's     │
 10││reconcile command, then re-enter. 'x' (export) still re-projects fresh and may│
 11││itself refuse.                                                                │
 12││                                                                              │
 13││Note: an in-force safe-harbor allocation or a pre-2025 tranche is present —   │
 14││the two are mutually exclusive.                                               │
 15││                                                                              │
 16││[x] export — plan the IRS-PDF packet (always available)                       │
 17││                                                                              │
 18││  export refused: usage: cannot export: the ledger is pseudo-reconciled (a    │
 19││synthetic, non-persisted default contributes to the projection) — this        │
 20││composed export step never prompts for an attestation override. Run `btctax   │
 21││reconcile pseudo off` (or approve + attest the defaults through the           │
 22││single-year `export-irs-pdf`/`export-snapshot` CLI path) before exporting.    │
 23│└──────────────────────────────────────────────────────────────────────────────┘
── styles (runs; terminal-default fg=Reset bg=Reset no-modifier omitted) ──
  1│ 1..79 fg=Yellow mod=BOLD
 18│ 1..75 fg=Cyan
 19│ 1..71 fg=Cyan
 20│ 1..76 fg=Cyan
 21│ 1..68 fg=Cyan
 22│ 1..75 fg=Cyan
